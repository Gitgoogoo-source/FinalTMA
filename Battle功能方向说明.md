# Battle 功能方向说明

## 文档范围

本文是产品裁决来源记录，保留形成 Battle 完整方案时已经确认的玩法边界，不作为实现、接口或运行时读取的独立事实源。当前唯一产品事实统一引用 [主功能文档第 21 章](docs/product/功能说明文档.md#21-宠物藏品-battle-功能说明)，完整开发约束统一引用 [Battle 功能开发方案](Battle功能开发方案.md)；字段、状态、金额、时限、错误或接口发生引用关系时不得从本文复制维护第二个版本。

## 一、产品目标与边界

- Battle 是当前 Telegram Mini App 内独立的宠物藏品 PvP 功能。
- Battle 的目标是提高盲盒宠物藏品的使用价值、进化价值和玩家长期回访。
- Battle 不设置开放 RPG 大世界，不提供地图探索或捕捉玩法。
- Battle 同时提供玩家主动分享挑战卡的好友邀请房，以及 20/100/500 三档严格隔离的公开随机匹配；不提供公开房间列表。

## 二、宠物来源与固定模板

- Battle 只能使用玩家在当前 TMA 中真实拥有的宠物藏品。
- 系统只有现有的 210 个固定宠物模板。
- 宠物不能通过 Battle 捕捉、掉落或免费获得。
- 宠物不能培养、升级、学习新技能或更换技能。
- 现有 `combat_power` 继续作为藏品的综合展示值，不直接参与实际战斗计算。
- 实际战斗使用每个模板固定配置的生命、攻击、防御、速度、属性和按阶段拥有的 2/3/4 个技能。

## 三、稀有度、进化与强度

- 高阶段、高稀有度宠物必须在实际战斗中比低阶段、低稀有度宠物更强。
- 五档稀有度使用以下固定强度系数：

| 稀有度 | 强度系数 |
| ------ | -------: |
| 普通   |     1.00 |
| 稀有   |     1.15 |
| 史诗   |     1.32 |
| 传说   |     1.52 |
| 神话   |     1.75 |

- 同一稀有度的模板使用相同总平衡预算，不同稀有度使用随上表递增的总平衡预算。
- 同一进化链的三个阶段保持相同战斗定位、相同属性和相同四技能候选池，实际技能按阶段形成连续前缀。
- 进化到下一阶段后，生命、攻击、防御和速度四项都必须严格提高。
- 稀有度强度通过固定战斗四维体现，阶段技能成长通过模板固定的 2/3/4 个技能体现；不使用 `combat_power`、阶段编号或其他隐藏倍率重复加成。
- 高稀有度形成明确战斗优势，不强制双方使用相同稀有度、相同阶段或相同总强度。

## 四、对称平衡档案

- 五个属性各有 42 个固定实际平衡档案，对应各属性的 42 个宠物模板。
- 每个档案固定生命、攻击、防御、速度和四个技能候选槽位。
- 五个属性使用完全相同的 42 档数值生态。
- 五属性之间只替换模板、属性、技能名称和视觉效果，不改变对应档案的数值强弱。
- 同一属性内允许通过四维分配、速度差异和四技能候选池形成不同战斗定位。
- 玩家不能自行调整四维、属性或技能组合。

## 五、属性与克制

- Battle 只使用五个单一属性：火焰、草系、土系、雷电、水系。
- 唯一克制循环为：

```text
火焰 → 草系 → 土系 → 雷电 → 水系 → 火焰
```

- 属性倍率固定为：

| 关系             | 伤害倍率 |
| ---------------- | -------: |
| 克制             |     1.50 |
| 被克制           |     0.75 |
| 同属性或其他关系 |     1.00 |

- 不设置特殊属性、无属性或双属性。
- 同一进化链的三个阶段保持同一属性。

## 六、技能

- 每个属性建立 10 个共享技能，五个属性共 50 个技能。
- 五个属性的 10 个技能使用完全相同的 10 组基础威力、命中率和动画轨迹配置。
- 五个属性分别使用不同的技能名称和视觉效果。
- 每条进化链固定搭配所属属性技能池中的 4 个候选技能，按基础威力从低到高形成唯一成长顺序；威力相同时保持候选池原始顺序。
- 模板有效威力在构建期按技能位置固化：普通链保留基础威力，高级链四个位置最低为 `71/81/96/126`，顶级链最低为 `72/82/106/127`；同一阶段、同一位置的稀有度越高，有效威力必须严格更高。
- 1 阶模板固定拥有前 2 个技能，2 阶固定拥有前 3 个，3 阶固定拥有全部 4 个；技能位置连续编号为 `1..N`。
- 同一技能允许被多个模板复用，不同模板通过固定候选池形成差异。
- 玩家不能学习、更换、手动解锁或重排技能；未拥有技能不进入数据库返回、API 或页面。
- 候选技能全部是同级主动直接伤害技能，不划分基础、战术或强力类别。
- 不设置被动技能、冷却、使用次数、能量消耗、异常状态、能力变化、治疗或护盾。
- 每只宠物实际拥有的技能都与该宠物属性一致。
- 模板不保证拥有 100% 命中技能，不为命中率调整威力排序。

## 七、伤害与随机性

- 技能命中后的伤害只由攻击者固定攻击、防守者固定防御、房间快照中的模板技能有效威力和属性克制计算。
- 不设置暴击。
- 不设置随机伤害浮动。
- 唯一随机结果是根据技能固定命中率判断命中或未命中。
- 相同战斗状态、相同行动和相同命中结果必须得到相同伤害。

## 八、三宠组队

- 每名玩家必须选择 3 个不同的宠物模板。
- 同一进化链的不同阶段属于不同模板，允许同时上阵。
- 组队时必须排列三只宠物的固定顺序。
- 第 1 位宠物自动成为首发，不增加额外首发选择环节。
- 每次只有一只宠物处于场上，其余存活宠物作为替补。

## 九、藏品占用

- 好友房创建者创建挑战房时，立即占用所选三个模板各 1 份。
- 好友对手成功接受时，以及公开匹配玩家点击匹配时，都在各自数据库事务内占用所选三个模板各 1 份并锁定对应档位入场费。
- 等待和战斗期间，被占用的具体数量不能用于出售、分解、进化、远征或 Mint。
- 同一模板拥有多份时，Battle 只占用其中 1 份，剩余真实可用数量不受影响。
- 战斗结束、房间取消、房间过期或挑战创建失败后，全部 Battle 占用立即释放。

## 十、挑战档位与 Stars

- 挑战房只允许以下三个固定入场费档位：

| 每人入场费 |   双方奖池 |  胜者获得 | 平台手续费 |
| ---------: | ---------: | --------: | ---------: |
|   20 Stars |   40 Stars |  36 Stars |    4 Stars |
|  100 Stars |  200 Stars | 180 Stars |   20 Stars |
|  500 Stars | 1000 Stars | 900 Stars |  100 Stars |

- 好友房创建者选择档位后，创建挑战房时立即锁定对应 Stars；好友对手成功接受时锁定同额 Stars。
- 公开匹配玩家点击匹配时立即原子锁定对应 Stars 和三宠；匹配成功后在加入事务内直接锁定 3 秒开战倒计时并转入既有结算链路，取消或 120 秒超时则原额解冻并释放三宠。
- 胜者获得双方奖池总额的 90%，剩余 10% 为平台手续费。
- 平局时双方原额退还，不收平台手续费。
- 房间合法取消、过期或挑战创建失败时，创建者获得原额退款。
- prepared challenge 创建明确失败形成的 `voided` 只存在创建者已锁定的一份 stake：该 stake 原额退款、三份藏品占用释放、Battle settlement 为 0；双人 lobby 或战斗不变量形成的 `voided` 仍保留双方安全作废 settlement 与审计。监控必须分别验证，不能忽略全部 `voided`。
- 接受失败或接受竞争失败的玩家不扣 Stars。
- Stars 可以通过 Telegram Stars 购买，不能提现，也不能在玩家之间自由转账。

## 十一、账号参与限制

- 每个账号最多只能处于一个 `preparing_share`、`waiting`、双人 `lobby` 或已开战房间。
- 正在创建挑战卡、等待邀请或匹配、双人等待或战斗中的账号不能创建、匹配或接受其他挑战；充值补差也必须由数据库按这四种参与状态统一拒绝。
- 当前房间结算、取消、过期或创建失败并完成退款与藏品释放后，账号才能参与下一场。

## 十二、挑战卡与分享

- 挑战卡属于一次性持卡邀请。
- 创建者必须先成功创建挑战卡，之后才能调用 Telegram 原生分享功能发送。
- 挑战卡允许发送到 Telegram 用户私聊、好友或任意 Telegram 群组。
- Battle 不要求游戏 Bot 加入目标群组。
- 挑战卡被转发到其他私聊或群组后继续有效。
- 除创建者本人外，任何拿到有效挑战卡的正常账号都能尝试接受。
- 最先成功接受的玩家成为唯一对手，其他玩家不能再接受该房间。
- 不校验接受者是否属于最初收到挑战卡的群组。

## 十二-A、公开随机匹配

- 玩家在三槽队伍选择页点击“随机匹配”即同意对战，不再经过对手接受确认。
- 20、100、500 三个档位形成三个完全隔离的候选池；只允许同一规则版本、同一档位玩家互相匹配，禁止跨档。
- 点击匹配的单一数据库事务先校验身份、唯一参与、档位、余额、库存与队伍，再锁定入场费并占用三宠。
- 当前档位存在公开 waiting 房时，数据库从全部合格候选中随机锁定一个并把当前玩家作为陌生对手加入；不存在候选时，当前玩家自动创建该档公开房并等待。
- 公开房等待固定 120 秒，期间创建者可以主动取消；取消或超时原额解冻并释放三宠，不产生对战结算。
- 好友邀请房不进入公开候选池，公开房不生成 invite token、挑战卡或公开房间列表；两种房间共同复用既有双人 lobby、倒计时、战斗与结算，但公开匹配成功后固定直接进入不可撤销的 `lobby_countdown`，不再等待 presence 条件。
- 同档“查找或创建”由数据库事务 advisory lock、随机候选 `FOR UPDATE` 行锁、唯一参与约束和幂等 operation 裁决；候选短暂被心跳、取消或到期事务占用时等待并重新裁决，禁止跳过仍有效候选、重复冻结、一房多对手或重复建房竞争结果。

## 十三、挑战信息公开范围

- 挑战卡和接受页面只公开创建者三只宠物的稀有度组合，例如 `史诗 ×2、传说 ×1`。
- 挑战卡和接受页面不公开创建者的宠物模板、属性、固定四维、技能或 `combat_power`。
- 接受者根据稀有度组合自行决定是否接受，不强制双方同档强度。
- 双方阵容锁定前，彼此不能看到对方的具体三宠阵容。
- 战斗开始后，双方可以看到对方三只宠物的具体模板。
- 战斗开始后，不直接展示对手宠物的固定四维、技能列表和属性。

## 十四、等待房与在线要求

- 挑战卡创建成功后，房间等待有效期固定为 30 分钟。
- 创建者在对手成功接受前允许随时取消。
- 创建者在线状态只作展示；创建者离线不阻止对手选择队伍或接受，也不触发等待邀请取消。
- 邀请 waiting 只有创建者发送纯展示心跳；进入双人等待房后双方心跳才共同裁决 presence。
- 有效邀请打开后直接显示接受者队伍选择；创建者本人入口显示不可接受的本人状态。
- 好友首位接受者在同一事务锁定三宠与同额 Stars，并按双方在线事实进入最长 5 分钟双人等待房或 3 秒倒计时；公开匹配加入者完成同样锁定后固定直接进入 3 秒倒计时。两条路径都不在加入事务创建第 1 回合。
- 双方在等待房内每 5 秒发送心跳；最近一次服务端心跳不超过 10 秒时判定在线。
- 每次页面可见、Telegram 激活并停留在游戏页的时段都是独立 presence 生命周期，使用数据库版本、UUID lease 和该 lease 内递增命令序号。页面隐藏、Telegram deactivated、`pagehide` 或离开游戏页立即结束当前 lease、中止在途心跳并尽力发送 offline。
- 当前创建者 `waiting` 房间实际打开原生分享面板后，属于同一分享尝试的 `shareMessage` callback、`shareMessageSent` 或 `shareMessageFailed` 是面板流程已经结束的平台完成信号。Web 保存当前有效完成信号；`/game` 已可见时立即恢复，完成信号先到时则在紧随其后的可见、页面显示、聚焦或激活事件中消费。即使没有再收到 `activated` 且 `Telegram.WebApp.isActive` 仍滞后为 `false`，也必须先 REST 回正；平台已经结束 lifecycle 时取得数据库认可的新 lease，平台只产生 `blur` 且原 lease 仍 active 时继续递增其命令序号。不得继续使用已经结束的旧 lease，也不得把分享结果当成房间或业务成功。
- 重新可见、激活、返回游戏页、页面重载或重新认证时，必须先读取数据库事实并取得数据库认可的新 lease；新 lease 接管后，旧 heartbeat/offline 无论重复、乱序或迟到都永久失效。倒计时开始前，合法新命令仍可改变在线状态与重连窗口；倒计时开始后，任何新旧命令都不得清空、延后或重置开战 deadline，也不得改变资产。该保证不依赖浏览器 abort 或 offline 成功送达。
- 数据库尚未进入 3 秒倒计时时，任一方离线后拥有连续 90 秒恢复窗口，5 分钟等待房硬边界继续生效；创建者在接受前已经离线时，其 90 秒从接受事务成功时重新开始。
- 双方同时在线且完整 3 秒能够落在 5 分钟等待房边界内时，数据库在同一房间锁内原子写入固定 `lobby_start_deadline` 并进入 `lobby_countdown`；该状态形成不可撤销的本场参战锁定。
- 从 `lobby_countdown` 开始到进入战斗，页面隐藏、Telegram deactivated、`pagehide`、离开游戏页、后台、断网、offline、lease 结束、刷新、重认证、旧请求或乱序请求只更新或回正 presence，不得取消、暂停、清空、延后或重置 `lobby_start_deadline`，也不得退款、释放占用或退出本场战斗。
- 倒计时到期时数据库不再以双方在线作为开战前置条件，在同一房间锁内 exactly-once 创建第 1 回合、写唯一 `battle_started` event/outbox 并进入战斗；重复 tick、请求重放和服务恢复只能读取同一结果。只有既有永久不变量损坏仍走幂等安全作废事务。
- 创建第 1 回合前，数据库在同一房间锁内复核双方身份、两份入场 stake 与账本、每方三份合法队伍快照、六个 Battle 占用和全部启动条件；永久错误不得开战，必须走同一幂等安全作废事务。后台监控取得同一房间锁后复用该路径，不把接受事务的中间态判为错误。
- 在 `lobby_waiting` 中，任一方连续离线满 90 秒、等待房满 5 分钟或参与者被封禁时，数据库终结房间；双方原额退款、六个藏品占用释放且不收手续费。倒计时锁定后这些 presence、页面或账号生命周期变化不再取消本场；开战后的离线继续适用既有超时托管规则。
- 未锁定时，5 分钟边界优先阻止新的倒计时；倒计时只能在完整 3 秒不越过该边界时启动，一旦启动即由锁定 deadline 裁决开战。接受后不提供玩家取消、分享或重新选择队伍。
- `lobby_countdown` 前端固定切换为全屏红蓝对决倒计时专用页面，覆盖顶部资产栏、主导航和全部产品内按钮，明确显示“倒计时已锁定”“离开不会取消战斗”；页面内不存在取消、退出、分享或重新选队动作。Telegram 宿主仍可物理关闭或切走窗口，但这不构成业务取消能力。
- 等待房左侧固定使用红方正方形 WebP，右侧固定使用蓝方正方形 WebP，不使用真实用户头像；在线同时显示彩色图片、状态点和“已进入房间”，离线显示中性用户图标、文字和权威重连剩余时间。
- 普通 5 秒心跳只回正当前房间，不刷新顶部资产或藏品；heartbeat/offline 实际触发取消、过期、退款或释放时，页面立即刷新 Battle、顶部余额和 `battling`，不得每 5 秒无条件刷新资产与库存。

## 十五、正常回合

- Battle 采用固定先手、双方严格轮流行动的 3v3 回合制，不存在双方同时选择或秘密锁定动作。
- 开战事务只比较双方首发宠物速度；速度高的一方成为整场固定先手，速度相同由挑战房创建者成为固定先手。换宠后不重新计算先手。
- 固定先手行动一次、固定后手行动一次，合计 1 个完整回合；每场最多 20 个完整回合。
- 每次只向当前行动者开放独立的 15 秒；其动作结算提交数据库后，服务端立即切换行动权并开始另一方新的 15 秒。
- 当前宠物存活时，行动者只能选择使用当前宠物的一个技能，或主动换入一只存活替补；主动换宠完成后本次行动立即结束。
- 玩家点击技能或换宠目标后立即提交，不增加确认步骤；服务端按当前 room、回合、行动序号和行动方原子裁决，旧请求与非行动方请求不能改变状态。
- 前端点击攻击后立即播放施法、移动或弹道动画；命中、未命中、伤害、生命变化和击倒必须等待服务端权威结果。
- 服务端动作结算后，下一行动者的倒计时与按钮立即开放；上一动作仍在播放时不能遮挡或阻止操作，后续动作的表现按服务端序号排队播放。

## 十六、行动超时与断线托管

- 当前宠物存活时，行动超时固定使用该宠物技能位置 1。
- 当前宠物已经倒下但仍有存活替补时，同一个 15 秒内必须完成“选择替补宠物＋选择该宠物技能”；超时按开战前队伍顺序换入第一只存活宠物并使用其技能位置 1。
- 每次超时都自动选择技能，不设置累计超时判负。
- 战斗开始后，玩家退出或断线不会取消对战。
- 断线玩家允许持续由系统按超时规则自动出招，直至战斗正常结束。

## 十七、换宠与攻击顺序

- 开战后的攻击顺序只由固定行动权决定，技能数值不改变行动顺序。
- 主动换宠只切换当前宠物并结束本次行动，对方随后按自己的独立行动攻击换入宠物。
- 所有宠物切换都必须由数据库在同一事务内完成唯一 active 宠物切换，不得因内部唯一性检查把合法动作作废。
- 当前行动者的攻击结算完成后才切换行动权，每个动作快照只产生单边攻击结果，被击倒宠物不能继续出招。

## 十八、倒下后的换宠反击

- 当前宠物被击倒且队伍仍有存活宠物时，轮到该玩家即进入 `replace_attack` 行动模式，并只获得一个新的 15 秒行动窗口。
- 该玩家必须先选择一只存活替补，再从该替补真实拥有的技能中选择一个技能；点击技能后把“换宠＋攻击”作为一个不可拆分动作提交。
- 本人点选存活替补后，客户端立即在我方战场显示该宠物并进入技能选择；该显示只属于本人当前行动的本地预览，不提交动作，也不向对手展示。
- 数据库在同一事务中完成换入、攻击、生命变化、击倒、终局检查和行动权转换；换宠与攻击不能拆成两个请求。
- 超时固定按开战前队伍顺序选择第一只存活替补，并使用该宠物技能位置 1。
- 当前宠物被击倒且全队没有存活替补时立即结束战斗，不开放换宠或反击。

## 十九、战斗结束与第 20 回合裁决

- 一方三只宠物全部倒下时，另一方获胜。
- 单行动者状态机不存在一次动作同时击倒双方最后宠物的结果。
- 正常战斗最多进行 20 个完整回合；必须完成第 20 回合先手与后手两次行动后才执行回合上限裁决，中途一方全灭则立即结束。
- 第 20 个完整回合的第 2 次行动结算后仍未产生胜负时，先比较双方存活宠物数量。
- 存活数量较多的一方获胜。
- 存活数量相同时，比较双方三只宠物的剩余生命百分比总和。
- 剩余生命百分比总和较高的一方获胜。
- 两项仍完全相同时，结果为平局。
- 平局时双方原额退还 Stars，不收平台手续费。

## 二十、认输、观战与结果记录

- Battle 不提供认输功能。
- 战斗开始后只能按照正常规则完成结算。
- Battle 不提供观战功能。
- 用户侧不提供对战历史页面。
- 用户侧不提供逐回合回放。
- 用户只在当前前台会话中查看当前这场对战的最终结算结果；关闭、刷新或重新打开 Mini App 后不恢复已经终结的结果页。
- 数据库终局、资产释放和账本结算在服务端确认胜负时立即完成；当前客户端必须等待本地动作表现队列全部播放完毕后再显示已确定的结果覆盖层。
- 当前会话已经持有未终局 room 时，bootstrap 不再返回 participation 不能单独使页面回到 Battle 首页；客户端必须先读取该 room 的权威快照，并按其中的进行中状态或终局结果回正。
- 结果页按钮固定为“返回 Battle 首页”，点击只执行本地导航，不发送确认 API、不写数据库、不改变结算。
- 从 Battle 入口或 `/game` 重新打开进入 Battle 首页，从普通 TMA 入口重新打开仍进入项目默认首页。
- 服务端永久保留精简对战摘要，包括对手、胜负、入场费、结算金额和时间。
- 服务端永久保留私有审计信息，包括双方锁定阵容、规则版本、每回合行动、命中判定、最终状态和账本流水 ID。
- 服务端审计信息只用于结果恢复和异常核查，不向用户提供历史或回放入口。

## 二十一、首次进入与分层运行时

- Battle 首次进入只同步下载八种页面状态、倒计时、权威恢复、战场操作和核心样式；Ably SDK、完整技能轨迹播放器与轨迹样式不得进入 Battle 核心静态闭包。
- 玩家已经进入 Battle 后，任一非首页权威状态以及触摸、指针或键盘焦点意图立即并行准备 realtime 与重表现运行时；页面切换、业务请求、按钮和倒计时不等待模块。
- Realtime 动态模块可以继续依赖已经执行的应用入口模块，但 Vite 动态 preload 提示不得再次包含或下载应用入口 JS；玩家意图后的入口资源请求总数保持为一。
- Battle 首页数据稳定后的空闲准备只允许在页面可见、浏览器在线、Telegram active、明确非省流量 4G 时发生；未知网络、慢速网络、省流量、离线、隐藏或 Telegram deactivated 不自动下载。
- Realtime token 与 Ably 模块并行取得；连接建立前继续当前活动状态既有的 1—2 秒 REST 回正，模块仍在下载不等于玩家离线。
- 快速点击技能时业务命令与表现模块下载并行；下载中只显示战场行内“战斗准备中”，不得增加页面状态、覆盖倒计时或锁定权威可用操作。
- 模块、样式或动画失败时跳过本次重表现，继续应用权威动作、生命、击倒、行动权和结果；失败 Promise 清除后允许后续重试，不得重提原业务命令。
- Battle 增量核心构建硬门禁固定为 JS 原始 `160000` 字节、gzip `45000` 字节，CSS 原始 `45000` 字节、gzip `9000` 字节；静态闭包中的 Ably、重特效播放器和轨迹样式必须为零，动态 preload 映射中的应用入口 JS 也必须为零。
