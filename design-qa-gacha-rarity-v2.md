# 开盒稀有度收藏级近景代表图设计验收

## Findings

- 无可操作的 P0、P1 或 P2 视觉问题。
- [P3] 真实 iPhone 镜像截图为包含 Telegram 与设备外框的 `330 × 733px` 画面，Web Inspector 暴露的稳定内容高度为 `896 CSS px`，但镜像工具没有提供页面截图的精确 `deviceScaleFactor`；因此本次只对用户点名的稀有度代表区进行归一化视觉比较，不虚构逐像素 CSS 换算。

## 验收对象

- 版本：`fa14da68`，Vercel Git Integration 状态为成功，稳定入口 `https://final-tma-pi.vercel.app` 已返回本次构建的五个哈希化 WebP。
- 页面：开盒页，真实 Telegram iPhone 镜像，浅色主题。
- 状态：普通、稀有、传说三档均已实际切换；未点击开盒、VIP 领取、充值或任何支付入口。
- 范围：五张前端静态稀有度代表图、对应展示容器样式与同步文档；概率、价格、保底、开盒逻辑、API、数据库和正式宠物目录未修改。

## 视觉依据与实现证据

- 选定设计方向：`/Users/mac/.codex/visualizations/2026/08/12/019ff711-49ab-7941-a965-74edc7eb2dc2/gacha-rarity-v2-source-option-1.png`，`1810 × 869px`；这是五档收藏级近景头像、椭圆徽章和逐级价值光效的视觉真值。
- 用户原始问题截图：`/var/folders/__/ffcc9r1113l4c8cd1z4m8tp80000gn/T/codex-clipboard-4e7c0960-4780-4700-b36b-f941109b522e.png`，用于确认旧图在真实页面中的尺寸与辨识问题。
- 最终稀有盲盒实机：`/Users/mac/.codex/visualizations/2026/08/12/019ff711-49ab-7941-a965-74edc7eb2dc2/gacha-rarity-v2-fresh-rare-iphone.png`，`330 × 733px`。
- 最终普通盲盒实机：`/Users/mac/.codex/visualizations/2026/08/12/019ff711-49ab-7941-a965-74edc7eb2dc2/gacha-rarity-v2-fresh-normal-iphone.png`，`330 × 733px`。
- 最终传说盲盒实机：`/Users/mac/.codex/visualizations/2026/08/12/019ff711-49ab-7941-a965-74edc7eb2dc2/gacha-rarity-v2-final-legendary-iphone.png`，`330 × 733px`。
- 五张项目成品均为 `304 × 304px` 透明 WebP；四角 Alpha 为 `0`，绿幕或洋红幕可见边缘残留为 `0`。Vite 生产构建输出与稳定入口返回的文件大小一致：普通 `72,924`、稀有 `99,138`、史诗 `96,656`、传说 `34,334`、神话 `40,748` bytes。

## 归一化与比较方法

- 完整视觉方向图和真实页面不是同一画布：前者是自然尺寸组件设计板，后者是 Telegram 内的最终 `44 × 44 CSS px` 展示位。本次比较先核对五档身份、构图、徽章和价值梯度，再以三档真实页面截图检查缩小后的辨识度、栅格间距和相邻区域稳定性。
- 全页比较覆盖主盲盒、三档选择、代表图、概率、保底、按钮和底部导航，确认局部改动没有改变页面总体层级。
- 聚焦比较使用真实页面中的代表图区；这里的龙脸、角、翼和徽章已经清晰可读，因此无需再创建放大裁切作为额外证据。

## 必检保真面

- 字体与文案：未修改字体、字号、行高、字重或概率文案；普通、稀有、史诗、传说、神话名称和既有公示概率保持原样，真实截图中没有新增换行或截断。
- 间距与布局：保留原 `54px` 代表项高度、普通三项 `84%` 居中和稀有／传说四项满宽布局；移除旧白色圆框的 `2px` 内边距、`1px` 边框和裁切后，徽章直接占满既有 `44px` 展示位，没有侵入概率、保底或开盒按钮。
- 色彩与视觉令牌：继续使用页面奶油白、暖金和橙色体系；五档依次以象牙、银绿、紫金、红金、黑金形成唯一价值梯度，未新增与页面冲突的大面积背景色。
- 图片质量：五只龙均为近景头胸构图，并保留明确角或翼特征；透明边缘在真实白色页面上没有绿边、洋红边、白盒、拉伸或裁切。普通更亲和，稀有和史诗色彩逐步增强，传说与神话的红金／黑金轮廓在小尺寸下仍可区分。
- 内容与业务语义：代表图仍只表示稀有度，不表示具体宠物、候选集或单品概率；无点击语义与原 `aria-label` 结构保持不变。
- 图标与交互：本次没有新增或替换应用图标；三档切换真实可用，选中态保持原橙色描边和圆点。验收没有触发开盒、领取或支付。
- 响应与可访问性：真实 iPhone 稳定视口下代表项未重叠或溢出，概率文字仍清晰；图片 `alt` 已更新为对应“收藏徽章”，列表项的稀有度与总概率标签保持完整。

## 比较历史

1. 初始实现的额外白色圆框、内边距和 `overflow: hidden` 会把自带徽章的近景头像再次缩小并裁掉稀有度外框，属于 P2 视觉层级冲突。
2. 已在 `gacha-page.css` 中移除该二次容器的边框、内边距、背景、阴影和裁切，同时不改变代表项栅格或页面高度。
3. 修正后的普通、稀有、传说真实页面显示五档徽章完整、龙脸清楚、概率与相邻区域无位移，前述 P2 已关闭。

## Implementation Checklist

- [x] 五档身份和从普通到神话的唯一顺序保持不变。
- [x] 五张独立透明 WebP 已落入项目并由前端静态映射引用。
- [x] 展示位移除二次白框和裁切，但页面总体几何不变。
- [x] 产品说明、运行架构、ADR-024 和开发记录同步更新。
- [x] Web typecheck、前端构建、架构门禁、资源门禁、清单门禁与完整开发构建通过。
- [x] 真实 Telegram iPhone 完成三档切换视觉验收，全程无消费与支付。

## Follow-up Polish

- 无必须跟进的 P3 美化项。完整 `validate:static` 的数据库差异步骤因本机 Docker daemon 未运行而未执行；这是数据库验证环境缺口，不影响本次无数据库变更的视觉结论。

final result: passed
