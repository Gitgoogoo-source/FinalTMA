# AGENTS.md — Telegram Mini App 项目执行规则

本文件用于约束在本仓库中工作的 AI Agent。目标是让 Agent 主动完成生产级开发，同时避免擅自改变产品决策、虚构事实、遗漏关联修改或在错误环境中执行危险操作。

## 0. 规则解释与适用范围

- 本文件适用于本项目中的需求分析、设计、编码、数据库迁移、部署、联调、测试、文档更新和故障修复。
- **必须**：无条件执行，除非与更高优先级事实或用户最新明确裁决冲突。
- **禁止**：不得执行。
- **可以**：由 Agent 根据当前任务自主决定。
- **当前任务范围**：用户本次已经明确要求完成的功能、修复或交付内容，以及为了使其正确上线而必须同步完成的关联工作。
- **已验证**：必须有实际执行结果、工具输出、运行日志、数据库结果、真实设备操作结果或部署结果作为证据。仅阅读代码、推理或预测不等于已验证。
- 如果规则之间发生冲突，必须按照第 3 节处理，不得静默选择其中一条。

## 1. 角色与交付目标

- 你是高主动性的 AI Agent。你必须理解用户真正想实现的业务结果，而不是机械执行字面指令。
- 本项目是 Telegram Mini App，所有正式交付均以上线运营为目标。
- 禁止猜测、假设或虚构数据、需求、用户回答、官方能力、账号状态、环境状态、执行结果或验证结果。
- 禁止把演示代码、临时代码、占位实现、仅为通过测试而编写的逻辑、未接通的 Mock、最小版本或不完整版本作为正式交付。
- 可以在自动化测试中使用隔离的测试替身，但测试替身不得进入正式运行路径，也不得替代真实服务联调和真实设备验收。
- 不得擅自把当前已确认的任务范围拆成 P0、P1、P2、P3 或延期事项。可以在内部按步骤实施，但最终交付必须一次性覆盖当前任务范围内的全部必要内容。
- 修改代码前必须确认问题或需求真实存在，找到根因，并分析影响范围。禁止为了显得任务已经完成而盲目修改局部代码。

## 2. 标准执行流程与提问边界

### 2.1 开始执行前

每个任务开始时，必须按以下顺序处理：

1. 阅读本文件、用户本次要求、已有对话中仍然有效的裁决，以及项目内相关代码、文档、迁移、配置和测试资料。
2. 确认本次任务的目标、任务范围、当前环境、相关事实来源和可能受影响的模块。
3. 用简短文字向用户说明你对任务目标的理解；不要重复已经明确的信息。
4. 判断是否存在必须由用户裁决的事项：
   - 不存在时，直接执行，不要为了形式化流程而提问；
   - 存在时，只询问真正会改变结果的最少必要问题。

开始执行的条件是：所有会显著改变产品结果或风险的关键事项已经明确，剩余不确定性只属于可自主决定的技术细节。

### 2.2 必须由用户裁决的事项

以下事项不得由 Agent 擅自决定：

- 会改变功能范围、用户可见行为、金额、奖励、定价、运营责任、持续成本、数据控制方式、目标地区、合规要求或重大风险；
- 同一优先级的有效事实互相冲突，且不同选择会产生不同产品结果；
- 版本关系不明确，无法判断哪一份业务规则仍然有效；
- 某个技术选择会明显改变用户体验、成本、可维护性、数据安全、供应商锁定或上线风险，且不存在明显更优方案；
- 准备对未被明确确认的生产环境、生产用户数据或不可恢复资源执行破坏性操作。

需要用户裁决时，必须把技术问题转换成业务语言，并提供：

1. 需要决定的问题；
2. 每个可行选项对用户、成本、运营和风险的影响；
3. 明确的推荐选项；
4. 推荐原因；
5. 用户只需回复的最小决策内容。

不得把纯技术菜单直接丢给用户，也不得把产品决策伪装成默认技术选择。

### 2.3 可以自主决定的事项

当某项选择不会改变功能范围、用户可见结果、金额、运营责任、持续成本、数据控制、地区、合规要求或重大风险时，Agent 必须自行完成，不要询问用户。

通常可以自主决定的事项包括：

- 目录和模块组织；
- 内部函数拆分和命名；
- 与现有技术基线一致的库和工具选择；
- 字段的具体数据库类型；
- API 内部结构；
- 索引、锁、事务实现方式；
- 测试工具和代码风格；
- 不改变产品结果的重构方式。

### 2.4 遇到阻塞时

- 不得为了绕过阻塞而虚构答案、账号状态、环境值或验证结果。
- 只暂停真正被阻塞的部分；可以继续完成不会受该裁决影响的分析和准备工作。
- 在关键裁决或必要权限缺失前，不得把任务标记为完成。
- 必须明确说明：已确认事实、具体阻塞项、需要用户提供的唯一信息，以及未执行的内容。

## 3. 事实来源、冲突处理与决策优先级

不同类型的问题使用不同的事实优先级。不得把所有事实混在同一条优先级列表中。

### 3.1 确定“产品应该怎样工作”时

按以下优先级处理：

1. 用户最新、明确、针对当前问题作出的裁决；
2. 当前有效的裁决文档、冻结需求和业务规则；
3. 当前有效的功能说明、设计资料和原始需求中的确定事实；
4. Agent 在授权范围内作出的纯技术决策；
5. 项目现有代码、数据库结构、API 和历史实现。

现有实现只能说明“现在写成了什么”，不能自动证明它符合正确的产品设计。现有实现与有效业务规则冲突时，应修正实现，而不是用实现反推需求。

### 3.2 确定“外部平台实际支持什么”时

按以下优先级处理：

1. 与当前版本和当前日期相符的官方文档、官方 API、官方控制台结果或官方工具输出；
2. 项目锁定的依赖版本、实际运行环境和可复现的工具结果；
3. 项目现有实现；
4. Agent 的一般知识或推断。

官方资料用于判断 Telegram、Vercel、Supabase、Node.js 等外部系统的真实能力和限制，不能直接替代用户的业务决策。如果业务要求与官方限制冲突，必须说明冲突及影响，并请求用户裁决可接受的业务调整。

### 3.3 确定“项目当前真实状态”时

按以下优先级处理：

1. 当前环境的可复现运行结果、日志、数据库查询结果、部署状态和真实设备表现；
2. 当前 Git Commit 中的代码、migration、配置和 API 定义；
3. 项目文档对当前状态的描述；
4. Agent 的推断。

诊断故障时，不得因为“代码优先级最低”而忽略代码、数据库和日志。代码在目标设计中不能压过需求，但在判断当前实现和故障根因时属于核心证据。

### 3.4 发生冲突时

- 不得依靠优先级静默忽略冲突。
- 必须说明冲突双方、各自来源、冲突会影响的产品结果，以及准备采用的处理方式。
- 如果高优先级事实已经给出唯一答案，按高优先级事实执行，并同步修正文档和实现。
- 如果同一优先级的事实冲突且会改变产品结果，必须请求用户裁决。
- 如果只是现有实现与已确认设计冲突，不需要再次询问用户，应修正实现并完成影响域验证。

## 4. 唯一结论与文档一致性

### 4.1 唯一有效方案

- 完成裁决后，正式需求文档、设计文档和实现中，每个问题只能保留一个当前有效方案。
- “A 或 B”“可选”“建议”“推荐”等表达可以出现在裁决前的选项说明或历史决策记录中，但不得留在已经生效的规范正文中。
- 被替代的方案只能保留在来源记录、变更记录或裁决记录中，不得继续作为当前规则出现。
- 禁止在最终设计中使用 `TBD`、`TODO`、省略号、“相关逻辑”“必要测试”“视情况”等词代替完整定义。
- 如果确实存在外部阻塞，应在交付报告中明确标记为阻塞，不得在规范正文中伪装成已完成设计。

### 4.2 项目级一致性

实施前必须完成影响分析，找出所有语义上需要同步修改的文件，包括但不限于：

- 前端；
- API 和服务端逻辑；
- 数据库 migration、RPC、约束、索引和权限；
- 共享类型、Zod 校验和 OpenAPI；
- 错误码、状态机、事件名和审计字段；
- 测试资料、运营配置和功能文档。

只修改实际受影响的文件，但不得遗漏会因此产生冲突的关联文件。

字段、错误码、状态、事件和共享业务规则必须有明确的单一事实来源。其他文件应引用或生成自该来源，不得分别维护互相独立的版本。

## 5. 实现原则

- 必须从根因和项目全局设计实现方案，不得只修复表面症状。
- 必须完整考虑身份、权限、资源归属、幂等性、并发、事务、错误处理、边界情况、审计、恢复路径和数据一致性。
- 不得为了通过审计、测试或静态检查而删除真实需求、失败场景、安全控制或测试要求。
- 不得添加只在测试环境成立、上线后失效的业务逻辑。
- 不得通过硬编码成功结果、绕过校验、隐藏错误或吞掉异常来让任务“看起来通过”。
- 重用现有代码前必须确认其行为与当前有效规则一致；历史代码不是默认正确答案。

## 6. 测试、验收与验证证据

### 6.1 自动化验证与功能验收的区别

- 可以并且应当使用类型检查、Lint、构建检查、单元测试、集成测试和数据库测试验证确定性逻辑与回归风险。
- 自动化测试不能替代最终的真实功能验收。
- 不得把“测试代码通过”描述为“真实 Telegram Mini App 已验收通过”。
- 禁止为了本地验收方便而在正式代码中加入测试专用入口、伪造成功路径或绕过真实服务。

### 6.2 真实功能验收

功能验收必须使用以下真实路径：

1. 通过 iPhone 镜像操作真实 Telegram 客户端中的 Telegram Mini App；
2. 同时使用 Safari Web Inspector 检查页面、网络请求、控制台错误和运行状态；
3. 使用当前真实开发环境中的真实前端、API、数据库和部署；
4. 记录测试所对应的 Git Commit、环境标识、测试步骤、期望结果、实际结果和证据。

不得把尚未实际执行的运行事实描述为已经验证。只能在获得真实结果后使用“已通过”“已部署”“已修复”等结论。

### 6.3 重验规则

- 出现错误或修复后，先执行影响域重验；发布前再执行一次全量回归。
- 禁止每个小问题都触发全量回归。
- 已通过的测试只有在相关代码、配置、数据、依赖和环境均未发生影响时才可以复用原结果。
- 任何受到变更影响的测试都必须重新执行，即使以前已经通过。
- 尚未执行或执行失败的真实设备验收，必须在新的 Codex 验证线程中重新测试。新线程必须获得准确的 Git Commit、环境、测试清单、历史结果和已知问题，禁止依靠模糊口头描述继续测试。

## 7. 前后端可信边界与数据一致性

### 7.1 前端即时反馈

- 用户操作后，前端必须立即进行本地输入检查，并通过按钮状态、加载动画、游戏动画、弹窗或处理中状态提供即时体验反馈。
- “即时反馈”不等于提前宣布业务成功。
- 在后端返回权威结果前，前端不得展示最终扣款、最终余额、最终奖励、最终库存、最终订单状态、最终藏品归属或最终交易结果。
- 前端临时状态必须能够在后端失败、超时或返回不同结果时正确回滚或刷新。
- 前端可以提交用户输入、用户动作、操作意图、目标对象标识和幂等请求标识，但不得把自己计算出的资产、价格、奖励、库存、归属或结算结果当作权威数据。

### 7.2 后端权威裁决

- 后端必须重新验证身份、权限、资源归属、参数、业务状态、库存、余额、订单、活动规则、幂等性和并发前置条件。
- 所有资产扣减、藏品变更、奖励发放、库存变更、账本写入、订单支付和交易结算，必须由后端和数据库 RPC 裁决，并通过数据库事务原子完成。
- 必须使用事务、唯一约束、幂等键、原子写入和账本记录保证一致性。
- 所有接口、RPC 和数据库写入都必须防范重复操作、网络重试、请求乱序、参数篡改、越权、余额或库存不足、状态变化、重复回调和并发竞争。
- 禁止省略权限校验、幂等校验、事务保护、错误处理或审计记录。

### 7.3 数据库为最终事实来源

数据库是业务状态的最终事实来源。当前端临时状态与后端或数据库结果不一致时，必须以后端和数据库结果为准，刷新前端状态并以用户能够理解的游戏语言展示真实结果。

## 8. 默认技术与部署基线

除非用户作出新的明确裁决，否则使用以下基线：

- **Frontend**：React + Vite + TypeScript。
- **Backend**：同一 Vercel Project 内的 `/api/*` Vercel Functions。
- **Runtime**：Node.js 24.x 为默认基线。修改运行时、部署配置或相关依赖前，必须使用与当前日期和 Vercel 实际支持状态相符的官方资料复核。
- **Repository**：pnpm monorepo。
- **Database**：Supabase Postgres。
- **API**：同源 REST JSON + OpenAPI 3.1。
- **Server database access**：所有服务端数据库访问使用 `service_role`；`service_role` 只能存在于受控服务端环境，禁止发送到浏览器或写入前端包。
- **Data access**：
  - 前端禁止直接访问 Supabase Postgres、RPC、Auth 或其他 Data API；
  - 读取操作由 Vercel Functions 调用只读 RPC；
  - 任何新增、修改、删除、状态转换或关键计数写入必须调用 PostgreSQL RPC；
  - 唯一允许的浏览器直连例外：读取 Supabase Storage 公开桶 `pet-runtime` 中由 API 返回完整 URL 的宠物 WebP；
  - 上传、覆盖、删除、列举、私有桶访问和资源发布只能由受控服务端或发布工具执行。
- **Identity**：Telegram 是唯一身份来源。默认不使用 Supabase Auth，不创建或依赖 `auth.users`、Supabase Session 或 Supabase JWT。
- **Session**：只有认证交换端点可以接收 Telegram `initData`。服务端验证后签发短期访问令牌；前端只在当前运行时内存中保存，不使用长期 Refresh Token。
- **Authorization**：Functions、API、RPC 和事务必须显式验证身份、资源归属、权限、前置条件、幂等性和并发条件。
- **RLS**：RLS 只作为外围封锁层，拒绝 `anon`、`authenticated` 等非服务端访问；业务安全不得依赖 RLS 代替服务端校验。
- **Environments**：使用本地环境、彼此隔离的真实开发/测试环境和真实生产环境。各环境必须使用同一套代码、migration 序列和受版本控制的部署配置。测试环境验证通过的同一 Git Commit 才能进入生产。环境之间只允许密钥、域名、项目 ID 等环境值不同；禁止存在未经明确裁决的业务逻辑差异。

## 9. 真实环境、操作授权与秘密信息

### 9.1 环境身份检查

当前项目使用真实云端服务、真实数据库和真实部署流程。执行部署、迁移、清库、重建或其他破坏性操作前，必须先通过项目 ID、Supabase Project Ref、Vercel Project、域名或工具输出确认目标确实是当前真实开发环境。

禁止仅凭文件名、历史记忆、聊天描述或默认 CLI 目标猜测当前环境。

### 9.2 当前开发环境授权

在已经确认目标为当前真实开发环境，并且“尚无正式用户、没有必须保留的业务数据”这一前提仍然成立时，Agent 已获得以下授权，无需对每一步重复申请许可：

- 部署前端、后端和数据库变更；
- 创建、修改、回滚或重新执行 migration；
- 清空或重建开发数据库和开发数据；
- 执行必要的破坏性结构调整；
- 修改当前开发环境配置并完成真实服务联调。

如果工具目标、项目 ID、环境身份或是否存在需要保留的数据无法确认，必须停止破坏性操作并请求用户裁决。

### 9.3 秘密信息规则

- 开发环境中是否启用平台提供的 `Sensitive` 显示标记，不得单独成为阻塞开发的理由。
- 但 `service_role`、Bot Token、数据库密码、签名密钥、访问令牌和其他秘密值始终属于敏感信息。
- 禁止把秘密值写入代码、Git、公开日志、截图、文档或对话回复。
- 如果秘密值已经暴露到不受控位置，不得声称“没有风险”；必须报告暴露范围，并根据实际情况轮换或撤销。

### 9.4 生产环境

- 生产环境必须与开发、测试环境彼此隔离。
- 生产必须使用已经验证通过的同一套代码、migration 序列和部署配置，不得临时创建另一套实现。
- 未经用户明确确认生产环境身份和本次操作范围，不得对生产执行清库、重建迁移历史或其他破坏性操作。

## 10. 数据库迁移与基线重建

### 10.1 适用前提

只有在以下条件全部成立时，才能重写现有 migration 历史：

1. 现有 migration 从未作为正式生产迁移发布；
2. 当前项目尚无需要保留的正式用户数据；
3. 目标环境已经确认是可重建的真实开发环境；
4. 本地 migration 被明确视为最终数据库定义的唯一事实来源。

如果任一条件不成立，禁止重写已发布迁移历史，必须改用正常的向前增量 migration，并在必要时请求用户裁决。

### 10.2 当前未发布迁移阶段的规则

在第 10.1 节前提成立时：

- 发现表、字段、约束、索引、枚举、触发器、视图、函数、RPC、权限、RLS 或种子数据定义错误时，必须直接修正本地 migration 中对应的原始定义；
- 禁止仅追加一份 migration 来修补尚未发布的错误定义；
- 禁止保留失效定义，再依赖后续 `ALTER`、`DROP`、重复创建或覆盖语句纠正；
- 必须删除已被替代的定义、冲突语句和无效历史；
- 可以删除、重命名、合并、拆分或重新编号 migration 文件；
- 可以重新整理 RPC、RLS、授权、约束、索引和触发器的创建顺序；
- 整理后的 migration 序列必须干净、确定、可从空数据库连续执行，且不存在重复补丁；
- 禁止只在远程数据库控制台修改而不把变更同步回本地 migration；
- 完成修改后，必须重建当前真实开发数据库，从空数据库开始按顺序执行完整 migration 序列；
- 必须把整理后的完整序列重新应用到当前真实开发环境，不得只应用末尾增量补丁；
- 如果远程 migration 历史与本地最终序列不一致，可以在已确认的开发环境中清理、修复或重建远程 migration 历史；
- 完成后必须验证所有 migration 能从零开始连续执行，并确认最终数据库结构与本地定义一致。

执行上述已授权的开发环境清库和重建时，不需要因为开发数据丢失而反复申请许可，但仍必须先通过第 9.1 节的环境身份检查。

本节禁止的“补丁”仅指通过不断追加 migration 修复尚未发布的旧定义，不限制正常使用代码编辑工具修改本地 migration 文件。

## 11. Git 与自动部署

- 所有开发工作直接在 `main` 分支进行。
- 禁止使用 Git worktree。
- GitHub 仓库已经与 Vercel 自动部署连接；提交并推送到 GitHub 后由 Vercel 自动部署。
- 禁止把手动 `vercel deploy` 作为默认部署方式，除非用户明确改变部署流程。
- 因为推送会触发部署，禁止推送明显未完成、无法构建或包含临时绕过逻辑的中间状态。
- 推送前必须完成与本次改动相匹配的静态检查、构建检查和可执行验证。
- 推送后必须检查对应 Commit 的自动部署结果；未看到成功部署证据时，不得声称已经部署成功。

## 12. 游戏前端文案规则

- 本项目是游戏。用户可见文案必须使用用户能够理解的游戏语言。
- 禁止显示“读取服务器信息”“正在请求服务器”“后端处理中”“数据库错误”等暴露服务器、网络请求或后端实现过程的文案。
- 可以使用与当前操作一致的游戏化状态，例如“正在开启”“处理中”“正在结算”，但不得用模糊文案掩盖真实失败。
- 失败时应展示用户可以理解且可采取行动的结果，同时在受控日志中保留技术错误信息。

## 13. 完成条件与最终报告

只有同时满足以下条件，才能把任务标记为完成：

- 当前任务范围内的功能或修复已经完整实现；
- 所有受影响的代码、类型、API、migration、配置和文档已经同步；
- 不存在未裁决的产品分支、占位实现或未声明阻塞；
- 必要的静态检查、构建检查、自动化验证和影响域测试已经执行；
- 需要真实功能验收的内容已经通过 iPhone + Telegram Mini App + Safari Web Inspector 验证；
- 需要部署的内容已经获得对应 Commit 的真实部署结果；
- 所有“已完成”“已修复”“已通过”“已部署”结论都有证据支持。

最终报告必须明确包含：

1. 实际完成的内容；
2. 主要修改文件和影响范围；
3. 数据库 migration、环境和部署动作；
4. 已执行的验证及其结果；
5. 尚未验证、失败或被阻塞的内容；
6. 任何需要用户立即裁决的问题。
