# AI 项目开发规则

你是一个高主动性的 AI 智能体。你的职责是理解我真正想达成的目标，我有很多内容没有表达出来，你要挖掘我真正的需求，主动检查可用上下文，使用合适的工具，并产出有用、完整且可上线运营的交付物。

## 一、项目性质与交付目标

- 本项目是 Telegram Mini App。
- 不得猜测、假设或虚构任何数据、原始需求、用户回答、官方能力、账号状态或验证结果。
- 所有工作均以上线运营为目标。不得交付演示代码、临时代码、Mock 代码、仅为通过测试而编写的代码、最小版本或不完整版本。
- 项目不采用分阶段开发方式，也不采用 P0、P1、P2、P3 等分期方案。每次开发都必须一次性完成上线运营所需的完整内容，不得将部分工作留到后续阶段。
- 不得为了“完成任务”而盲目修改代码。只有确认问题真实存在，找出问题的根因后，才能进行修改。
- 必须从项目全局规划修复或实现方案，不得只处理局部表象。

## 二、任务理解、提问与裁决边界

### 2.1 执行前的沟通要求

- 执行任务前，必须先向我提问，并根据我的回答继续追问，直到你掌握至少 95% 的必要信息，完全理解我的真实需求和目标后，才能开始执行任务。
- 只提出与项目开发有关的问题，不要提出运营、平台法律等与开发无关的问题。
- 开始执行前，先描述你对本次任务的理解。
- 必须理解任务的真实目标，而不是只按字面意思执行。

### 2.2 必须由我裁决的事项

以下事项必须向我提问，不得自行决定：

- 会改变功能范围、用户可见结果、金额、运营责任、持续成本、数据控制、地区、合规要求或重大风险的问题；
- 同一级别的事实发生冲突、版本关系不明，或采用不同方案会改变产品结果的问题；
- 你无法明确判断最优方案的技术问题。

当技术问题需要我裁决时，必须将其转化为我能够理解的业务问题，说明不同方案对业务和用户结果的影响，提供明确选项，并标记推荐方案及推荐原因。不得把纯技术菜单直接交给我，也不得把产品问题伪装成技术默认值。

### 2.3 可以自主决定的事项

如果某项选择不会改变功能范围、用户可见结果、金额、运营责任、持续成本、数据控制、地区、合规要求或重大风险，你可以自行完成纯技术裁决，无需向我提问。

此类事项包括框架、库、目录结构、字段类型、API 风格、索引、锁、测试工具和代码约定等。每项自主技术决策必须形成唯一的 `ADR-*` 记录。

## 三、事实来源、优先级与唯一结论

### 3.1 固定优先级

发生冲突时，按以下顺序处理，但不得依靠优先级静默忽略冲突：

1. 用户在澄清过程中最新、明确的裁决；
2. 官方文档；
3. 当前有效的裁决文档，以及已经冻结的需求和业务规则；
4. 当前有效的功能说明、设计资料和原始功能说明中的确定事实；
6. 你的自主决策；
7. 项目现有代码、数据库结构、API、任务规划和其他现有实现。

现有实现的优先级最低。它只能说明“当前项目已经写了什么”，不能证明现有方案正确，也不能限制最终方案。不得用现有代码倒推功能设计；如果现有实现与有效文档冲突，必须以设计并实现正确的功能方案为目标，而不是迁就现有实现。

### 3.2 唯一结论要求

- 文档正文必须给出唯一、确定的最终方案，不得保留“A 或 B”“可选”“建议”“推荐”等并列或未裁定方案，禁止同时存在多个方案。
- 需要用户裁决的内容必须先在澄清阶段完成裁决，再写入正文。
- 正文只保留当前唯一结论；被替代的内容只能保留在来源记录或裁决记录中。
- 不得使用 `TBD`、`TODO`、省略号、“相关逻辑”“必要测试”“视情况”等表述代替完整设计。

### 3.3 文档与代码的一致性

- 项目代码必须依据有效文档编写，禁止编写没有文档依据的项目代码。
- 实施前必须主动查找并阅读项目现有文件和业务文档，确认真实约束，不得只依赖对话内容。
- 执行前必须分析本次任务需要同步修改的全部文件，并同步更新全项目相关文档，避免只修改局部而产生文档冲突。
- 不得在多个文档中分别维护互相独立的字段、错误码或状态版本。

## 四、实现与验证原则

- 设计和实现必须完整考虑权限、幂等性、错误处理、边界情况与数据一致性。
- 不得为了通过审计而删除需求、失败场景、安全控制或测试要求。
- 不得把只能在实现阶段验证的运行事实描述为已经验证。必须记录明确的复核任务，以及验证失败时既定的返回路径。
- 不得编写本地功能验收测试的 test 代码。所有功能验收测试均须通过 iPhone 镜像操作真实的 Telegram Mini App，并同时使用 Safari Web Inspector 检查页面运行情况，而不是通过编写 test 代码进行验收测试。
- 开发过程中出现错误或失败时，采用“影响域重验 + 发布前一次全量回归”：先对受影响范围进行选择性重验，发布前再执行一次全量回归。禁止每次小问题都触发全量重验。
- 已经通过的测试无需重复执行；尚未执行或执行失败的测试，必须在新的 Codex 线程中重新测试。

## 五、前后端可信边界与数据一致性

### 5.1 前端先行

- 用户操作后，前端必须立即通过本地预检查、按钮状态、动画、弹窗或临时结果提供体验反馈；不得等待 后端数据 返回后才响应用户。
- 前端反馈只代表临时体验状态，不代表业务成功。
- 前端不可信，只能提交用户动作、操作意图、目标对象标识和幂等请求标识。
- 前端不得提交、计算或决定资产、价格、奖励、库存、订单、支付、藏品归属、交易等最终业务数据或结果。

### 5.2 后端裁决

- 后端必须重新校验身份、权限、参数、业务状态、库存、余额、订单、活动规则和幂等性。
- 所有资产扣减、藏品变更、奖励发放、库存变更、账本写入、订单支付和交易结算，必须由后端或数据库 RPC 裁决，并通过数据库事务原子完成。
- 必须使用事务、原子写入、唯一约束、幂等键和账本记录保证一致性。
- 所有接口、RPC 和数据库写入都必须防范重复操作、网络重试、请求乱序、参数篡改、越权、余额或库存不足、状态变化、重复回调和并发竞争。
- 不得省略权限校验、幂等校验、事务保护、错误处理或审计记录。

### 5.3 数据库为最终事实来源

数据库是最终事实来源。当前端临时状态与后端或数据库结果不一致时，必须以后端和数据库结果为准，刷新前端状态并展示真实结果。

## 六、默认技术与部署基线

- **Frontend**：React + Vite + TypeScript。
- **Backend**：同一 Vercel Project 内的 `/api/*` Vercel Functions；Node.js 24.x 是默认运行时基线，但使用前必须通过官方资料复核。
- **Repository**：pnpm monorepo。
- **Database**：Supabase Postgres；所有服务端数据库访问使用 `service_role`。
- **API**：同源 REST JSON + OpenAPI 3.1。
- **Data access**：前端不得直接访问 Supabase Postgres、RPC、Auth 或其他 Data API。唯一例外是浏览器可以直接读取 Supabase Storage 公开桶 `pet-runtime` 中由 API 返回完整 URL 的宠物 WebP；上传、覆盖、删除、列举、私有桶访问和资源发布只允许受控服务端或发布工具执行。其他读取操作由 Functions 调用只读 RPC；任何新增、修改、删除、状态转换或关键计数写入都必须调用 PostgreSQL RPC。
- **Identity**：Telegram 是唯一身份来源。默认不使用 Supabase Auth，不创建或依赖 `auth.users`、Supabase Session 或 Supabase JWT。
- **Session**：只有认证交换端点可以接收 Telegram `initData`。服务端验证后签发短期访问令牌；前端只在运行时内存中保存令牌，不使用长期 Refresh Token。
- **Authorization**：Functions、API、RPC 和事务必须显式验证身份、资源归属、权限、前置条件、幂等性和并发条件。RLS 只作为外围封锁层，拒绝 `anon`、`authenticated` 等非服务端访问；业务安全不得依赖 RLS。
- **Environments**：本项目固定使用本地环境和一套真实云环境。用户已于 2026-08-24 明确指定复用现有 Vercel `final-tma` 与 Supabase `final-tma-real-test`（ref `ebewtjerusxcioegpzjd`）作为正式生产资源，不再新建第二套生产环境。上线前验收和上线后生产必须使用同一 Git commit、migration 序列、部署配置、项目 ID、域名与 Storage 对象；切换时只更新生产 Bot、环境身份和机密配置。

## 七、既有云环境的生产切换与操作授权

当前连接的是即将转为正式生产的真实云环境，不是本地模拟环境、Mock 环境或临时环境。目标固定为 Vercel `final-tma` 和 Supabase `final-tma-real-test`（ref `ebewtjerusxcioegpzjd`）。生产 Bot 固定为 `@EvoMyPet_bot`，named Mini App short name 固定为 `evomypet`，稳定域名固定为 `https://final-tma-pi.vercel.app/`。

正式开放 Telegram 入口前仍无须保留现有业务数据。用户已明确授权在最终切换窗口清空数据库和 migration history，并从仓库当前完整 migration 序列重建一次。为完成本次生产切换，你有权：

- 直接部署前端、后端和数据库变更；
- 创建、修改、回滚或重新执行数据库 migration；
- 清空或重建数据库及开发数据；
- 执行必要的破坏性结构调整；
- 修改当前环境配置并完成真实服务联调。

对上述一次性切换操作，不要因为现有数据受影响而停止或重复申请许可；执行前仍须逐项确认目标 project ID、ref、域名和 Git commit。Telegram 生产入口、webhook 与 Cron 在应用、数据库、环境变量和 Bot 配置一致前必须保持关闭。

Vercel Production 的 `APP_ENV` 固定为 `production`。Bot token、webhook secret、数据库 service role、会话与 Battle secret 等真实生产机密不得进入 Git、聊天、日志、截图或浏览器环境；环境变量变更必须由新的 `main` 提交对应 Production deployment 生效。

用户此前关于“未来另行提供隔离生产环境”的规则已被 2026-08-24 的最新明确裁决替代。不得再创建或等待另一套 Vercel、Supabase、Storage 或生产域名。

## 八、数据库迁移与基线重建

截至正式开放 `@EvoMyPet_bot` 入口前，现有 migration 尚未作为正式生产迁移冻结，因此表结构、字段、约束、索引、枚举、触发器、视图、数据库函数、RPC、权限授权、RLS 策略、种子数据或其他数据库定义发生调整时，必须直接修正本地 migration 中对应的原始定义，不得在错误定义后继续追加修复补丁。

具体规则如下：

- 禁止仅新增一份 migration，用来修补本地已有 migration 中的错误定义。
- 禁止保留已经失效的定义，再依赖后续 `ALTER`、`DROP`、重复创建或覆盖语句进行纠正。
- 必须删除已被替代的数据库定义、冲突语句和无效历史。
- 可以删除、重命名、合并、拆分或重新编号现有 migration 文件。
- 可以重新整理 RPC、RLS、授权、约束、索引和触发器的创建顺序。
- 整理后的本地 migration 序列必须是一条干净、确定且没有重复补丁的最终迁移序列。
- 本地 migration 文件是数据库结构的唯一事实来源。禁止只在远程数据库控制台修改而不将变更同步回本地文件。
- 完成修改后，必须在最终生产切换窗口清空或重建目标数据库，并从空数据库开始按顺序重新执行完整 migration 序列。
- 必须将整理后的完整 migration 序列重新推送到目标 Supabase，不得只推送增量修复补丁。
- 如果远程 migration 历史与重新整理后的本地序列不一致，可以在入口开放前清理、修复或重建 migration history。
- 重建完成后，必须确认所有 migration 能从零开始连续执行，且最终数据库结构与本地定义完全一致。

在生产入口首次开放前执行已批准的最终清库、重建迁移历史或重新推送时，不需要因为数据丢失或破坏性变更而反复申请许可。

本规则禁止的“补丁”，仅指为修复尚未上线的旧数据库定义而不断追加增量 migration；不限制正常编辑本地文件所使用的代码修改工具。

当 `@EvoMyPet_bot` 的 Main Mini App、named Mini App、菜单入口、webhook 和生产 Cron 恢复并开始承载用户后，立即冻结当次 migration 历史；此后绝对禁止清库、重建 migration history、修改既有 migration 或恢复旧 schema，只允许兼容现有数据与已加载客户端的前向应用和追加 migration。

## 九、Git 与自动部署规则

- 所有开发工作都必须直接在 `main` 分支进行，不得使用 Git worktree。
- GitHub 仓库已经与 Vercel 打通。提交到 GitHub 后会自动部署到 Vercel，不需要手动执行 Vercel 部署。

## 十、资源、支付、日志与受保护文件

- 开发和测试过程中，允许消耗游戏内的 Gems、Stars、资源和道具。
- 未经我明确许可，绝对不得支付 Telegram Stars。
- 不得创建视觉验收记录日志。
