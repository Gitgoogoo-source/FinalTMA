# Monster Tamer 藏品展示家园说明

## 1. 文档状态

本文记录 Monster Tamer 当前唯一有效的产品方案。本文替代此前关于探索区域、NPC、道具、遭遇、战斗、捕捉、队伍、背包、经验、任务、奖励、服务端游戏进度和本地存档的全部定义。

Monster Tamer 只是一张展示玩家真实可用藏品的水上家园地图，不形成第二套资产、养成或战斗系统。

## 2. 功能定位与入口

Monster Tamer 位于登录后的 FinalTMA“游戏”页，顺序保持为 `Monster Tamer → 藏品远征 → 幸运转盘`。

玩家点击“进入家园”后，React 通过挂载到 `document.body` 的全屏覆盖层打开游戏。关闭家园后回到原游戏页，远征和幸运转盘的页面状态不变。

家园继续使用当前 FinalTMA 内存会话。会话无效、账号被封禁或入口交接未完成时，不读取藏品、不启动地图，并沿用主应用现有账号门禁。

## 3. 唯一藏品来源

家园只读取同源认证接口 `GET /api/inventory` 返回的当前玩家藏品。前端不直连 Supabase，不向服务端提交藏品归属、数量、图片、稀有度、阶段或战斗力。

进入家园时，React 固定执行以下展示规则：

1. 只保留 `available > 0` 的条目。
2. 以 `template_id` 去重。
3. 同一模板无论拥有多少只，地图上只显示一只。
4. `available = 0` 的出售中、交易中、远征中或 Mint 中模板不显示。
5. 不设置展示数量上限；全部不同的可用模板同时进入地图。
6. 不提供藏品名单、勾选、上阵、隐藏、排序或分区选择。
7. 宠物图片只使用该条目的正式 `image_thumbnail_path`。

静态 Phaser 渲染器不请求 API、不持有访问令牌，也不读取数据库。它只接收同源 React 父页面通过 `postMessage` 注入的最小只读展示数据：`template_id`、名称和正式缩略图路径。

## 4. 50×50 水上家园

唯一地图固定为 `50 × 50` 格，每格 `64 × 64` 世界像素。

地图规则固定为：

- 最外侧两格是连续水域，任何陆地、桥梁、建筑、岩石、宠物或交互对象都不能进入。
- 中央是一块不规则、连续、漂浮在水面上的草地岛屿。
- 岛岸使用草地边缘与崖壁视觉，水面使用 Tiny Swords 的 `#47ABA9` 水色、泡沫和水中岩石。
- 岛上保留城堡、蓝顶房屋、塔楼、树木、树桩、灌木和岩石作为纯展示场景。
- 水域、岛岸崖壁、建筑占地、树干、树桩和陆地岩石全部是不可通行格。
- 除静态障碍外的全部陆地组成一个连续可通行区域。
- 不存在第二张地图、室内、传送点、探索区域、战斗区域、遭遇区、隐藏区域或关卡。

Tiny Swords 地形和建筑继续使用仓库内已固定来源与许可证的白名单资源。地图只额外恢复原开放 RPG 的 AxulArt 默认玩家人物，不引入 NPC、其他单位、动物、敌人、工具、资源采集、FX 或 Enemy Pack。

## 5. 宠物漫游

每个不同的可用 `template_id` 对应一个 Phaser 宠物实体。实体的业务身份始终是 `template_id`，图片和显示名称只来自认证接口结果。

宠物在整块连续岛屿内自由漫游：

- 每次只移动到相邻的可通行格。
- 不进入水域、崖壁、建筑、树木、树桩或岩石占地。
- 不与另一只宠物占用或预定同一格。
- 到达目标格后短暂停留，再继续选择新的相邻目标。
- 移动方向改变时水平翻转正式缩略图。
- 移动时使用轻微上下浮动、压缩伸展和地面阴影形成跑动感。
- 停止时保留轻微待机浮动。
- 不生成或伪造 210 套方向帧、战斗帧或新宠物素材。
- 用户打开藏品详情时，全部宠物移动和场景动画立即暂停；关闭详情后从原位置继续。

地图状态只存在于当前家园覆盖层的页面生命周期内。关闭整个家园后不保存宠物位置、方向或随机漫游状态。

## 6. 玩家移动与地图查看

地图内固定生成一个原开放 RPG 默认玩家人物，使用原四方向三帧行走素材。人物只用于浏览这张藏品家园，不代表藏品、队伍或可持久化游戏角色。

镜头固定为 `1` 倍并平滑跟随人物。地图不允许拖动、滚轮缩放、双指缩放或自动展示全岛，也不提供镜头缩放按钮。

人物移动的唯一输入是手机屏幕点按：

1. 玩家点按可通行地面后，运行时把触点转换为世界格坐标。
2. 运行时在当前连续可通行格上执行四方向寻路，逐格播放对应方向的行走动画。
3. 寻路避开水域、岸边、建筑、树木、树桩、岩石以及宠物当前占用或已经预定的格子。
4. 宠物移动导致下一格被占用时，人物从当前格重新寻路；目标不可到达时停止在最后一个有效格。
5. 不支持桌面键盘、WASD、方向键、鼠标点地移动、虚拟摇杆、方向按钮或 A/B 按钮。

地图不显示宠物名单、宠物数量、重复藏品数量、血条、等级、队伍、任务或战斗 HUD。每只宠物使用不小于其可见图像的透明点击区域；点击宠物优先直接打开详情，不会把该次点击当作人物移动目标。

## 7. 藏品详情覆盖层

React 收到宠物点击结果后，必须重新在本次 `inventory.list` 认证结果中匹配 `template_id`。无法匹配时不打开详情，并显示读取失败状态，不能相信静态渲染器传回的其他字段。

匹配成功后，在游戏上方打开现有藏品详情视觉组件，固定展示：

- “当前藏品”和正式名称。
- 正式详情图。
- 稀有度。
- 进化阶段。
- 战斗力。
- 可用、出售中、交易中、Mint 中和远征中的现有数量摘要。

家园详情不展示进化、分解、出售、Mint 或藏品切换条；详情层是只读展示。关闭详情后回到同一张地图、同一镜头和同一批宠物位置。

详情层通过 React Portal 位于 Phaser `iframe` 和主应用壳层之上。打开期间锁定文档滚动和地图输入，关闭时恢复。

## 8. 加载、空态与错误

| 场景                   | 固定用户结果                                             |
| ---------------------- | -------------------------------------------------------- |
| 藏品读取中             | 全屏家园层显示“正在读取你的藏品”，不注入本地默认宠物     |
| 藏品读取失败           | 显示失败原因、重试和关闭入口，不启动带旧数据的地图       |
| 没有可用藏品           | 显示空岛和“当前没有可展示的藏品”，不生成临时宠物         |
| 正式缩略图加载失败     | 该模板不生成宠物实体，并在家园状态区提示部分图片加载失败 |
| 渲染器未收到父页面数据 | 不启动地图，停留在“请从游戏中心进入”状态                 |
| 宠物点击结果无法匹配   | 不打开详情，提示藏品状态已变化并要求重新进入家园         |
| 详情打开               | Phaser 场景暂停，现有地图状态保留                        |
| 详情关闭               | Phaser 场景恢复，不重新生成地图或宠物位置                |
| 家园关闭               | 销毁渲染器并恢复主应用滚动，不写入任何游戏进度           |

## 9. 数据与可信边界

本功能只读，不新增 API、RPC、数据库表、migration、浏览器持久化、服务端游戏进度、幂等写操作、账本或恢复工作流。

家园不能改变或发放：

- 藏品归属、数量、可用状态、稀有度、阶段或战斗力。
- K-coin、Fgems、盲盒、奖励、任务、邀请、VIP 或钱包状态。
- 远征、市场、进化、分解、Mint、支付、退款或链上资产。

React 是认证业务数据与 Phaser 之间的适配层；Phaser 只负责地图、默认人物、触摸寻路、跟随镜头、宠物渲染、漫游和点击事件。数据库和 `inventory.list` 结果仍是藏品事实来源。

## 10. 删除范围

以下旧内容必须从运行时代码、资源加载、地图数据、检查脚本和产品文档中删除：

- 旧玩家控制器、NPC、对话和告示牌；只恢复默认玩家精灵与本次重新编写的触摸寻路。
- 探索区域、遭遇区域、随机或可见生态怪。
- 回合制战斗、攻击、血条、经验、捕捉和逃跑。
- 队伍、背包、道具、拾取、菜单、过场和本地数据管理。
- 原有怪兽目录、攻击、动画、遭遇、事件、道具、NPC 和告示牌 JSON。
- 原有角色、战斗、攻击、Monster Tamer 怪物、背包和队伍 UI 图片。
- 原有战斗与探索音频。
- 摇杆、A/B、键盘移动、跑步、地图拖动和缩放输入。
- Web Font Loader、Tweakpane 及其不再使用的运行时文件。

保留内容只有 Phaser 3.60.0、本次家园运行源码、`main_1.json`、Tiny Swords 白名单地形与场景资源、原 AxulArt 默认玩家精灵、正式 Catalog 图片路径以及相应许可证和来源记录。

## 11. 上线验收

1. 只有登录后的游戏页能够向家园注入真实藏品；直接访问 `/monster-tamer/` 不会获得藏品或启动可用家园。
2. 地图 JSON 严格为 `50 × 50`，最外两格全部为水域和不可通行格。
3. 地图只有 `Flat-Ground`、`Collision`、`Scenery` 与 `Water-Scenery`，不存在 Encounter、Item、NPC、Sign、玩家出生点或战斗数据。
4. 所有可通行格连通；所有水域、岸边、建筑、树干、树桩和岩石格不可通行。
5. `inventory.list` 结果只保留 `available > 0`，并按 `template_id` 一种显示一只，不设总数上限。
6. 地图宠物全部使用 `/assets/catalog/v1/thumb/` 正式图片，不加载旧 Monster Tamer 怪物。
7. 宠物不会进入静态障碍，也不会彼此占用同一格。
8. 手机点按可通行地面后，默认人物通过四方向寻路逐格移动，固定 `1` 倍镜头平滑跟随。
9. 地图不存在拖动、滚轮或双指缩放、键盘、WASD、方向键、鼠标点地移动或虚拟摇杆输入。
10. 点击宠物立即暂停场景，并在游戏上方展示由当前认证结果匹配的现有藏品详情；关闭后原地恢复。
11. 运行时没有资产写入、业务命令、浏览器持久化、Supabase 直连或第三方资源请求。
12. 旧探索与战斗源码、数据、图片、音频和文档定义全部不存在。
13. Phaser、Tiny Swords、AxulArt 默认人物、Catalog 图片和全部许可证路径在大小写敏感部署中可用。
14. 真实 Telegram iOS 与 Android 验收分别确认安全区、触摸寻路、镜头跟随、宠物点击、详情覆盖与返回流程；Desktop 与 Web 确认键盘和鼠标点地不能移动人物。
