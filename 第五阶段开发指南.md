# 第五阶段：支付和链上开发指南

<!-- AI-READING-INDEX:START -->

> 使用规则：先按本索引定位章节，再读取对应步骤；不要默认通读全文。本文是第五阶段支付、钱包、链上、Mint、后台和上线开发、验收指南。

- 范围判断：见 1. 第五阶段范围定义。
- 现状差距：见 2. Gap Analysis。
- 开发顺序：见 3. 第五阶段总体开发顺序。
- 分步实施：见 4.1 到 4.16，每一步都包含目标、检查对象、开发工作、测试要求、验收标准和产物。
- 数据库 / RLS / RPC：见 5. 数据库设计指南。
- API 合同：见 6. API 设计指南。
- 前端改造：见 7. 前端开发指南。
- Stars 支付流程：见 8. Telegram Stars 支付全流程。
- TON 钱包验证：见 9. TON Connect 与钱包验证全流程。
- NFT Mint 队列：见 10. NFT Mint 队列全流程。
- 测试、密钥、部署、回滚、验收：见 11 到 15。

边界提醒：涉及 SQL / 数据库改动时先核对远程 Supabase 真值；敏感变量只放服务端 / Vercel 环境变量；前端只负责 UI、状态展示和 API 调用。

<!-- AI-READING-INDEX:END -->

---

## 1. 第五阶段范围定义

### 1.1 Telegram Stars 支付模块

完成正式 Telegram Stars 支付接入：创建 invoice、打开支付、接收 payment webhook、根据 `successful_payment` 发货。Telegram Stars 数字商品支付使用 `XTR`，必须在收到 `successful_payment` 后才交付数字商品，`pre_checkout_query` 只用于校验订单并确认是否允许支付。

不做：前端直接判断支付成功、前端直接发货、前端保存 Bot Token。

### 1.2 支付订单与支付流水模块

基于现有 `payments.star_orders`、`star_invoices`、`star_payments`、`telegram_webhook_events`、`star_refunds`、`payment_disputes` 补齐正式状态机、异常状态、幂等索引、后台查询字段。

不做：把 Telegram Stars 当成用户可自由转移的内部余额；Stars 只作为外部支付凭证。

### 1.3 Telegram webhook 模块

实现 `api/telegram/webhook.ts`，处理 `pre_checkout_query`、`successful_payment`、退款 / 支付争议相关事件的落库、校验、幂等、错误记录。

不做：在 webhook 未落库前直接执行业务发货。

### 1.4 支付成功后开盒发货模块

使用现有 `api.gacha_process_paid_order` 作为正式发货事务入口。支付成功后必须能追溯到：`star_order`、`star_payment`、`draw_order`、`draw_results`、`item_instances`、`currency_ledger`、`user_pity_states`、`referral` 事件。

不做：前端生成抽卡结果。

### 1.5 TON Connect 钱包连接模块

前端接入 TON Connect UI，后端保存公开地址和钱包 app 信息。

不做：保存私钥、助记词、服务端读取用户钱包私钥。

### 1.6 钱包签名 / ton_proof 验证模块

实现后端 challenge 创建、ton_proof 验证、防重放、防过期、绑定钱包。

不做：仅凭前端传入地址就标记 verified。

### 1.7 NFT Mint 队列模块

基于现有 `onchain.mint_queue`、`wallet_enqueue_mint`、`onchain_mark_mint_success`、`onchain_mark_mint_failed` 实现异步 Mint 队列。

不做：用户请求直接阻塞等待链上确认。

### 1.8 链上交易同步模块

基于 `onchain.transactions` 记录 Mint 交易提交、确认、失败、重试、超时。

不做：交易失败时直接丢弃上下文。

### 1.9 链上 NFT 同步模块

实现钱包 NFT 同步任务，写入 `onchain.wallet_sync_jobs`、`wallet_nft_snapshots`，并尝试关联 `onchain.nft_items` 与游戏内 `item_instances`。

不做：前端直接写入 NFT 快照。

### 1.10 前端支付与钱包 UI 模块

改造开盒支付流程、钱包入口、Mint 状态、链上同步状态、刷新恢复状态。

不做：前端执行任何资产发放、NFT mint、支付确认逻辑。

### 1.11 后台运营管理模块

至少完成支付订单列表、异常支付列表、webhook 事件、退款 / dispute、Mint 队列、失败重试、链上交易、用户钱包查询。

不做：不受审计的后台余额修改或强制发货。

### 1.12 测试、监控、部署与回滚模块

完成支付、webhook、发货、钱包、Mint、链上同步、E2E、回滚开关、feature flag、日志、对账。

不做：未测试直接上线真实 Stars 支付。

---

## 2. Gap Analysis：现有项目与第五阶段目标的差距

| 模块                    | 当前已有内容                                                                                 | 缺失内容                                                                                           | 风险等级 | 建议处理方式                                                                                                                            |
| ----------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Telegram Stars 支付模块 | `.env.example` 已有 Stars 变量；`create-open-order` 已返回 `invoice_payload`、`xtr_amount`。 | 未实际调用 Telegram Bot API 创建 invoice；前端未打开 invoice。                                     | 高       | 复用 `api/boxes/create-open-order.ts`，扩展 `packages/server/src/payments/telegramStars.ts`；返回 invoice link 或前端可打开的 payload。 |
| 支付订单与流水          | `payments.star_orders`、`star_payments`、`star_invoices`、`telegram_webhook_events` 已有。   | 状态机与目标不完全一致；`star_invoices` 当前 rows 0；缺少完整 invoice 请求 / 响应记录。            | 中       | 扩展 payments 表状态字段和索引；不要重建已有表。                                                                                        |
| Telegram webhook        | `api/telegram/webhook.ts` 真实存在。                                                         | 文件为空；未实现 webhook secret、pre_checkout、successful_payment、事件落库、幂等。                | 高       | 实现 `api/telegram/webhook.ts`；所有事件先写 `payments.telegram_webhook_events`。                                                       |
| 支付成功后发货          | `api.gacha_process_paid_order`、`gacha_process_dev_paid_order` 已存在。                      | webhook 尚未调用正式 paid RPC；失败重试和异常状态不完整。                                          | 高       | successful_payment 后调用 `api.gacha_process_paid_order`；补充发货失败重试与 dispute 记录。                                             |
| 支付幂等                | `telegram_payment_charge_id` unique；`idempotency_key` 已在多个表出现。                      | webhook 重放处理和 `update_id` / charge id 联合幂等流程未实现。                                    | 高       | 使用 `payments.star_payments.telegram_payment_charge_id` 唯一约束；webhook 事件按 `update_id` 去重。                                    |
| 前端支付 UI             | `PaymentPendingSheet` 已存在；`BoxPage` 已能展示等待与结果查询。                             | 未打开 Telegram Stars 支付；支付取消 / 失败 / 过期状态展示不足。                                   | 高       | 新增 Stars 支付 Sheet / hook；调用 Telegram invoice；刷新后通过 order id 恢复。                                                         |
| TON Connect 前端        | `@tonconnect/ui-react` 已在 `apps/web` 依赖中。                                              | `AppProviders` 未接入；钱包按钮仍是占位。                                                          | 高       | 新增 `TonConnectProvider`；改造 `WalletEntryButton`；新增 `features/wallet`。                                                           |
| ton_proof 验证          | `wallet.schemas.ts` 已有 TonProof schema；`core.wallet_proofs` 表已存在。                    | `api/wallet/proof.ts` 和 server tonConnect 为空。                                                  | 高       | 新增 challenge / verify API；后端验证签名，保存 verified wallet。                                                                       |
| 钱包地址保存            | `core.user_wallets` 与 `wallet_save_verified_address` RPC 已存在。                           | API 未实现；状态枚举只支持 connected/disconnected/revoked，不直接表达 unverified / invalid_proof。 | 中       | 扩展或派生钱包状态；实现 `api/wallet/connect.ts` / `proof.ts` / `disconnect.ts`。                                                       |
| NFT Mint 队列           | `onchain.mint_queue`、`wallet_enqueue_mint` 已存在。                                         | API 空；状态机不足；无 worker / cron；无链上提交实现。                                             | 高       | 扩展 `mint_queue` 状态；实现 `api/wallet/mint.ts`、`mint-status.ts`、`api/cron/retry-mint-queue.ts`。                                   |
| 链上交易同步            | `onchain.transactions` 已存在。                                                              | 无 API、无 worker、无 TON API 查询实现。                                                           | 高       | 新增交易状态同步 service 与 cron；失败可重试。                                                                                          |
| 链上 NFT 同步           | `wallet_sync_jobs`、`wallet_nft_snapshots` 已存在。                                          | `api/wallet/sync-nfts.ts` 为空；无前端展示。                                                       | 中       | 实现同步 API；写入 job 和 snapshot；前端显示同步状态。                                                                                  |
| 后台运营                | `ops` 表存在；`apps/admin` placeholder；`api/admin/payments.ts` 空。                         | 没有支付 / Mint / 链上管理 UI。                                                                    | 高       | 新增 admin API 与最小后台页面；先做查询、暂停、重试、人工处理。                                                                         |
| 合约                    | `contracts` placeholder。                                                                    | 未发现 NFT Collection / Item 合约。                                                                | 高       | 阶段五至少完成已部署 Collection 接入方案；合约开发可单独分支。                                                                          |
| 测试                    | README 显示已有 API test、DB test。                                                          | 未发现 webhook、Stars、wallet、Mint 专项测试。                                                     | 高       | 新增 webhook 幂等测试、支付发货事务测试、Mint 队列测试、E2E。                                                                           |
| 部署配置                | `vercel.json` 已有 crons。                                                                   | 未接入 Mint cron、链上 sync cron；CI 未确认。                                                      | 中       | 扩展 `vercel.json` crons；确认 GitHub Actions 或 Vercel Preview 规则。                                                                  |
| Performance             | Supabase performance advisor 返回多条 unindexed FK。                                         | 阶段五高频查询路径可能变慢。                                                                       | 中       | 阶段五 migration 中补充支付、钱包、onchain、gacha 关键索引。                                                                            |

---

## 3. 第五阶段总体开发顺序

1. 第 1 步：冻结当前第四阶段基线。
2. 第 2 步：统一环境变量、密钥管理和 feature flags。
3. 第 3 步：支付 / 钱包 / onchain 数据库差异迁移设计，并固化 Telegram Stars 支付状态机。
4. 第 4 步：完善 Stars 开盒订单创建与 invoice 创建。
5. 第 5 步：实现 Telegram webhook：pre_checkout_query。
6. 第 6 步：实现 Telegram webhook：successful_payment 落库与幂等。
7. 第 7 步：successful_payment 后触发开盒发货事务。
8. 第 8 步：前端接入 Telegram Stars 支付打开、等待、结果恢复。
9. 第 9 步：前端接入 TON Connect Provider 与钱包入口。
10. 第 10 步：实现 wallet challenge、ton_proof 验证、钱包保存、断开。
11. 第 11 步：设计并实现 NFT Collection / metadata 接入准备。
12. 第 12 步：实现 Mint 请求 API 与 Mint 队列入队。
13. 第 13 步：实现 Mint worker / cron、链上交易记录、状态同步。
14. 第 14 步：实现链上 NFT 同步。
15. 第 15 步：实现后台支付与链上运营管理。
16. 第 16 步：补齐测试、监控、对账、异常补偿、预发布部署、真实测试账号验收、正式上线与回滚预案。

---

## 4. 详细开发步骤

### 4.1 第 1 步：冻结当前第四阶段基线

#### 目标

建立第五阶段开发前的稳定基线，确保任务、邀请、签到、分红、开盒开发模式、交易、库存、图鉴现有功能不被破坏。

#### 需要先检查的现有文件 / 表

| 类型 | 路径 / 表                                                                                                                 |
| ---- | ------------------------------------------------------------------------------------------------------------------------- |
| 前端 | `apps/web/src/features/tasks/pages/TasksPage.tsx`、`apps/web/src/features/box/pages/BoxPage.tsx`                          |
| API  | `api/boxes/create-open-order.ts`、`api/boxes/result.ts`                                                                   |
| DB   | `gacha.draw_orders`、`gacha.draw_results`、`inventory.item_instances`、`economy.currency_ledger`、`tasks.*`、`album.*`    |
| RPC  | `api.gacha_create_order`、`api.gacha_process_dev_paid_order`、`api.gacha_get_draw_result`、`api.task_*`、`api.referral_*` |
| 测试 | `tests/api/`、`supabase/tests/`                                                                                           |
| 配置 | `.env.example`、`vercel.json`                                                                                             |

#### 需要新增或修改的内容

不新增业务能力。只新增第五阶段基线说明文档和发布分支策略。

建议新增：

| 建议新增                           | 责任                                                  |
| ---------------------------------- | ----------------------------------------------------- |
| `docs/phase5-baseline.md`          | 记录当前可用功能、不可用功能、测试命令、冻结 commit。 |
| Git 分支 `phase5-payments-onchain` | 第五阶段开发主分支。                                  |

#### 后端开发工作

确认 `DEV_GACHA_PAYMENT_MODE` 在开发和测试环境可用，生产环境必须关闭。
确认 `api/boxes/create-open-order.ts` 在 dev mode 下仍能跑通抽卡闭环。
确认 `api/boxes/result.ts` 可查询历史订单结果。
确认 `requireSession` 对所有需要登录的 API 生效。

#### 数据库开发工作

记录当前 Supabase migration 最新版本。当前最新已确认到 `20260527045136 phase4_redact_commission_source_ids`。
导出现有 schema 差异说明。
记录 `payments` 和 `onchain` 表当前 rows 状态，避免上线时把 dev 数据误认为正式支付数据。

#### 前端开发工作

确认现有底部导航、资产栏、开盒页、任务页正常。
确认 `PaymentPendingSheet` 当前只作为等待占位，不代表正式支付完成。

#### 安全和幂等要求

不得在第五阶段分支中删除现有 idempotency 逻辑。
不得把 `.env`、Bot Token、service role key、TON 私钥提交到仓库。
不得修改现有 `currency_ledger` 历史数据。

#### 测试要求

运行现有 API test。
运行现有 DB test。
手动验证 dev 支付模式开盒不会破坏任务分红逻辑。
确认任务领取、签到、邀请分享仍可用。

#### 验收标准

- [ ] 第四阶段任务中心功能仍可访问。
- [ ] dev mode 开盒仍可生成 `draw_results`。
- [ ] `currency_ledger` 与 `user_balances` 可对账。
- [ ] 创建第五阶段开发分支。
- [ ] 记录当前 migration 最新版本。
- [ ] 生产环境 `DEV_GACHA_PAYMENT_MODE=false` 的检查项已写入上线 checklist。

#### 完成后产物

- 新增基线文档。
- 新增第五阶段开发分支。
- 一次完整回归测试记录。
- 当前 Supabase migration 和 API 状态快照。

---

### 4.2 第 2 步：补齐环境变量、密钥管理和 Feature Flags

#### 目标

确保 Telegram Stars、Telegram webhook、TON Connect、Mint、TON API、NFT Collection、cron、admin 所需环境变量已分类配置，并明确哪些变量可进前端、哪些只能在后端。

#### 需要先检查的现有文件 / 表

| 类型          | 路径 / 表             |
| ------------- | --------------------- |
| 环境变量      | `.env.example`        |
| Vercel        | `vercel.json`         |
| Feature flags | `ops.feature_flags`   |
| Settings      | `ops.system_settings` |

`.env.example` 已包含 Telegram Stars、webhook、Supabase、session、idempotency、TON、TON Connect、Mint、NFT metadata、wallet sync、feature flags 等变量。

#### 需要新增或修改的内容

建议修改：

| 文件 / 表                                              | 责任                                                                                               |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| `.env.example`                                         | 核对第五阶段所有变量是否完整；增加注释说明哪些必须在 Vercel server env。                           |
| `ops.feature_flags`                                    | 确认存在 `FEATURE_WALLET_ENABLED`、`FEATURE_TON_MINT_ENABLED`、支付暂停、Mint 暂停、链上同步暂停。 |
| `docs/security.md` 或建议新增 `docs/phase5-secrets.md` | 记录密钥管理规则。                                                                                 |

建议新增 feature flags：

| Flag                                          | 作用                         |
| --------------------------------------------- | ---------------------------- |
| `FEATURE_STARS_PAYMENT_ENABLED`               | 全局暂停 / 开启 Stars 支付。 |
| `FEATURE_PAYMENT_WEBHOOK_FULFILLMENT_ENABLED` | webhook 可落库但暂停发货。   |
| `FEATURE_WALLET_PROOF_ENABLED`                | 钱包验证开关。               |
| `FEATURE_WALLET_SYNC_ENABLED`                 | 链上 NFT 同步开关。          |
| `FEATURE_MINT_WORKER_ENABLED`                 | Mint worker 执行开关。       |
| `FEATURE_ADMIN_PAYMENT_OPS_ENABLED`           | 支付后台管理开关。           |

#### 后端开发工作

后端读取环境变量时必须做启动前校验或首次调用校验。
生产环境缺少 `TELEGRAM_BOT_TOKEN`、`TELEGRAM_WEBHOOK_SECRET`、`SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_SECRET_KEY`、`APP_SESSION_SECRET` 时应拒绝支付相关操作。
`TON_MINTER_PRIVATE_KEY` 只能在 Mint worker 使用，普通 API 不应读取。
`FEATURE_TON_MINT_ENABLED=false` 时，Mint API 应返回“Mint 暂未开放”。

#### 数据库开发工作

检查 `ops.feature_flags` 是否已有支付和 Mint 相关 key。
如果没有，新增 migration 插入 feature flag seed。
`ops.admin_audit_logs` 记录每一次开关变更。
`ops.system_settings` 可放置支付客服、支付过期时间、Mint retry 策略等非敏感配置。

#### 前端开发工作

前端只读取 `VITE_` 公开变量。
增加前端运行时判断：如果 `VITE_ENABLE_TON_CONNECT=false`，钱包入口显示“暂未开放”。
不要让 `VITE_` 变量包含 Bot Token、service role key、TON 私钥、webhook secret。

#### 安全和幂等要求

`TELEGRAM_WEBHOOK_SECRET` 必须只存在后端。
`SUPABASE_SERVICE_ROLE_KEY` 或 `SUPABASE_SECRET_KEY` 必须只存在后端。
`TON_MINTER_PRIVATE_KEY` 必须只存在 worker / server 环境。
支付开关关闭时，已支付订单仍必须允许补发，不应因为 feature flag 关闭而无法补偿。

#### 测试要求

测试生产环境启用 `DEV_GACHA_PAYMENT_MODE=true` 时后端拒绝启动或拒绝执行。
测试缺失 `TELEGRAM_BOT_TOKEN` 时创建正式支付订单失败。
测试 `FEATURE_STARS_PAYMENT_ENABLED=false` 时前端无法创建新支付订单。
测试 `FEATURE_PAYMENT_WEBHOOK_FULFILLMENT_ENABLED=false` 时 webhook 可以落库但不会发货。

#### 验收标准

- [ ] `.env.example` 覆盖第五阶段所有变量。
- [ ] 所有敏感变量未以 `VITE_` 开头。
- [ ] Vercel preview / production 环境变量清单已创建。
- [ ] 支付、钱包、Mint 开关可由后台或数据库控制。
- [ ] 支付暂停不影响历史订单查询。
- [ ] Mint 暂停不影响历史 Mint 状态查询。

#### 完成后产物

- 更新 `.env.example`。
- 新增或更新 feature flag seed migration。
- 新增密钥管理文档。
- 新增环境变量校验测试。

---

### 4.3 第 3 步：设计支付、钱包、onchain 数据库迁移

#### 目标

在不破坏已有表的前提下，补齐第五阶段所需状态、索引、约束、审计字段和查询能力。

#### 需要先检查的现有文件 / 表

| 类型      | 表 / RPC                                                                                                                                   |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| payments  | `payments.star_orders`、`star_invoices`、`star_payments`、`telegram_webhook_events`、`star_refunds`、`payment_disputes`                    |
| wallet    | `core.user_wallets`、`core.wallet_proofs`                                                                                                  |
| onchain   | `onchain.nft_collections`、`nft_items`、`mint_queue`、`transactions`、`wallet_sync_jobs`、`wallet_nft_snapshots`                           |
| inventory | `inventory.item_instances`、`inventory.inventory_locks`                                                                                    |
| ops       | `ops.idempotency_keys`、`ops.risk_events`、`ops.admin_audit_logs`                                                                          |
| RPC       | `gacha_process_paid_order`、`wallet_save_verified_address`、`wallet_enqueue_mint`、`onchain_mark_mint_success`、`onchain_mark_mint_failed` |

#### 需要新增或修改的内容

不建议重建已有表。建议使用 migration 扩展：

| 表                                                        | 建议处理                                                                                                |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `payments.star_orders`                                    | 扩展状态机：增加 `precheckout_checked` 或明确映射现有 `precheckout_ok`；增加 `fulfilling`、`disputed`。 |
| `payments.telegram_webhook_events`                        | 增加处理耗时、retry_count、next_retry_at、request_headers_hash、webhook_secret_verified 等字段。        |
| `payments.star_invoices`                                  | 增加 invoice 打开方式、invoice_link、bot_api_method、expires_at、last_opened_at。                       |
| `core.user_wallets`                                       | 增加状态表达或明确用 `verified_at` 派生 `verified`。                                                    |
| `core.wallet_proofs`                                      | 增加 proof nonce、request_id、used_at、wallet_public_key、proof_hash，防重放。                          |
| `core.wallet_connect_sessions`                            | 建议新增，用于 challenge 生命周期；也可复用 `wallet_proofs` pending 行。                                |
| `onchain.mint_queue`                                      | 扩展状态：`submitted`、`confirming`、`retrying`、`manual_review`。                                      |
| `onchain.transactions`                                    | 增加 transaction_type、external_api_provider、last_checked_at、check_count、raw_response。              |
| `onchain.wallet_sync_jobs`                                | 增加 idempotency_key、retry_count、next_retry_at、cursor。                                              |
| `ops.reconciliation_runs` / `economy.reconciliation_runs` | 当前在 `economy`，阶段五支付对账可以复用。                                                              |
| 关键索引                                                  | 补齐 performance advisor 中与支付 / 钱包 / onchain / gacha 相关的外键索引。                             |

#### 后端开发工作

所有 API 调用必须基于新的状态机。
RPC 返回状态必须能被前端识别。
所有 webhook / mint worker 更新状态时需要带 request_id、source、错误原因。

#### 数据库开发工作

添加状态枚举或 check constraint 扩展。
添加唯一约束：charge id、invoice payload、active mint queue、wallet proof challenge。
添加 partial index：active mint queue、pending webhook、pending payment、processing mint。
添加 RLS policy：用户只能看自己的钱包、Mint 状态、链上同步状态；后台只走 admin API。
确保 service role 能调用 RPC，但前端 anon role 不能直接写核心表。

#### 前端开发工作

前端不直接读取表；只适配 API 返回的新状态。
更新状态文案映射：支付中、已支付、发货中、已完成、失败、过期、退款、争议。

#### 安全和幂等要求

`telegram_payment_charge_id` 保持唯一。
`telegram_invoice_payload` 保持唯一。
`mint_queue` 同一 `item_instance_id` 不能有多个 active queue。
`wallet_proofs.challenge` 保持唯一且过期后不可复用。
所有支付 / Mint 状态更新必须可重复执行但不会重复发货 / 重复 Mint。

#### 测试要求

迁移测试：旧数据仍可查询。
约束测试：重复 charge id 插入失败。
Mint 队列测试：同一藏品不能创建两个 active queue。
RLS 测试：用户不能读取他人钱包 / Mint 队列。
Performance 测试：关键列表查询使用索引。

#### 验收标准

- [ ] 现有 payments 表未被重建。
- [ ] 支付状态机覆盖 created、invoice_created、precheckout_checked、paid、fulfilling、fulfilled、failed、expired、refunded、disputed。
- [ ] `telegram_payment_charge_id` 只能处理一次。
- [ ] 同一藏品只能有一个 active Mint 队列。
- [ ] 用户不能直接写 payments / onchain 核心表。
- [ ] Supabase performance advisor 中支付、钱包、onchain 相关关键索引已处理或记录延后原因。

#### 完成后产物

- 新增数据库 migration。
- 更新 database types。
- 新增 DB 事务测试。
- 更新数据库设计文档。

---

### 4.4 第 4 步：完善 Stars 开盒订单创建与 invoice 创建

#### 目标

把当前 `api/boxes/create-open-order.ts` 从“创建内部订单 + dev mode 可直接发货”升级为“创建正式 Telegram Stars invoice 的开盒支付订单”。

#### 需要先检查的现有文件 / 表

| 类型       | 路径 / 表                                                             |
| ---------- | --------------------------------------------------------------------- |
| API        | `api/boxes/create-open-order.ts`                                      |
| Server     | `packages/server/src/payments/telegramStars.ts`                       |
| Validation | `packages/validation/src/box.schemas.ts`                              |
| DB         | `payments.star_orders`、`payments.star_invoices`、`gacha.draw_orders` |
| RPC        | `api.gacha_create_order`                                              |

#### 需要新增或修改的内容

| 文件 / 表                                       | 责任                                                                                  |
| ----------------------------------------------- | ------------------------------------------------------------------------------------- |
| `api/boxes/create-open-order.ts`                | 保留现有 RPC 创建订单逻辑；新增正式环境 invoice 创建逻辑。                            |
| `packages/server/src/payments/telegramStars.ts` | 封装 Telegram Bot API invoice 创建、payload 构造、金额校验、响应落库。                |
| `payments.star_invoices`                        | 保存 invoice request / response / invoice_link / payload。                            |
| `gacha.draw_orders`                             | 关联 `payment_star_order_id`，保持 `invoice_payload` 与 `star_order` 一致。           |
| 前端 `box.api.ts`                               | 扩展 response：invoice link / invoice open mode / expires_at / payment_order_status。 |

#### 后端开发工作

创建订单时流程：

1. `requireSession` 获取真实 `user_id`。
2. 校验 box、draw count、expected price。
3. 调用 `api.gacha_create_order`。
4. 根据返回的 `star_order_id`、`invoice_payload`、`xtr_amount` 创建 Telegram invoice。
5. 把 invoice 请求和响应写入 `payments.star_invoices`。
6. 返回前端：`draw_order_id`、`star_order_id`、`invoice_payload`、`xtr_amount`、`invoice_link` 或前端可打开 invoice 的参数、`expires_at`。
7. 生产环境不得调用 `gacha_process_dev_paid_order`。

#### 数据库开发工作

确认 `star_orders.business_type='gacha_open'`，`business_id=draw_order_id`。
确认 `star_orders.xtr_amount` 与 `draw_orders.total_price_stars` 一致。
确认 `star_invoices.payload` 与 `star_orders.telegram_invoice_payload` 一致。
为 `star_invoices.star_order_id`、`payload`、`status` 建索引。
为过期订单添加查询索引。

#### 前端开发工作

`createOpenOrder` response 需要保留：

- `orderId`
- `starOrderId`
- `invoicePayload`
- `invoiceLink`
- `xtrAmount`
- `drawCount`
- `expiresAt`
- `paymentStatus`

`BoxPage` 在创建订单成功后，不能只显示等待 Sheet，需要尝试打开 Telegram Stars invoice。
若 Telegram invoice 打开失败，显示“支付未打开，可重试支付”而不是“支付成功”。

#### 安全和幂等要求

同一 `idempotency_key` 重复请求必须返回同一订单，不创建第二个订单。
前端 `expected_price_stars` 只用于发现价格变化，最终价格以后端 / DB 为准。
invoice payload 必须不可预测，且与订单强绑定。
生产环境不允许 dev paid mode。

#### 测试要求

创建单抽订单。
创建十连订单。
重复提交同一 idempotency key。
价格变化时拒绝或返回冲突。
盲盒暂停 / 售罄时拒绝。
dev mode 与正式 mode 分支测试。
invoice 创建失败时订单进入可重试或 failed 状态，不发货。

#### 验收标准

- [ ] 用户可以创建 Stars 开盒订单。
- [ ] 订单包含唯一 invoice payload。
- [ ] 单抽和十连金额正确。
- [ ] 十连 9 折金额以后端计算为准。
- [ ] 订单创建失败不会产生半完成发货。
- [ ] 未支付订单不会生成 `draw_results`。
- [ ] 生产环境不会调用 dev paid RPC。

#### 完成后产物

- 修改 `api/boxes/create-open-order.ts`。
- 实现 `packages/server/src/payments/telegramStars.ts`。
- 新增或扩展 payments migration。
- 更新 `box.api.ts` response normalize。
- 新增支付订单创建测试。

---

### 4.5 第 5 步：实现 Telegram webhook 的 pre_checkout_query

#### 目标

实现 `api/telegram/webhook.ts` 对 `pre_checkout_query` 的接收、secret 校验、订单校验、事件落库和确认响应。

#### 需要先检查的现有文件 / 表

| 类型   | 路径 / 表                                                                                            |
| ------ | ---------------------------------------------------------------------------------------------------- |
| API    | `api/telegram/webhook.ts`                                                                            |
| Server | `packages/server/src/payments/telegramStars.ts`                                                      |
| DB     | `payments.telegram_webhook_events`、`payments.star_orders`、`gacha.draw_orders`、`gacha.blind_boxes` |
| ENV    | `TELEGRAM_WEBHOOK_SECRET`、`TELEGRAM_BOT_TOKEN`                                                      |
| RPC    | 可新增 `api.payment_mark_precheckout_checked` 或通过 service role 表更新                             |

#### 需要新增或修改的内容

| 文件 / 表                                       | 责任                                                                |
| ----------------------------------------------- | ------------------------------------------------------------------- |
| `api/telegram/webhook.ts`                       | 处理 Telegram webhook 入口。                                        |
| `packages/server/src/payments/telegramStars.ts` | 校验 update、解析 pre_checkout_query、调用 answerPreCheckoutQuery。 |
| `payments.telegram_webhook_events`              | 先落库原始事件，再处理。                                            |
| `payments.star_orders`                          | 状态更新为 `precheckout_checked` 或当前兼容状态 `precheckout_ok`。  |

#### 后端开发工作

pre_checkout 处理步骤：

1. 校验 HTTP method。
2. 校验 webhook secret header。
3. 解析 Telegram update。
4. 识别 `pre_checkout_query`。
5. 将原始 update 写入 `payments.telegram_webhook_events`。
6. 根据 invoice payload 查询 `payments.star_orders`。
7. 校验订单存在、未过期、状态可支付、金额一致、currency 为 XTR。
8. 校验关联 `gacha.draw_orders` 状态可支付。
9. 校验盲盒仍可用、库存仍足够。
10. 调用 Telegram Bot API 确认 pre_checkout。
11. 更新 `star_orders.precheckout_at` 和状态。
12. 标记 webhook event processed。

#### 数据库开发工作

`telegram_webhook_events.update_id` 已有 unique，继续使用。
增加 event_type 索引。
增加 `process_status` + `created_at` 索引。
`star_orders.telegram_invoice_payload` 已唯一，作为订单查找入口。
需要记录 pre_checkout 校验失败原因。

#### 前端开发工作

无直接 UI 变更。
前端只会看到支付窗口内的失败信息或后续订单状态。
可在支付等待 Sheet 中展示“支付校验中”。

#### 安全和幂等要求

pre_checkout 不发货。
重复 pre_checkout 只重复确认订单可支付，不创建新订单。
金额不一致必须拒绝。
订单过期必须拒绝。
盲盒不可用或库存不足必须拒绝。
webhook 事件必须先落库。
secret 校验失败不落入业务处理链路，可写 risk event。

#### 测试要求

有效 pre_checkout。
订单不存在。
金额不一致。
currency 非 XTR。
订单过期。
盲盒暂停。
库存不足。
重复 update_id。
secret 错误。

#### 验收标准

- [ ] webhook 可以接收 pre_checkout_query。
- [ ] webhook secret 错误会被拒绝。
- [ ] pre_checkout 事件先写入 `telegram_webhook_events`。
- [ ] 金额一致、订单有效时返回确认。
- [ ] 金额不一致、订单过期、盲盒不可用时拒绝。
- [ ] pre_checkout 不会生成 `draw_results`。
- [ ] 重复 pre_checkout 不产生副作用。

#### 完成后产物

- 实现 `api/telegram/webhook.ts` 的 pre_checkout 分支。
- 支付 webhook 事件落库逻辑。
- 新增 pre_checkout 测试。
- 更新 Telegram webhook 文档。

---

### 4.6 第 6 步：实现 successful_payment 落库与幂等

#### 目标

在收到 Telegram `successful_payment` 后，先落库支付流水，再基于 `telegram_payment_charge_id` 做唯一幂等，防止重复发货。

#### 需要先检查的现有文件 / 表

| 类型   | 路径 / 表                                                                            |
| ------ | ------------------------------------------------------------------------------------ |
| API    | `api/telegram/webhook.ts`                                                            |
| Server | `packages/server/src/payments/telegramStars.ts`                                      |
| DB     | `payments.star_payments`、`payments.star_orders`、`payments.telegram_webhook_events` |
| RPC    | `api.gacha_process_paid_order`                                                       |

#### 需要新增或修改的内容

| 文件 / 表                          | 责任                                            |
| ---------------------------------- | ----------------------------------------------- |
| `api/telegram/webhook.ts`          | 处理 successful_payment 分支。                  |
| `payments.star_payments`           | 保存 charge id、XTR 金额、payload、raw update。 |
| `payments.star_orders`             | 状态转为 `paid`，再进入发货。                   |
| `payments.telegram_webhook_events` | 记录处理状态、错误原因。                        |

#### 后端开发工作

successful_payment 处理步骤：

1. 校验 webhook secret。
2. 原始 update 先写 `telegram_webhook_events`。
3. 解析 `successful_payment`。
4. 获取 `invoice_payload`、`telegram_payment_charge_id`、`currency`、`total_amount`。
5. 通过 invoice payload 查 `star_orders`。
6. 校验 `currency='XTR'`、金额一致、订单未 fulfilled。
7. 尝试插入 `star_payments`。
8. 如果 `telegram_payment_charge_id` 已存在，判定为重复 webhook，返回成功但不发货。
9. 更新 `star_orders.status='paid'`。
10. 进入下一步发货事务。

#### 数据库开发工作

确认 `star_payments.telegram_payment_charge_id` unique。
新增或确认 `star_payments.invoice_payload` 索引。
新增或确认 `star_payments.star_order_id` 索引。
支付落库失败不能调用发货 RPC。
`star_orders.status` 需要支持 `paid`、`fulfilling`、`fulfilled`。

#### 前端开发工作

无需直接处理 webhook。
支付等待 Sheet 可轮询 `/api/boxes/result` 或新增订单状态 API。
重复刷新不会影响发货。

#### 安全和幂等要求

`telegram_payment_charge_id` 是支付幂等核心。
不要只依赖 invoice payload 判断支付成功。
successful_payment 中金额不一致必须进入异常状态。
重复 webhook 必须返回 200，避免 Telegram 继续重试造成噪音，但不能重复发货。

#### 测试要求

成功支付落库。
重复 `telegram_payment_charge_id`。
重复 `update_id`。
金额不一致。
payload 不存在。
订单已 fulfilled 后再次收到 successful_payment。
插入 `star_payments` 成功但发货失败。

#### 验收标准

- [ ] successful_payment 可以写入 `star_payments`。
- [ ] `telegram_payment_charge_id` 唯一。
- [ ] 重复 successful_payment 不重复发货。
- [ ] 金额不一致进入异常状态。
- [ ] webhook 事件处理状态可查询。
- [ ] 原始 update 可审计。

#### 完成后产物

- successful_payment 分支实现。
- 支付流水落库测试。
- 重复 webhook 幂等测试。
- 支付异常记录文档。

---

### 4.7 第 7 步：successful_payment 后触发开盒发货事务

#### 目标

将正式支付成功事件接入现有 `api.gacha_process_paid_order`，在数据库事务中完成发货、抽卡、库存、保底、K-coin 返还、任务 / 邀请进度更新。

#### 需要先检查的现有文件 / 表

| 类型 | 路径 / 表                                                                                                                                                     |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API  | `api/telegram/webhook.ts`、`api/boxes/result.ts`                                                                                                              |
| RPC  | `api.gacha_process_paid_order`、`api.referral_process_first_open`、`api.referral_create_commission`、`api.task_record_progress`                               |
| DB   | `payments.star_orders`、`star_payments`、`gacha.draw_orders`、`draw_results`、`inventory.item_instances`、`economy.currency_ledger`、`gacha.user_pity_states` |

#### 需要新增或修改的内容

| 文件 / 表 / RPC                    | 责任                                                                             |
| ---------------------------------- | -------------------------------------------------------------------------------- |
| `api/telegram/webhook.ts`          | successful_payment 后调用 paid RPC。                                             |
| `api.gacha_process_paid_order`     | 确认接受 `star_order_id` / `payment_charge_id` / `invoice_payload`，并保证幂等。 |
| `payments.star_orders`             | 状态从 `paid` → `fulfilling` → `fulfilled` 或 `failed`。                         |
| `payments.telegram_webhook_events` | 记录发货结果。                                                                   |
| `ops.risk_events`                  | 记录异常发货、金额不一致、重复发货尝试。                                         |

#### 后端开发工作

发货处理步骤：

1. 将 `star_orders` 状态置为 `fulfilling`。
2. 调用 `api.gacha_process_paid_order`。
3. RPC 内部锁定 `draw_orders`、`star_orders`、相关库存 / 余额行。
4. 生成 `draw_results`。
5. 创建 `inventory.item_instances`。
6. 更新 `gacha.user_pity_states`。
7. 返还 K-coin，写 `economy.currency_ledger`。
8. 更新 `draw_orders.status='completed'`。
9. 更新 `star_orders.status='fulfilled'`。
10. 如果失败，记录错误，保持可重试状态。

#### 数据库开发工作

`gacha_process_paid_order` 必须检查：

- draw_order 存在。
- star_order 存在。
- star_payment 存在。
- payment charge id 未被其他订单使用。
- draw_order 未 completed。
- draw_results 不重复生成。
- ledger idempotency key 唯一。
- inventory item instance 不重复生成。
- 库存足够。
- drop pool version 与订单一致。

#### 前端开发工作

`useDrawResult` 可继续复用。
前端应区分：

- `pending_payment`
- `paid_waiting_fulfillment`
- `fulfilling`
- `completed`
- `failed`
- `expired`
- `refunded`

`DrawResultModal` 在 `fulfilling` 时显示“支付已成功，发货处理中”。
支付成功但发货失败时显示“支付已成功，奖励补发中”，不要让用户重复支付。

#### 安全和幂等要求

发货 RPC 必须可重复调用。
重复调用只能返回已有结果。
发货中断后可以由后台重试。
任何发货失败不能丢失 `star_payment`。
K-coin 返还和 item_instances 必须与 draw_results 一致。

#### 测试要求

支付成功正常发货。
重复 webhook 只发货一次。
支付成功但 RPC 中途失败后可重试。
K-coin 返还正确。
保底正确更新。
库存不足时进入异常流程。
draw_results 数量等于 draw_count。
ledger 和 balance 对账一致。

#### 验收标准

- [ ] 支付成功后触发 `gacha_process_paid_order`。
- [ ] 每个 paid order 只发货一次。
- [ ] `draw_results` 数量正确。
- [ ] `item_instances` 正确创建。
- [ ] K-coin 返还正确。
- [ ] `currency_ledger` 有完整流水。
- [ ] 保底状态正确更新。
- [ ] 前端刷新后仍能查询结果。
- [ ] 发货失败可后台重试。

#### 完成后产物

- webhook 发货调用实现。
- paid order 幂等事务测试。
- 发货失败重试文档。
- 支付发货对账脚本规划。

---

### 4.8 第 8 步：前端 Stars 支付打开、等待和结果恢复

#### 目标

让用户点击单抽 / 十连后，前端创建订单、打开 Telegram Stars 支付、显示支付状态、支持刷新后恢复订单结果。

#### 需要先检查的现有文件 / 表

| 类型       | 路径                                                                        |
| ---------- | --------------------------------------------------------------------------- |
| 页面       | `apps/web/src/features/box/pages/BoxPage.tsx`                               |
| API client | `apps/web/src/features/box/box.api.ts`                                      |
| Hook       | `apps/web/src/features/box/hooks/useCreateOpenOrder.ts`、`useDrawResult.ts` |
| Component  | `PaymentPendingSheet.tsx`、`DrawResultModal.tsx`                            |
| Endpoints  | `apps/web/src/api/endpoints.ts`                                             |

#### 需要新增或修改的内容

| 文件                                                             | 责任                                                                     |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `apps/web/src/features/box/hooks/useStarsPayment.ts`（建议新增） | 打开 Telegram invoice、处理支付窗口关闭、写入本地 pending order id。     |
| `PaymentPendingSheet.tsx`                                        | 增加支付中、支付取消、支付已打开、支付已成功待发货、发货失败补发中状态。 |
| `box.api.ts`                                                     | 增加支付订单状态字段 normalize。                                         |
| `queryKeys`                                                      | 增加 payment order / pending draw order query key。                      |
| `API_ENDPOINTS`                                                  | 增加支付订单状态查询 endpoint，或复用 `/boxes/result`。                  |

#### 后端开发工作

提供订单状态查询 API。可以复用 `api/boxes/result.ts`，但建议新增：

| 建议 API                      | 责任                                                                     |
| ----------------------------- | ------------------------------------------------------------------------ |
| `api/boxes/payment-status.ts` | 返回 star_order、draw_order、payment、fulfillment 状态，不返回抽卡结果。 |

#### 数据库开发工作

无需直接变更。
但订单状态查询 RPC 应避免暴露敏感 charge id、raw update、Bot 信息。

#### 前端开发工作

用户流程：

1. 点击单抽 / 十连。
2. 调用 `createOpenOrder`。
3. 拿到 invoice link / payload。
4. 打开 Telegram Stars 支付。
5. 支付窗口关闭后不直接认为成功。
6. 展示等待 Sheet。
7. 轮询订单状态或点击“查看结果”。
8. completed 后展示 `DrawResultModal`。
9. 刷新页面后从本地 pending order id 或服务端最近订单恢复。

#### 安全和幂等要求

前端不能因支付窗口返回成功就发货。
前端不能把 invoice payload 当作支付成功凭证。
重复点击必须依赖 idempotency key 和按钮锁。
刷新恢复只查服务端状态，不相信 localStorage 中的状态。

#### 测试要求

支付窗口打开成功。
支付窗口取消。
支付成功但 webhook 延迟。
支付成功但发货处理中。
用户刷新页面后恢复 pending order。
订单过期。
重复点击单抽按钮。
十连价格展示和后端返回一致。

#### 验收标准

- [ ] 点击单抽 / 十连可以打开 Telegram Stars 支付。
- [ ] 用户取消支付不会发货。
- [ ] 支付窗口关闭后前端不会自行判断成功。
- [ ] 支付成功后等待 webhook 发货。
- [ ] 发货完成后展示抽卡结果。
- [ ] 页面刷新后仍能恢复订单状态。
- [ ] 支付失败 / 过期 / 发货失败有明确提示。

#### 完成后产物

- 新增 Stars payment hook。
- 修改 `BoxPage` 和 `PaymentPendingSheet`。
- 修改 box API response normalize。
- 新增前端 E2E 支付状态测试。

---

### 4.9 第 9 步：前端接入 TON Connect Provider 与钱包入口

#### 目标

把当前钱包占位入口升级为真实 TON Connect 钱包连接入口，并在资产栏 / 钱包面板显示连接状态。

#### 需要先检查的现有文件 / 表

| 类型     | 路径                                                            |
| -------- | --------------------------------------------------------------- |
| Provider | `apps/web/src/app/providers/AppProviders.tsx`                   |
| 入口     | `apps/web/src/features/assets/components/WalletEntryButton.tsx` |
| ENV      | `VITE_TONCONNECT_MANIFEST_URL`、`VITE_ENABLE_TON_CONNECT`       |
| 依赖     | `@tonconnect/ui-react`                                          |

#### 需要新增或修改的内容

| 文件                                                                          | 责任                                                |
| ----------------------------------------------------------------------------- | --------------------------------------------------- |
| `apps/web/src/app/providers/TonConnectProvider.tsx`（建议新增）               | 接入 TON Connect UI provider。                      |
| `apps/web/src/features/wallet/components/WalletConnectButton.tsx`（建议新增） | 打开钱包连接。                                      |
| `apps/web/src/features/wallet/components/WalletStatusSheet.tsx`（建议新增）   | 展示地址、验证状态、断开、同步 NFT、Mint 队列入口。 |
| `WalletEntryButton.tsx`                                                       | 从占位 toast 改为打开钱包连接 / 状态面板。          |
| `public/tonconnect-manifest.json`                                             | 确认已存在；如果未发现则建议新增。                  |
| `API_ENDPOINTS`                                                               | 增加 wallet 分组。                                  |

#### 后端开发工作

本步骤后端只需要提供 wallet status API 的准备。
前端拿到地址后，不能直接标记 verified，必须调用后端 proof 流程。

#### 数据库开发工作

无需直接变更。
后续会写入 `core.user_wallets`、`core.wallet_proofs`。

#### 前端开发工作

显示状态：

| 状态         | UI                         |
| ------------ | -------------------------- |
| 未连接       | “Connect Wallet”           |
| 连接中       | loading                    |
| 已连接未验证 | 显示短地址 + “验证钱包”    |
| 已验证       | 显示短地址 + verified 标识 |
| 验证失败     | 显示错误 + 重试            |
| 已断开       | 回到未连接                 |

#### 安全和幂等要求

前端不得保存私钥。
前端只保存公开地址和 UI 状态。
真正 verified 状态来自后端。
钱包地址显示需要截断，避免 UI 泄漏完整长地址到不必要区域。

#### 测试要求

未启用 feature flag 时显示暂未开放。
连接钱包成功。
用户取消连接。
连接后未验证状态。
刷新后从后端状态恢复。
断开钱包后 UI 恢复未连接。

#### 验收标准

- [ ] `AppProviders` 已接入 TonConnectProvider。
- [ ] 钱包入口不再是占位 toast。
- [ ] 用户可以打开 TON Connect 钱包选择。
- [ ] 前端拿到公开地址后不会直接标记 verified。
- [ ] 刷新后可查询后端钱包状态。
- [ ] 断开钱包后 UI 状态正确。

#### 完成后产物

- 新增 TonConnect Provider。
- 新增 wallet feature 前端模块。
- 更新 `WalletEntryButton`。
- 新增 wallet API endpoint 常量。
- 新增前端钱包连接测试。

---

### 4.10 第 10 步：实现 wallet challenge、ton_proof 验证、钱包保存和断开

#### 目标

后端生成 challenge，前端请求钱包签名 / ton_proof，后端验证后保存公开地址，并支持断开钱包。

#### 需要先检查的现有文件 / 表

| 类型       | 路径 / 表                                      |
| ---------- | ---------------------------------------------- |
| API        | `api/wallet/connect.ts`、`api/wallet/proof.ts` |
| Server     | `packages/server/src/ton/tonConnect.ts`        |
| Validation | `packages/validation/src/wallet.schemas.ts`    |
| DB         | `core.user_wallets`、`core.wallet_proofs`      |
| RPC        | `api.wallet_save_verified_address`             |

#### 需要新增或修改的内容

| 文件 / 表                               | 责任                                                            |
| --------------------------------------- | --------------------------------------------------------------- |
| `api/wallet/challenge.ts`（建议新增）   | 创建 proof challenge。                                          |
| `api/wallet/proof.ts`                   | 验证 ton_proof，调用 RPC 保存 verified address。                |
| `api/wallet/connect.ts`                 | 保存 connected_unverified 状态或返回当前 wallet session。       |
| `api/wallet/status.ts`（建议新增）      | 查询钱包状态。                                                  |
| `api/wallet/disconnect.ts`（建议新增）  | 断开钱包。                                                      |
| `packages/server/src/ton/tonConnect.ts` | 实现 proof 验证、domain、timestamp、signature、防重放。         |
| `core.wallet_proofs`                    | 记录 challenge、proof、status、expires_at、verified_at、error。 |

#### 后端开发工作

推荐流程：

1. 前端请求 `/api/wallet/challenge`。
2. 后端创建 `core.wallet_proofs` pending 行，写入 challenge 和 expires_at。
3. 前端通过 TON Connect 请求 ton_proof。
4. 前端提交 account + proof 到 `/api/wallet/proof`。
5. 后端校验 challenge 存在、未过期、未使用。
6. 后端校验 domain、timestamp、address、public key、signature。
7. 校验通过后调用 `api.wallet_save_verified_address`。
8. `core.user_wallets.verified_at` 写入时间。
9. proof 状态更新为 verified。
10. 用户断开时更新 `core.user_wallets.status='disconnected'`、`disconnected_at`。

#### 数据库开发工作

`core.wallet_proofs.challenge` 已 unique。
建议增加 `proof_hash`、`used_at`、`wallet_public_key`、`request_id`。
为 `wallet_proofs.user_id`、`wallet_id` 增加索引，performance advisor 已提示相关外键未索引。
`core.user_wallets` 当前 status 只有 `connected/disconnected/revoked`，建议：

- 方案 A：不扩展 status，用 `verified_at IS NULL` 表达 connected_unverified。
- 方案 B：扩展 status，加入 `connected_unverified`、`verified`。

推荐方案 A，迁移较小。

#### 前端开发工作

新增 hooks：

| Hook                  | 责任                   |
| --------------------- | ---------------------- |
| `useWalletStatus`     | 查询后端 wallet 状态。 |
| `useWalletChallenge`  | 获取 challenge。       |
| `useVerifyTonProof`   | 提交 proof。           |
| `useDisconnectWallet` | 断开钱包。             |

钱包状态 UI 显示：

- 未连接
- 已连接，等待验证
- 验证中
- 已验证
- 验证失败
- proof 过期，请重试
- 已断开

#### 安全和幂等要求

challenge 有效期建议 5 分钟。
challenge 只能使用一次。
proof timestamp 必须校验。
domain 必须匹配当前 app 域名。
重放 proof 必须失败。
前端传来的 address 不能直接信任。
同一用户可以连接多个钱包时，必须有 primary wallet 规则；如果只允许一个主钱包，切换钱包要审计。

#### 测试要求

有效 proof。
过期 proof。
重复 proof。
错误 domain。
错误 address。
错误 signature。
用户断开钱包。
同一用户重复连接同一钱包。
同一钱包被多个用户绑定时的策略测试。

#### 验收标准

- [ ] 后端可以生成 wallet challenge。
- [ ] 前端可以请求 ton_proof。
- [ ] 后端可以验证 proof。
- [ ] proof 过期会失败。
- [ ] proof 重放会失败。
- [ ] 钱包公开地址保存到 `core.user_wallets`。
- [ ] `verified_at` 正确写入。
- [ ] 用户可以断开钱包。
- [ ] 前端不保存私钥。

#### 完成后产物

- 新增 wallet challenge / status / disconnect API。
- 实现 `api/wallet/proof.ts`。
- 实现 `packages/server/src/ton/tonConnect.ts`。
- 新增 wallet proof 测试。
- 更新 wallet 开发文档。

---

### 4.11 第 11 步：准备 NFT Collection、metadata 与 Mint 前置条件

#### 目标

让项目具备 Mint 的前置配置：NFT Collection 地址、metadata base URI、藏品是否可 Mint、用户钱包 verified、藏品状态可 Mint。

#### 需要先检查的现有文件 / 表

| 类型       | 路径 / 表                                                                                                           |
| ---------- | ------------------------------------------------------------------------------------------------------------------- |
| Contracts  | `contracts/package.json`                                                                                            |
| ENV        | `TON_COLLECTION_ADDRESS`、`NFT_METADATA_BASE_URL`、`NFT_ITEM_METADATA_BASE_URI`                                     |
| DB         | `onchain.nft_collections`、`catalog.collectible_templates`、`catalog.collectible_media`、`inventory.item_instances` |
| Validation | `CreateMintBodySchema`                                                                                              |

#### 需要新增或修改的内容

| 文件 / 表                                    | 责任                                                   |
| -------------------------------------------- | ------------------------------------------------------ |
| `contracts/README.md`（建议新增）            | 记录 Collection 地址、部署网络、owner、metadata 规则。 |
| `onchain.nft_collections`                    | 写入当前网络 Collection 配置。                         |
| `catalog.collectible_templates.nft_mintable` | 确认哪些藏品可 Mint。                                  |
| `catalog.collectible_media`                  | 确认 NFT image / metadata URL。                        |
| `public/nft-metadata/` 或 Storage bucket     | 提供 Collection 和 item metadata。                     |

#### 后端开发工作

读取 active collection。
校验 collection network 与用户钱包 network 一致。
生成 item metadata snapshot。
如果合约尚未开发，阶段五可以先支持“已部署 Collection 地址接入”，合约开发另设分支。

#### 数据库开发工作

`onchain.nft_collections` 已有字段 `collection_address`、`metadata_url`、`content_base_url`、`royalty_config`、`status`。
需要确认至少一条 active collection。
如果还没有合约地址，Mint API 应返回“Collection 未配置”。

#### 前端开发工作

藏品详情页增加 Mint 入口显示条件：

- 用户已验证钱包。
- 藏品属于用户。
- 藏品 `nft_mintable=true`。
- 藏品未挂售、未分解、未锁定。
- `nft_mint_status='not_minted'` 或 failed 可重试。

#### 安全和幂等要求

metadata 生成必须后端完成。
前端不能提交任意 metadata 覆盖链上 NFT 内容。
Collection 地址必须来自数据库 / 环境，不信任前端。

#### 测试要求

Collection 未配置。
Collection paused。
NFT metadata 缺失。
不可 Mint 藏品。
已 Mint 藏品重复 Mint。
用户钱包 network 不匹配。

#### 验收标准

- [ ] active Collection 可查询。
- [ ] NFT metadata base URI 配置完成。
- [ ] 不可 Mint 藏品不会显示 Mint 按钮。
- [ ] 未验证钱包不能 Mint。
- [ ] Collection 未配置时给出明确提示。
- [ ] metadata 不由前端生成。

#### 完成后产物

- Collection 配置记录。
- metadata 规则文档。
- 前端 Mint 入口显示规则。
- Collection 配置测试。

---

### 4.12 第 12 步：实现 Mint 请求 API 与 Mint 队列入队

#### 目标

用户选择可 Mint 的藏品后，请求后端创建 Mint 队列；后端锁定藏品，写入 `onchain.mint_queue`，返回队列状态。

#### 需要先检查的现有文件 / 表

| 类型       | 路径 / 表                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------- |
| API        | `api/wallet/mint.ts`                                                                               |
| Server     | `packages/server/src/ton/mintQueue.ts`                                                             |
| Validation | `CreateMintBodySchema`                                                                             |
| DB         | `onchain.mint_queue`、`inventory.item_instances`、`inventory.inventory_locks`、`core.user_wallets` |
| RPC        | `api.wallet_enqueue_mint`                                                                          |

#### 需要新增或修改的内容

| 文件 / 表                              | 责任                                               |
| -------------------------------------- | -------------------------------------------------- |
| `api/wallet/mint.ts`                   | 校验 session、参数、调用 `wallet_enqueue_mint`。   |
| `api/wallet/mint-status.ts`            | 查询用户 Mint 队列。                               |
| `packages/server/src/ton/mintQueue.ts` | 封装入队、状态转换、重试策略。                     |
| `onchain.mint_queue`                   | 扩展状态和错误字段。                               |
| `inventory.item_instances`             | 状态更新为 `minting`，`nft_mint_status='queued'`。 |
| `inventory.inventory_locks`            | 创建 `lock_type='mint'` active lock。              |

#### 后端开发工作

Mint 入队流程：

1. `requireSession` 获取用户。
2. 校验 feature flag。
3. 校验钱包 verified。
4. 校验 item instance 属于用户。
5. 校验藏品状态 `available`，不在市场，不在分解，不在合成，不在 active lock。
6. 校验 `nft_mint_status='not_minted'` 或允许 failed retry。
7. 校验 Collection active。
8. 调用 `api.wallet_enqueue_mint`。
9. 返回 `mint_queue_id`、status、预计处理状态。

#### 数据库开发工作

`wallet_enqueue_mint` 必须在一个事务中：

- 锁定 item instance。
- 创建 inventory lock。
- 创建 mint_queue。
- 更新 item nft_mint_status。
- 写 `item_instance_events` 为 `mint_queued`。
- 防止同一 item 重复入队。

需要为 active mint queue 增加唯一约束或 partial unique index。

#### 前端开发工作

新增：

| 文件                                            | 责任                           |
| ----------------------------------------------- | ------------------------------ |
| `features/wallet/hooks/useCreateMint.ts`        | 调用 Mint API。                |
| `features/wallet/hooks/useMintQueue.ts`         | 查询 Mint 状态。               |
| `features/collection/components/MintButton.tsx` | 藏品详情 Mint 按钮。           |
| `features/wallet/components/MintQueueSheet.tsx` | 展示排队、处理中、成功、失败。 |

#### 安全和幂等要求

非本人藏品不能 Mint。
挂售中藏品不能 Mint。
分解 / consumed 藏品不能 Mint。
重复 `idempotency_key` 返回同一 queue。
同一 item 不能多个 active queue。
前端不能指定任意 collection 覆盖后端默认规则，除非后端允许并校验。

#### 测试要求

未连接钱包。
未验证钱包。
非本人藏品。
挂售中藏品。
已分解藏品。
重复 Mint 请求。
Collection 未配置。
正常入队。
入队后库存被锁定。

#### 验收标准

- [ ] 用户可以对可 Mint 藏品创建 Mint 请求。
- [ ] Mint 请求进入 `onchain.mint_queue`。
- [ ] item instance 状态进入 minting / queued。
- [ ] active inventory lock 创建成功。
- [ ] 重复请求不会创建多个 active queue。
- [ ] 非本人 / 挂售中 / 分解 / 已 Mint 藏品不能 Mint。
- [ ] 前端能展示 queued 状态。

#### 完成后产物

- 实现 `api/wallet/mint.ts`。
- 实现 `api/wallet/mint-status.ts`。
- 实现 `packages/server/src/ton/mintQueue.ts` 入队部分。
- 新增 Mint 入队 DB 测试。
- 新增前端 Mint 状态组件。

---

### 4.13 第 13 步：实现 Mint worker / cron 与链上交易状态同步

#### 目标

异步处理 `onchain.mint_queue`，提交链上 Mint 交易，写入 `onchain.transactions`，查询确认状态，成功后绑定 NFT Item 地址。

#### 需要先检查的现有文件 / 表

| 类型   | 路径 / 表                                                                                     |
| ------ | --------------------------------------------------------------------------------------------- |
| Cron   | `api/cron/retry-mint-queue.ts`                                                                |
| Server | `packages/server/src/ton/nft.ts`、`packages/server/src/ton/mintQueue.ts`                      |
| DB     | `onchain.mint_queue`、`onchain.transactions`、`onchain.nft_items`、`inventory.item_instances` |
| RPC    | `api.onchain_mark_mint_success`、`api.onchain_mark_mint_failed`                               |
| Vercel | `vercel.json`                                                                                 |

#### 需要新增或修改的内容

| 文件 / 表                                           | 责任                                                                                    |
| --------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `api/cron/retry-mint-queue.ts`                      | 定时拉取 queued / retrying Mint 任务。                                                  |
| `api/cron/sync-onchain-transactions.ts`（建议新增） | 查询 pending / confirming 交易状态。                                                    |
| `packages/server/src/ton/nft.ts`                    | 封装 Collection Mint、NFT item 地址解析、交易查询。                                     |
| `onchain.transactions`                              | 保存 tx hash、query id、状态、raw response。                                            |
| `onchain.mint_queue`                                | 状态扩展为 processing、submitted、confirming、minted、failed、retrying、manual_review。 |
| `vercel.json`                                       | 增加 Mint worker cron 与交易同步 cron。                                                 |

#### 后端开发工作

Mint worker 流程：

1. 校验 cron secret。
2. 校验 `FEATURE_MINT_WORKER_ENABLED` 和 `TON_MINT_ENABLED`。
3. 拉取 `queued` / `retrying` 且 `next_attempt_at <= now` 的任务。
4. 将任务状态置为 `processing`。
5. 读取 Collection、metadata、目标 wallet。
6. 使用服务端 minter key 提交链上交易。
7. 写入 `onchain.transactions`。
8. 将 `mint_queue` 状态改为 `submitted` / `confirming`。
9. 交易确认后调用 `api.onchain_mark_mint_success`。
10. 失败时调用 `api.onchain_mark_mint_failed`，按 retry 策略进入 retrying 或 manual_review。

#### 数据库开发工作

扩展 `mint_queue.status`。
`onchain.transactions.related_type='mint_queue'`、`related_id=mint_queue.id`。
`tx_hash` unique。
`query_id` 建索引。
`mint_queue.attempt_count` 和 `max_attempts` 控制重试。
超过最大重试进入 `manual_review`。
成功后：

- `onchain.nft_items` 插入 item。
- `inventory.item_instances.nft_mint_status='minted'`。
- `inventory.item_instances.minted_nft_item_id` 写入。
- `inventory.item_instances.status='minted'` 或保持 available + minted 标记，需要产品确认。
- release / consume mint lock 的规则需要明确。

#### 前端开发工作

`MintQueueSheet` 显示：

- 排队中
- 处理中
- 已提交链上
- 等待确认
- Mint 成功
- Mint 失败
- 重试中
- 人工处理中

刷新页面后从 `mint-status` 查询恢复。

#### 安全和幂等要求

Mint worker 必须 cron secret 鉴权。
worker 每次领取任务要加锁，防止并发 worker 重复提交。
链上交易提交成功但 API 超时时，不可盲目再次提交，必须先查询 `query_id` / tx 状态。
`onchain_mark_mint_success` 必须幂等。
同一个 item_instance 只能绑定一个 nft_item。

#### 测试要求

queued → processing。
processing 失败 → retrying。
超过重试 → manual_review。
提交成功 → confirming。
确认成功 → minted。
重复 worker 并发抢任务。
交易 hash 已存在。
成功回写时重复调用。
TON API 不可用。

#### 验收标准

- [ ] Mint worker 可以领取队列任务。
- [ ] Mint worker 不会重复处理同一任务。
- [ ] 链上交易写入 `onchain.transactions`。
- [ ] Mint 成功后创建 `onchain.nft_items`。
- [ ] item instance 正确绑定 NFT 地址。
- [ ] Mint 失败可重试。
- [ ] 超过重试次数进入人工处理。
- [ ] 前端可以查询 Mint 状态。

#### 完成后产物

- 实现 Mint worker cron。
- 实现 onchain transaction sync cron。
- 更新 `vercel.json` crons。
- 实现 TON NFT service。
- 新增 Mint worker 测试。
- 新增链上交易同步文档。

---

### 4.14 第 14 步：实现链上 NFT 同步

#### 目标

用户连接 verified 钱包后，可以手动或定时同步链上 NFT，展示钱包持有的游戏 NFT、Mint 队列和链上状态。

#### 需要先检查的现有文件 / 表

| 类型       | 路径 / 表                                                                                            |
| ---------- | ---------------------------------------------------------------------------------------------------- |
| API        | `api/wallet/sync-nfts.ts`                                                                            |
| Server     | `packages/server/src/ton/nft.ts`                                                                     |
| DB         | `onchain.wallet_sync_jobs`、`onchain.wallet_nft_snapshots`、`onchain.nft_items`、`core.user_wallets` |
| Validation | `WalletNftSyncBodySchema`、`WalletNftSyncQuerySchema`                                                |

#### 需要新增或修改的内容

| 文件 / 表                        | 责任                             |
| -------------------------------- | -------------------------------- |
| `api/wallet/sync-nfts.ts`        | 创建同步任务或执行轻量同步。     |
| `api/wallet/nfts.ts`（建议新增） | 查询当前钱包 NFT 快照。          |
| `onchain.wallet_sync_jobs`       | 记录同步 job、状态、错误、结果。 |
| `onchain.wallet_nft_snapshots`   | 保存链上 NFT 快照。              |
| `core.user_wallets.last_sync_at` | 同步成功后更新时间。             |

#### 后端开发工作

同步流程：

1. 校验 session。
2. 校验钱包 verified。
3. 创建 `wallet_sync_jobs`。
4. 调用 TON API 查询钱包 NFT。
5. 过滤当前游戏 Collection。
6. 写入 `wallet_nft_snapshots`。
7. 更新 `onchain.nft_items.owner_address`、`last_seen_at`。
8. 更新 `core.user_wallets.last_sync_at`。
9. 返回同步数量和状态。

#### 数据库开发工作

`wallet_sync_jobs` 增加 idempotency key、retry 字段。
`snapshots` 需要 `wallet_id + item_address + seen_at` 索引。
对已知 collection 的 NFT，要能关联到 `onchain.nft_items`。
如果发现链上 owner 与数据库 owner 不一致，需要记录 risk event 或 sync event。

#### 前端开发工作

新增：

| 组件 / Hook         | 责任                              |
| ------------------- | --------------------------------- |
| `useSyncWalletNfts` | 触发同步。                        |
| `useWalletNfts`     | 查询链上 NFT。                    |
| `WalletSyncPanel`   | 显示同步中、成功、失败。          |
| `MintQueueSheet`    | 同时显示 Mint 队列和已 Mint NFT。 |

#### 安全和幂等要求

前端不能提交 NFT 快照。
链上数据只作为外部状态，不直接改变游戏内资产归属，除非产品规则明确。
同步任务要限流。
同一钱包短时间重复同步应返回最近 job 或排队。

#### 测试要求

未验证钱包同步失败。
同步空钱包。
同步包含非游戏 Collection NFT。
同步包含游戏 NFT。
TON API 失败。
重复同步。
链上 owner 变化。

#### 验收标准

- [ ] 用户可以手动同步链上 NFT。
- [ ] 同步任务写入 `wallet_sync_jobs`。
- [ ] NFT 快照写入 `wallet_nft_snapshots`。
- [ ] 游戏 Collection NFT 可以关联 `onchain.nft_items`。
- [ ] 同步失败可重试。
- [ ] 前端显示同步状态。

#### 完成后产物

- 实现 NFT sync API。
- 实现 wallet NFT query API。
- 新增链上同步前端组件。
- 新增 NFT sync 测试。

---

### 4.15 第 15 步：后台支付与链上运营管理

#### 目标

提供最小可用后台能力，让运营能查看支付异常、webhook 事件、退款 / dispute、Mint 队列、链上交易、用户钱包，并执行安全的重试 / 暂停 / 人工处理。

#### 需要先检查的现有文件 / 表

| 类型      | 路径 / 表                                                                                        |
| --------- | ------------------------------------------------------------------------------------------------ |
| Admin app | `apps/admin/package.json`                                                                        |
| Admin API | `api/admin/payments.ts`                                                                          |
| DB        | `ops.admin_users`、`admin_roles`、`admin_audit_logs`、`feature_flags`、`payments.*`、`onchain.*` |
| Server    | `api/_shared/requireSession.ts`、admin auth 需要确认                                             |

#### 需要新增或修改的内容

| 文件 / 表                                            | 责任                              |
| ---------------------------------------------------- | --------------------------------- |
| `apps/admin/src/pages/PaymentsPage.tsx`（建议新增）  | 支付订单、流水、webhook、异常。   |
| `apps/admin/src/pages/MintQueuePage.tsx`（建议新增） | Mint 队列、失败、重试、人工处理。 |
| `apps/admin/src/pages/WalletsPage.tsx`（建议新增）   | 用户钱包、proof、同步记录。       |
| `api/admin/payments.ts`                              | 支付列表和异常查询。              |
| `api/admin/mint-queue.ts`（建议新增）                | Mint 队列列表。                   |
| `api/admin/retry-mint.ts`（建议新增）                | 重试 Mint。                       |
| `api/admin/feature-flags.ts`（建议新增）             | 暂停支付 / Mint。                 |
| `ops.admin_audit_logs`                               | 所有操作记录。                    |

#### 后端开发工作

实现 admin 鉴权。
所有后台写操作必须记录 `admin_audit_logs`。
退款、补发、重试、暂停支付、暂停 Mint 都必须有 reason。
后台不能直接改 ledger，补偿必须走 RPC。

#### 数据库开发工作

确保 admin 用户和角色权限可用。
增加支付查询索引：status、created_at、user_id、invoice_payload。
增加 Mint 查询索引：status、created_at、attempt_count、next_attempt_at。
风险事件写入 `ops.risk_events`。

#### 前端开发工作

后台最小页面：

- 支付订单列表。
- 支付详情。
- webhook 事件列表。
- 支付异常列表。
- dispute / refund 列表。
- Mint 队列列表。
- Mint 失败详情。
- 手动重试。
- 暂停支付 / 暂停 Mint 开关。

#### 安全和幂等要求

后台 API 必须 admin 鉴权。
高风险操作二次确认。
所有后台操作写 audit。
重试操作必须幂等。
不允许后台绕过 RPC 直接发货。

#### 测试要求

非管理员访问失败。
管理员查询支付。
管理员重试 failed fulfillment。
管理员重试 failed Mint。
暂停支付后用户不能新建支付。
暂停 Mint 后用户不能新建 Mint。
audit log 完整记录。

#### 验收标准

- [ ] 后台可以查看支付订单。
- [ ] 后台可以查看 webhook 事件。
- [ ] 后台可以查看异常支付。
- [ ] 后台可以查看 Mint 队列。
- [ ] 后台可以重试 Mint。
- [ ] 后台可以暂停支付。
- [ ] 后台可以暂停 Mint。
- [ ] 所有后台写操作进入 audit log。

#### 完成后产物

- 新增 admin 页面。
- 新增 admin API。
- 新增 admin audit 测试。
- 新增运营手册。

---

### 4.16 第 16 步：测试、监控、预发布、上线与回滚

#### 目标

第五阶段上线前完成支付、钱包、Mint、链上同步的完整测试、监控、部署和故障回滚方案。

#### 需要先检查的现有文件 / 表

| 类型 | 路径 / 表                                                                            |
| ---- | ------------------------------------------------------------------------------------ |
| 测试 | `tests/api/`、`supabase/tests/`                                                      |
| 配置 | `vercel.json`、`.env.example`                                                        |
| Logs | Vercel logs、Supabase logs                                                           |
| DB   | `ops.risk_events`、`economy.reconciliation_runs`、`payments.telegram_webhook_events` |

#### 需要新增或修改的内容

| 内容      | 责任                                                  |
| --------- | ----------------------------------------------------- |
| 支付测试  | webhook、订单、发货、重复 webhook、异常订单。         |
| 钱包测试  | proof、断开、多钱包、重放。                           |
| Mint 测试 | 入队、worker、失败、重试、人工处理。                  |
| E2E       | 开盒支付、钱包连接、Mint 状态、刷新恢复。             |
| 监控      | 支付失败率、发货失败率、webhook 延迟、Mint 卡住数量。 |
| 回滚开关  | 支付暂停、webhook 发货暂停、Mint 暂停、market 暂停。  |

#### 后端开发工作

补齐日志字段：requestId、userId、starOrderId、drawOrderId、paymentChargeId、mintQueueId、txHash。
异常写入 `ops.risk_events` 或对应 dispute / failed 状态。
对账任务检查：

- paid 但未 fulfilled。
- fulfilled 但无 draw_results。
- draw_results 与 item_instances 不一致。
- ledger 与 balance 不一致。
- mint_queue minted 但 nft_items 缺失。
- onchain tx confirmed 但 mint_queue 未 minted。

#### 数据库开发工作

新增 reconciliation run 类型：

- payment_fulfillment
- mint_queue
- wallet_sync
- ledger_balance

或者复用现有 `economy.reconciliation_runs.run_type`，按当前 check 约束决定是否扩展。

#### 前端开发工作

E2E 场景：

- 创建支付订单。
- 支付等待。
- 成功后展示结果。
- 连接钱包。
- 验证钱包。
- 创建 Mint。
- 查看 Mint 状态。
- 刷新恢复状态。

#### 安全和幂等要求

所有回滚只关闭新请求，不删除历史数据。
已支付订单必须仍能补发。
已入队 Mint 必须有明确处理策略，不能静默丢弃。
回滚脚本不能直接修改 ledger。

#### 测试要求

见第 12 章完整测试计划。

#### 验收标准

- [ ] 支付全链路测试通过。
- [ ] 钱包 proof 测试通过。
- [ ] Mint 队列测试通过。
- [ ] 前端 E2E 通过。
- [ ] 监控指标可见。
- [ ] 支付暂停开关有效。
- [ ] Mint 暂停开关有效。
- [ ] 回滚流程已演练。

#### 完成后产物

- 新增 API / DB / E2E 测试。
- 新增监控指标文档。
- 新增上线 checklist。
- 新增回滚预案。

---

## 5. 数据库设计指南

### 5.1 payments 相关

| 表名                               | 是否已有 | 作用                                                          | 关键字段                                                                                                                                                    | 关键约束                                                                                           | 需要的索引                                                                                                 | RLS 策略建议                                                              |
| ---------------------------------- | -------- | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `payments.star_orders`             | 已有     | 应用侧 Stars 订单，绑定业务订单。                             | `user_id`、`business_type`、`business_id`、`status`、`xtr_amount`、`telegram_invoice_payload`、`idempotency_key`、`expires_at`、`paid_at`、`fulfilled_at`。 | `telegram_invoice_payload` unique；`idempotency_key` unique；状态 check 需扩展到第五阶段完整状态。 | `user_id + created_at`、`status + created_at`、`business_type + business_id`、`telegram_invoice_payload`。 | 用户只可读自己的脱敏订单；写操作仅 RPC / service role；admin 走后台 API。 |
| `payments.star_invoices`           | 已有     | 记录 Telegram invoice 创建请求和响应。                        | `star_order_id`、`invoice_link`、`payload`、`status`、`raw_request`、`raw_response`。                                                                       | `payload` 应唯一或与 `star_order_id` 唯一绑定。                                                    | `star_order_id`、`payload`、`status + created_at`。                                                        | 用户可读自己的 invoice 简要状态；raw request/response 仅后台。            |
| `payments.star_payments`           | 已有     | successful_payment 支付流水。                                 | `star_order_id`、`user_id`、`telegram_payment_charge_id`、`xtr_amount`、`currency`、`invoice_payload`、`raw_update`、`paid_at`。                            | `telegram_payment_charge_id` unique；金额必须大于 0；currency 默认 XTR。                           | `star_order_id`、`user_id + paid_at`、`invoice_payload`。                                                  | 用户可读自己的脱敏 payment；raw_update 仅后台。                           |
| `payments.telegram_webhook_events` | 已有     | webhook 原始事件审计、重放保护、处理状态。                    | `update_id`、`event_type`、`invoice_payload`、`payload`、`process_status`、`processed_at`、`error_message`。                                                | `update_id` unique；process_status check。                                                         | `event_type + created_at`、`process_status + created_at`、`invoice_payload`。                              | 不开放前端直读；admin 后台查询。                                          |
| `payments.star_refunds`            | 已有     | Stars 退款记录；用户要求的 `refunds` 建议映射到此表，不另建。 | `star_payment_id`、`star_order_id`、`telegram_payment_charge_id`、`xtr_amount`、`status`、`reason`、`raw_response`。                                        | refund 状态 check；关联 payment / order。                                                          | `star_order_id`、`star_payment_id`、`status + created_at`。                                                | 用户可读自己的退款状态；创建和处理仅 admin / RPC。                        |
| `payments.payment_disputes`        | 已有     | 支付争议 / 客服处理。                                         | `user_id`、`star_order_id`、`star_payment_id`、`status`、`subject`、`message`、`resolution`。                                                               | dispute 状态 check。                                                                               | `user_id + created_at`、`status + created_at`、`star_order_id`。                                           | 用户可读 / 创建自己的 dispute；处理字段仅 admin。                         |
| `ops.idempotency_keys`             | 已有     | 跨 API 幂等记录。                                             | `key`、`user_id`、`scope`、`request_hash`、`response`、`status`、`locked_until`。                                                                           | `key` primary key；request_hash 冲突拒绝。                                                         | `user_id + scope`、`status + locked_until`。                                                               | 仅后端访问，前端不直连。                                                  |

### 5.2 wallet 相关

| 表名                           | 是否已有 | 作用                                                                        | 关键字段                                                                                                                                         | 关键约束                                                                            | 需要的索引                                                     | RLS 策略建议                                            |
| ------------------------------ | -------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------- |
| `core.user_wallets`            | 已有     | 保存 TON 钱包公开地址和 verified 状态。                                     | `user_id`、`network`、`address`、`address_raw`、`wallet_app_name`、`is_primary`、`status`、`verified_at`、`disconnected_at`、`last_sync_at`。    | `chain='TON'`；network check；建议 `user_id + network + address` 唯一；主钱包唯一。 | `user_id + status`、`address`、`verified_at`、`last_sync_at`。 | 用户只可读自己的钱包；写操作必须后端 proof 验证后执行。 |
| `core.wallet_proofs`           | 已有     | ton_proof challenge、签名、验证结果、防重放。                               | `user_id`、`wallet_id`、`challenge`、`address`、`domain`、`payload`、`proof_signature`、`status`、`expires_at`、`verified_at`、`error_message`。 | `challenge` unique；proof 过期不可用；建议增加 `proof_hash` unique。                | `user_id`、`wallet_id`、`status + expires_at`、`address`。     | 不建议前端直读原始 proof；用户只看验证状态。            |
| `core.wallet_connect_sessions` | 建议新增 | 管理连接挑战生命周期，避免 `wallet_proofs` 同时承担 session 与 proof 结果。 | `user_id`、`challenge`、`status`、`expires_at`、`used_at`、`created_at`、`metadata`。                                                            | `challenge` unique；used once。                                                     | `user_id + status`、`expires_at`。                             | 仅后端访问；前端只拿 challenge。                        |

### 5.3 onchain 相关

| 表名                           | 是否已有 | 作用                                      | 关键字段                                                                                                                                                                                           | 关键约束                                                                     | 需要的索引                                                                               | RLS 策略建议                                             |
| ------------------------------ | -------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `onchain.nft_collections`      | 已有     | TON NFT Collection 配置。                 | `code`、`network`、`collection_address`、`owner_address`、`standard`、`metadata_url`、`content_base_url`、`status`。                                                                               | `code` unique；`collection_address` unique；network check。                  | `status + network`、`collection_address`。                                               | 前端可读 active collection 的脱敏 view；写仅 admin。     |
| `onchain.nft_items`            | 已有     | 链上 NFT Item 与游戏 item_instance 绑定。 | `collection_id`、`item_instance_id`、`template_id`、`form_id`、`item_index`、`item_address`、`owner_address`、`owner_user_id`、`metadata_url`、`status`、`minted_tx_hash`。                        | `item_instance_id` unique；`item_address` unique。                           | `owner_user_id`、`owner_address`、`collection_id + item_index`、`status`。               | 用户可读自己的 NFT；公开 NFT 可用只读 view；写仅 RPC。   |
| `onchain.mint_queue`           | 已有     | Mint 异步队列。                           | `user_id`、`wallet_id`、`collection_id`、`item_instance_id`、`template_id`、`form_id`、`status`、`attempt_count`、`max_attempts`、`next_attempt_at`、`nft_item_id`、`tx_hash`、`idempotency_key`。 | `idempotency_key` unique；同一 item active queue 唯一；status check 需扩展。 | `user_id + created_at`、`status + next_attempt_at`、`item_instance_id`、`tx_hash`。      | 用户只读自己的队列；创建必须通过 API/RPC；admin 可管理。 |
| `onchain.transactions`         | 已有     | 链上交易状态。                            | `network`、`tx_hash`、`query_id`、`user_id`、`wallet_id`、`related_type`、`related_id`、`direction`、`status`、`payload`、`error_message`、`submitted_at`、`confirmed_at`。                        | `tx_hash` unique；status check；direction check。                            | `status + created_at`、`related_type + related_id`、`user_id + created_at`、`query_id`。 | 用户只读自己的交易摘要；raw payload 仅后台。             |
| `onchain.wallet_sync_jobs`     | 已有     | 钱包 NFT / transaction 同步任务。         | `user_id`、`wallet_id`、`status`、`sync_type`、`started_at`、`finished_at`、`error_message`、`result`。                                                                                            | status check；sync_type check；建议 idempotency key。                        | `user_id + created_at`、`wallet_id + status`、`status + created_at`。                    | 用户只读自己的同步状态；写仅 API。                       |
| `onchain.wallet_nft_snapshots` | 已有     | 钱包 NFT 快照。                           | `wallet_id`、`user_id`、`collection_address`、`item_address`、`owner_address`、`metadata_url`、`raw_payload`、`seen_at`。                                                                          | 建议 `wallet_id + item_address + seen_at` 或最新快照唯一策略。               | `wallet_id + seen_at`、`item_address`、`collection_address`。                            | 用户可读自己的快照；raw_payload 仅后台或脱敏。           |

### 5.4 audit / ops 相关

| 表名                          | 是否已有 | 作用                            | 关键字段                                                                                                           | 关键约束                                          | 需要的索引                                                                        | RLS 策略建议          |
| ----------------------------- | -------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- | --------------------------------------------------------------------------------- | --------------------- |
| `ops.admin_audit_logs`        | 已有     | 记录后台高风险操作。            | `admin_user_id`、`action`、`target_schema`、`target_table`、`target_id`、`before_state`、`after_state`、`reason`。 | 不应允许普通用户写；后台写必须带 reason。         | `admin_user_id + created_at`、`target_table + target_id`、`action + created_at`。 | 仅 admin 访问。       |
| `ops.risk_events`             | 已有     | 记录支付、钱包、Mint 风险事件。 | `user_id`、`event_type`、`severity`、`status`、`source_type`、`source_id`、`detail`。                              | severity/status check。                           | `user_id + created_at`、`status + severity`、`source_type + source_id`。          | 仅后端 / admin 访问。 |
| `economy.reconciliation_runs` | 已有     | 对账运行记录。                  | `run_type`、`status`、`started_at`、`finished_at`、`result`、`error_message`。                                     | 当前 run_type check 可能需扩展到 payment / mint。 | `run_type + started_at`、`status + started_at`。                                  | 仅后端 / admin 访问。 |

---

## 6. API 设计指南

### 6.1 支付 API

| API 路径                                | 方法 | 责任                                                        | 调用的 RPC / 表                                                                                      | 鉴权要求                  | 幂等要求                                              | 验收标准                                                                |
| --------------------------------------- | ---: | ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------- |
| `/api/boxes/create-open-order`          | POST | 创建 Stars 开盒订单和 invoice。                             | `api.gacha_create_order`、`payments.star_orders`、`star_invoices`。                                  | App session。             | `X-Idempotency-Key` + DB unique。                     | 返回 order id、star order id、invoice payload、XTR 金额、invoice link。 |
| `/api/boxes/payment-status`（建议新增） |  GET | 查询支付 / 发货状态，不暴露敏感 raw update。                | `payments.star_orders`、`star_payments`、`gacha.draw_orders`。                                       | App session。             | 只读。                                                | 返回 pending / paid / fulfilling / fulfilled / failed / expired。       |
| `/api/boxes/result`                     |  GET | 查询开盒结果。                                              | `api.gacha_get_draw_result`。                                                                        | App session。             | 只读。                                                | fulfilled 后返回 results；未完成返回 pending。                          |
| `/api/telegram/webhook`                 | POST | Telegram webhook，处理 pre_checkout 和 successful_payment。 | `payments.telegram_webhook_events`、`star_orders`、`star_payments`、`api.gacha_process_paid_order`。 | Telegram webhook secret。 | `update_id`、`telegram_payment_charge_id`、RPC 幂等。 | 重复 webhook 不重复发货。                                               |
| `/api/payments/dispute`（建议新增）     | POST | 用户发起支付争议。                                          | `payments.payment_disputes`。                                                                        | App session。             | `idempotency_key`。                                   | 用户可提交支付异常工单。                                                |
| `/api/payments/history`（建议新增）     |  GET | 用户查看支付历史摘要。                                      | `payments.star_orders`、`star_payments`。                                                            | App session。             | 只读。                                                | 用户只能看到自己的订单。                                                |

### 6.2 钱包 API

| API 路径                             | 方法 | 责任                                         | 调用的 RPC / 表                                            | 鉴权要求      | 幂等要求                                 | 验收标准                                          |
| ------------------------------------ | ---: | -------------------------------------------- | ---------------------------------------------------------- | ------------- | ---------------------------------------- | ------------------------------------------------- |
| `/api/wallet/challenge`（建议新增）  | POST | 创建 ton_proof challenge。                   | `core.wallet_proofs` 或 `core.wallet_connect_sessions`。   | App session。 | 可按 active pending challenge 复用。     | 返回 challenge 和 expires_at。                    |
| `/api/wallet/connect`                | POST | 保存 connected_unverified 状态或连接上下文。 | `core.user_wallets`。                                      | App session。 | `idempotency_key`。                      | 地址不会被直接标记 verified。                     |
| `/api/wallet/proof`                  | POST | 验证 ton_proof，保存 verified wallet。       | `core.wallet_proofs`、`api.wallet_save_verified_address`。 | App session。 | challenge used once；proof hash 防重放。 | verified_at 写入，错误 proof 被拒绝。             |
| `/api/wallet/status`（建议新增）     |  GET | 查询钱包连接和验证状态。                     | `core.user_wallets`。                                      | App session。 | 只读。                                   | 返回 connected、verified、address、last_sync_at。 |
| `/api/wallet/disconnect`（建议新增） | POST | 断开钱包。                                   | `core.user_wallets`。                                      | App session。 | `idempotency_key`。                      | status 变为 disconnected。                        |

### 6.3 Mint / 链上 API

| API 路径                                          |       方法 | 责任                       | 调用的 RPC / 表                                                     | 鉴权要求                        | 幂等要求                                       | 验收标准                                                  |
| ------------------------------------------------- | ---------: | -------------------------- | ------------------------------------------------------------------- | ------------------------------- | ---------------------------------------------- | --------------------------------------------------------- |
| `/api/wallet/mint`                                |       POST | 创建 Mint 请求，进入队列。 | `api.wallet_enqueue_mint`、`onchain.mint_queue`。                   | App session + verified wallet。 | `idempotency_key` + active item queue unique。 | 可 Mint 藏品进入 queued。                                 |
| `/api/wallet/mint-status`                         |        GET | 查询 Mint 队列状态。       | `onchain.mint_queue`、`onchain.nft_items`、`onchain.transactions`。 | App session。                   | 只读。                                         | 返回 queued / processing / confirming / minted / failed。 |
| `/api/wallet/nfts`（建议新增）                    |        GET | 查询链上 NFT 快照。        | `onchain.wallet_nft_snapshots`、`nft_items`。                       | App session。                   | 只读。                                         | 返回用户钱包 NFT 列表。                                   |
| `/api/wallet/sync-nfts`                           |       POST | 手动同步链上 NFT。         | `onchain.wallet_sync_jobs`、`wallet_nft_snapshots`。                | App session + verified wallet。 | 短时间复用 job 或 idempotency key。            | 创建同步 job 并返回状态。                                 |
| `/api/wallet/transactions`（建议新增）            |        GET | 查询链上交易状态。         | `onchain.transactions`。                                            | App session。                   | 只读。                                         | 用户只能查看自己的交易摘要。                              |
| `/api/cron/retry-mint-queue`                      | POST / GET | Mint worker 定时处理队列。 | `onchain.mint_queue`、`transactions`、`onchain_mark_*` RPC。        | Cron secret。                   | worker claim lock。                            | 不重复提交链上交易。                                      |
| `/api/cron/sync-onchain-transactions`（建议新增） | POST / GET | 同步链上交易确认状态。     | `onchain.transactions`、`mint_queue`。                              | Cron secret。                   | tx hash / query id 幂等。                      | pending 交易最终确认或失败。                              |

### 6.4 后台 API

| API 路径                                      |        方法 | 责任                                    | 调用的 RPC / 表                                | 鉴权要求                              | 幂等要求                             | 验收标准                               |
| --------------------------------------------- | ----------: | --------------------------------------- | ---------------------------------------------- | ------------------------------------- | ------------------------------------ | -------------------------------------- |
| `/api/admin/payments`                         |         GET | 支付订单列表。                          | `payments.star_orders`、`star_payments`。      | Admin session。                       | 只读。                               | 可按状态、用户、时间过滤。             |
| `/api/admin/payment-events`（建议新增）       |         GET | webhook 事件列表。                      | `payments.telegram_webhook_events`。           | Admin session。                       | 只读。                               | 可查 failed / ignored / processed。    |
| `/api/admin/payment-disputes`（建议新增）     |  GET / POST | dispute 列表与处理。                    | `payments.payment_disputes`、`star_refunds`。  | Admin session + permission。          | 处理操作带 idempotency key。         | dispute 处理写 audit。                 |
| `/api/admin/retry-fulfillment`（建议新增）    |        POST | 重试 paid 但未 fulfilled 的订单。       | `api.gacha_process_paid_order`。               | Admin session + permission。          | star_order_id + action idempotency。 | 不重复发货。                           |
| `/api/admin/mint-queue`（建议新增）           |         GET | Mint 队列列表。                         | `onchain.mint_queue`。                         | Admin session。                       | 只读。                               | 可查 queued / failed / manual_review。 |
| `/api/admin/retry-mint`（建议新增）           |        POST | 手动重试 Mint。                         | `onchain.mint_queue`、`ops.admin_audit_logs`。 | Admin session + permission。          | mint_queue_id + idempotency key。    | 状态进入 retrying / queued。           |
| `/api/admin/onchain-transactions`（建议新增） |         GET | 链上交易查询。                          | `onchain.transactions`。                       | Admin session。                       | 只读。                               | 可按 tx hash / status 查询。           |
| `/api/admin/user-wallets`（建议新增）         |         GET | 用户钱包查询。                          | `core.user_wallets`、`wallet_proofs`。         | Admin session。                       | 只读。                               | 可查地址、验证时间、同步状态。         |
| `/api/admin/feature-flags`（建议新增）        | GET / PATCH | 暂停支付、暂停 Mint、暂停 wallet sync。 | `ops.feature_flags`、`admin_audit_logs`。      | Admin session + danger confirmation。 | 操作 idempotency key。               | flag 生效并写 audit。                  |

---

## 7. 前端开发指南

| 前端模块          | 文件路径建议                                                                                 | 责任                                                                           | 依赖 API                                           | 验收标准                          |
| ----------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------- | --------------------------------- |
| 开盒支付创建      | 现有 `apps/web/src/features/box/pages/BoxPage.tsx`                                           | 创建订单后打开 Stars invoice，不再只显示等待。                                 | `/api/boxes/create-open-order`                     | 单抽 / 十连都能打开支付。         |
| 支付等待 Sheet    | 现有 `PaymentPendingSheet.tsx`                                                               | 展示支付中、取消、成功待发货、发货中、失败、过期。                             | `/api/boxes/payment-status` 或 `/api/boxes/result` | 用户清楚知道当前状态。            |
| 支付 hook         | 建议新增 `apps/web/src/features/box/hooks/useStarsPayment.ts`                                | 调用 Telegram invoice 打开、处理窗口关闭、触发状态查询。                       | create order + status API                          | 前端不把窗口关闭当成功。          |
| 订单恢复          | 建议新增 `apps/web/src/features/box/hooks/usePendingDrawOrder.ts`                            | 刷新后恢复最近 pending order。                                                 | `/api/boxes/payment-status`                        | 刷新页面仍能看到支付 / 发货状态。 |
| API endpoint      | 修改 `apps/web/src/api/endpoints.ts`                                                         | 增加 payment、wallet、mint、onchain endpoint。                                 | 所有第五阶段 API                                   | endpoint 常量完整。               |
| TON Provider      | 建议新增 `apps/web/src/app/providers/TonConnectProvider.tsx` 并接入 `AppProviders`           | 全局 TON Connect 上下文。                                                      | TON Connect manifest                               | App 启动可初始化钱包连接。        |
| 钱包入口          | 修改 `apps/web/src/features/assets/components/WalletEntryButton.tsx`                         | 从占位 toast 改为连接 / 状态入口。                                             | `/api/wallet/status`                               | 未连接、已连接、已验证状态正确。  |
| Wallet API client | 建议新增 `apps/web/src/features/wallet/wallet.api.ts`                                        | 钱包 status、challenge、proof、disconnect、sync、mint。                        | wallet / mint API                                  | API normalize 完整。              |
| Wallet hooks      | 建议新增 `useWalletStatus`、`useWalletChallenge`、`useVerifyTonProof`、`useDisconnectWallet` | 管理钱包状态与 proof 流程。                                                    | wallet API                                         | proof 成功后 UI 显示 verified。   |
| Wallet 状态面板   | 建议新增 `features/wallet/components/WalletStatusSheet.tsx`                                  | 显示地址、验证状态、断开、同步 NFT。                                           | wallet status / sync                               | 用户可断开和同步。                |
| NFT 同步面板      | 建议新增 `features/wallet/components/WalletSyncPanel.tsx`                                    | 展示同步中、成功、失败、重试。                                                 | `/api/wallet/sync-nfts`、`/api/wallet/nfts`        | 同步状态可见。                    |
| Mint 按钮         | 修改藏品详情组件，建议新增 `features/collection/components/MintButton.tsx`                   | 对可 Mint 藏品创建 Mint 请求。                                                 | `/api/wallet/mint`                                 | 只对 eligible item 显示。         |
| Mint 队列 Sheet   | 建议新增 `features/wallet/components/MintQueueSheet.tsx`                                     | 展示 queued、processing、confirming、minted、failed、retrying、manual_review。 | `/api/wallet/mint-status`                          | 刷新后状态恢复。                  |
| 全局反馈          | 复用现有 `FeedbackProvider`                                                                  | Toast / Modal / Reward sheet 统一反馈。                                        | 全局                                               | 支付、钱包、Mint 错误统一展示。   |

支付状态展示规则：

| 状态                      | 前端文案                                 |
| ------------------------- | ---------------------------------------- |
| created / invoice_created | 支付订单已创建。                         |
| precheckout_checked       | Telegram 正在校验支付。                  |
| paid                      | 支付已成功，等待发货。                   |
| fulfilling                | 发货处理中。                             |
| fulfilled                 | 开盒完成。                               |
| failed                    | 支付或发货异常，稍后自动补发或联系客服。 |
| expired                   | 订单已过期，请重新开盒。                 |
| refunded                  | 已退款。                                 |
| disputed                  | 订单争议处理中。                         |

Mint 状态展示规则：

| 状态          | 前端文案         |
| ------------- | ---------------- |
| queued        | Mint 排队中。    |
| processing    | 正在处理 Mint。  |
| submitted     | 交易已提交链上。 |
| confirming    | 等待链上确认。   |
| minted        | Mint 成功。      |
| failed        | Mint 失败。      |
| retrying      | 正在重试。       |
| manual_review | 需要人工处理。   |
| cancelled     | 已取消。         |

---

## 8. Telegram Stars 支付全流程

Telegram Stars 数字商品支付应使用 `XTR`。官方流程是创建 invoice，等待 `pre_checkout_query`，调用确认接口，然后等待 `successful_payment`，保存 `telegram_payment_charge_id`，最后交付数字商品；不能在 `pre_checkout_query` 阶段发货。([Telegram][1])

### 8.1 完整流程

1. 用户点击单抽 / 十连。
2. 前端调用 `/api/boxes/create-open-order`。
3. 后端校验 session、盲盒、价格、库存、活动状态。
4. 后端调用 `api.gacha_create_order` 创建 `draw_order` 和 `star_order`。
5. 后端生成 invoice payload。
6. 后端调用 Telegram invoice 创建能力。
7. 后端写入 `payments.star_invoices`。
8. 前端打开 Telegram Stars 支付。
9. Telegram 发送 `pre_checkout_query` 到 `/api/telegram/webhook`。
10. webhook 先写入 `payments.telegram_webhook_events`。
11. 后端校验订单是否存在。
12. 后端校验金额是否一致。
13. 后端校验状态是否可支付。
14. 后端校验盲盒是否可用。
15. 后端校验库存是否可用。
16. 后端返回 pre_checkout 确认。
17. Telegram 发送 `successful_payment`。
18. webhook 事件先落库。
19. 后端根据 `telegram_payment_charge_id` 做唯一幂等。
20. 后端写入 `payments.star_payments`。
21. 后端更新 `star_orders.status='paid'`。
22. 后端调用数据库事务 `api.gacha_process_paid_order`。
23. RPC 创建 `draw_results`。
24. RPC 创建 `inventory.item_instances`。
25. RPC 更新 `gacha.user_pity_states`。
26. RPC 返还 100 K-coin。
27. RPC 写入 `economy.currency_ledger`。
28. RPC 更新 `draw_orders.status='completed'`。
29. RPC 更新 `star_orders.status='fulfilled'`。
30. 前端轮询或查询订单结果。
31. 前端展示抽卡结果和资产变化。

### 8.2 支付状态机

| 状态                  | 说明                                      | 可转移到                                   | 触发条件                                        |
| --------------------- | ----------------------------------------- | ------------------------------------------ | ----------------------------------------------- |
| `created`             | 内部支付订单已创建。                      | `invoice_created`、`expired`、`failed`     | `gacha_create_order` 成功。                     |
| `invoice_created`     | Telegram invoice 已创建。                 | `precheckout_checked`、`expired`、`failed` | invoice 创建成功。                              |
| `precheckout_checked` | Telegram pre_checkout 已校验通过。        | `paid`、`expired`、`failed`                | 收到并确认 pre_checkout_query。                 |
| `paid`                | 收到 successful_payment，支付流水已落库。 | `fulfilling`、`disputed`、`refunded`       | `star_payments` 插入成功。                      |
| `fulfilling`          | 正在执行开盒发货事务。                    | `fulfilled`、`failed`                      | webhook 调用发货 RPC。                          |
| `fulfilled`           | 发货完成。                                | `refunded`、`disputed`                     | draw_results、item_instances、ledger 全部完成。 |
| `failed`              | 支付创建、校验或发货失败。                | `fulfilling`、`refunded`、`disputed`       | 可重试失败或不可重试错误。                      |
| `expired`             | 用户未支付且订单过期。                    | 无，或重新创建订单                         | 超过 expires_at。                               |
| `refunded`            | 已退款。                                  | `disputed`                                 | 后台退款处理成功。                              |
| `disputed`            | 用户或平台争议处理中。                    | `refunded`、`fulfilled`、`failed`          | 用户提交 dispute 或后台标记。                   |

当前数据库 `payments.star_orders` 已有部分状态，但没有完整覆盖 `fulfilling`、`disputed`，且当前 precheckout 状态名为 `precheckout_ok`。建议在 migration 中统一到上述状态，或在 API 层做兼容映射。

---

## 9. TON Connect 与钱包验证全流程

### 9.1 完整流程

1. 前端显示钱包连接按钮。
2. 用户通过 TON Connect 连接钱包。
3. 前端拿到公开地址。
4. 前端请求 `/api/wallet/challenge`。
5. 后端创建 challenge，并记录到 `core.wallet_proofs` 或 `core.wallet_connect_sessions`。
6. 前端请求钱包签名 / ton_proof。
7. 前端提交 account + proof 到 `/api/wallet/proof`。
8. 后端验证 challenge、domain、timestamp、address、signature、防重放。
9. 后端调用 `api.wallet_save_verified_address`。
10. 后端保存 `core.user_wallets`。
11. 后端更新 `verified_at`。
12. 前端显示已连接 / 已验证。
13. 用户可以断开连接。
14. 后端记录 `disconnected_at` 和 status。
15. verified wallet 用于 Mint 和链上 NFT 同步。

### 9.2 钱包状态机

| 状态                   | 说明                                   | 可转移到                                                     | 触发条件                  |
| ---------------------- | -------------------------------------- | ------------------------------------------------------------ | ------------------------- |
| `not_connected`        | 用户未连接钱包。                       | `connecting`                                                 | 点击连接钱包。            |
| `connecting`           | TON Connect 连接中。                   | `connected_unverified`、`not_connected`                      | 用户确认或取消。          |
| `connected_unverified` | 前端拿到公开地址，但后端未验证 proof。 | `verified`、`invalid_proof`、`expired_proof`、`disconnected` | 提交 proof。              |
| `verified`             | 后端验证 proof 成功。                  | `disconnected`、`expired_proof`                              | 保存 wallet verified_at。 |
| `disconnected`         | 用户断开钱包。                         | `connecting`、`not_connected`                                | 用户点击断开。            |
| `invalid_proof`        | proof 签名、domain、address 校验失败。 | `connected_unverified`、`disconnected`                       | 用户重新请求 proof。      |
| `expired_proof`        | challenge 或 proof 已过期。            | `connected_unverified`、`disconnected`                       | 重新生成 challenge。      |

数据库当前 `core.user_wallets.status` 只包含 `connected`、`disconnected`、`revoked`，可以用 `verified_at` 派生 `connected_unverified` / `verified`，减少 migration；如果要直接持久化这些状态，需要扩展 check constraint。

---

## 10. NFT Mint 队列全流程

### 10.1 完整流程

1. 用户选择可 Mint 的藏品。
2. 前端调用 `/api/wallet/mint`。
3. 后端校验钱包已 verified。
4. 后端校验藏品属于用户。
5. 后端校验藏品未挂售、未分解、未锁定、未 Mint。
6. 后端校验 Collection active。
7. 后端锁定藏品。
8. 后端创建 `onchain.mint_queue`。
9. 后端写入 `inventory.item_instance_events`。
10. 前端显示 queued。
11. 后台 worker / cron 拉取队列。
12. worker 设置 processing。
13. worker 生成 metadata snapshot。
14. worker 提交链上 Mint 交易。
15. worker 写入 `onchain.transactions`。
16. worker 设置 submitted / confirming。
17. 交易同步任务查询链上交易状态。
18. Mint 成功后调用 `api.onchain_mark_mint_success`。
19. 创建 / 更新 `onchain.nft_items`。
20. 更新 `inventory.item_instances.nft_mint_status='minted'`。
21. 绑定 NFT item address。
22. Mint 失败时调用 `api.onchain_mark_mint_failed`。
23. 失败可重试，超过次数进入 manual_review。
24. 用户前端可以查看 Mint 状态。

### 10.2 Mint 状态机

| 状态            | 说明                              | 可转移到                                 | 触发条件                   |
| --------------- | --------------------------------- | ---------------------------------------- | -------------------------- |
| `queued`        | Mint 请求已入队。                 | `processing`、`cancelled`                | 用户创建 Mint 请求。       |
| `processing`    | worker 已领取并准备提交链上交易。 | `submitted`、`failed`、`retrying`        | worker 加锁处理。          |
| `submitted`     | 链上交易已提交。                  | `confirming`、`failed`                   | 获得 tx hash / query id。  |
| `confirming`    | 等待链上确认。                    | `minted`、`failed`、`retrying`           | 交易查询中。               |
| `minted`        | Mint 成功，NFT 地址已绑定。       | 无                                       | 链上确认成功。             |
| `failed`        | Mint 失败。                       | `retrying`、`manual_review`、`cancelled` | 不可立即确认或链上失败。   |
| `retrying`      | 等待下一次重试。                  | `processing`、`manual_review`            | next_attempt_at 到达。     |
| `manual_review` | 超过重试或异常状态，需人工处理。  | `retrying`、`cancelled`、`minted`        | 后台人工操作。             |
| `cancelled`     | Mint 被取消。                     | 无                                       | 仅在未提交链上交易前允许。 |

当前 `onchain.mint_queue.status` 只有 `queued`、`processing`、`minted`、`failed`、`cancelled`，需要扩展以支持 `submitted`、`confirming`、`retrying`、`manual_review`。

---

## 11. 测试计划

| 测试类型                   | 测试目标            | 覆盖场景                                                                                         | 验收标准                         |
| -------------------------- | ------------------- | ------------------------------------------------------------------------------------------------ | -------------------------------- |
| 支付 API 单元测试          | 创建订单逻辑正确。  | 单抽、十连、价格变化、库存不足、盲盒暂停、重复 idempotency key。                                 | 不产生重复订单；金额以后端为准。 |
| Telegram pre_checkout 测试 | 校验支付前置条件。  | 有效订单、订单不存在、金额不一致、currency 错误、订单过期、库存不足。                            | 只确认合法订单；非法订单拒绝。   |
| successful_payment 测试    | 支付流水落库。      | 正常 payment、重复 charge id、重复 update_id、payload 不存在、金额不一致。                       | 重复 webhook 不重复发货。        |
| 支付成功但发货失败测试     | 可恢复。            | `star_payments` 已写入，`gacha_process_paid_order` 失败。                                        | 后台可重试；用户不会重复支付。   |
| 开盒发货 DB 测试           | 资产与库存一致。    | draw_order 和 payment 一致、draw_results 数量、item_instances、K-coin 返还、pity state、ledger。 | ledger、库存、结果可对账。       |
| 钱包连接测试               | 连接状态正确。      | 用户连接钱包、取消连接、刷新恢复。                                                               | UI 状态来自后端。                |
| ton_proof 测试             | 验证安全。          | 有效 proof、无效 proof、过期 proof、重放 proof、domain 错误、address 错误。                      | 只有有效 proof 可保存 verified。 |
| 钱包断开测试               | 断开状态正确。      | 已验证钱包断开、重复断开、多钱包策略。                                                           | status 与 disconnected_at 正确。 |
| Mint 入队测试              | 资产锁定正确。      | 未连接钱包、未验证钱包、非本人藏品、挂售中、分解、重复 Mint。                                    | 只有合法藏品可入队。             |
| Mint worker 测试           | 队列处理正确。      | queued、processing、submitted、confirming、minted、failed、retrying、manual_review。             | 不重复提交链上交易。             |
| 链上交易同步测试           | 交易状态恢复。      | pending confirmed、failed、TON API 不可用、超时。                                                | 状态可重试、可人工处理。         |
| NFT 同步测试               | 钱包 NFT 快照正确。 | 空钱包、非游戏 Collection、游戏 NFT、owner 变化。                                                | 快照写入并可查询。               |
| Admin 测试                 | 运营安全。          | 非管理员拒绝、管理员查询、重试、暂停支付、暂停 Mint。                                            | 所有后台写操作写 audit。         |
| 前端 E2E                   | 用户主流程。        | 开盒支付成功展示结果、钱包连接、Mint 请求、Mint 状态、刷新恢复。                                 | 用户端状态与服务端一致。         |
| 回归测试                   | 第四阶段不回退。    | 任务、邀请、签到、分红、市场、藏品。                                                             | 现有功能不被破坏。               |

---

## 12. 环境变量和密钥管理

当前 `.env.example` 已覆盖大部分第五阶段变量；以下表格按是否前端可见重新整理。

| 环境变量                                                          | 用途                                                       |   前端可见 | 后端可见 |            必填 | 风险说明                              |
| ----------------------------------------------------------------- | ---------------------------------------------------------- | ---------: | -------: | --------------: | ------------------------------------- |
| `TELEGRAM_BOT_TOKEN`                                              | 调用 Telegram Bot API、创建 invoice、answer pre_checkout。 |         否 |       是 |              是 | 泄漏后可控制 bot。                    |
| `TELEGRAM_WEBHOOK_SECRET`                                         | 校验 Telegram webhook 来源。                               |         否 |       是 |              是 | 泄漏后可伪造 webhook。                |
| `TELEGRAM_WEBHOOK_URL`                                            | 设置 webhook 地址。                                        |         否 |       是 |              是 | 错误会导致收不到支付回调。            |
| `TELEGRAM_STARS_CURRENCY`                                         | Stars 支付币种，固定 XTR。                                 |         否 |       是 |              是 | 不应让前端决定。                      |
| `TELEGRAM_STARS_PROVIDER_TOKEN`                                   | Stars 支付 provider token；数字商品通常为空。              |         否 |       是 | 按 Bot API 要求 | 不应暴露。                            |
| `STARS_OPEN_ORDER_EXPIRES_MINUTES`                                | 开盒支付订单有效期。                                       |         否 |       是 |              是 | 太长会增加库存锁风险。                |
| `PAYMENT_WEBHOOK_IDEMPOTENCY_TTL_SECONDS`                         | webhook 幂等 TTL。                                         |         否 |       是 |              是 | 太短会导致重复处理风险。              |
| `SUPABASE_URL`                                                    | Supabase 服务地址。                                        |   可选公开 |       是 |              是 | 前端可用 anon URL，但业务写仍走 API。 |
| `SUPABASE_ANON_KEY`                                               | Supabase anon key。                                        |   可选公开 |       是 |      视前端直读 | 只能用于 RLS 保护的公开读。           |
| `SUPABASE_SERVICE_ROLE_KEY`                                       | Supabase service role。                                    |         否 |       是 |              是 | 绝不能进入前端。                      |
| `SUPABASE_SECRET_KEY`                                             | 新版 Supabase secret key 替代 service role。               |         否 |       是 |          二选一 | 绝不能进入前端。                      |
| `APP_SESSION_SECRET`                                              | app session 签名 / 加密。                                  |         否 |       是 |              是 | 泄漏会影响登录安全。                  |
| `IDEMPOTENCY_SECRET`                                              | 幂等 key 签名或 hash。                                     |         否 |       是 |              是 | 泄漏会增加伪造请求风险。              |
| `CRON_SECRET`                                                     | Mint worker / sync cron 鉴权。                             |         否 |       是 |              是 | 泄漏会触发后台任务。                  |
| `TON_NETWORK`                                                     | mainnet / testnet。                                        | 可公开展示 |       是 |              是 | 配错会导致钱包 / Collection 不匹配。  |
| `TON_API_BASE_URL`                                                | TON API 地址。                                             |         否 |       是 |              是 | 前端不应绕过后端同步。                |
| `TON_API_KEY`                                                     | TON API key。                                              |         否 |       是 |        按供应商 | 泄漏会被滥用。                        |
| `TONCENTER_API_KEY`                                               | Toncenter API key。                                        |         否 |       是 |        按供应商 | 泄漏会被滥用。                        |
| `TONCONNECT_MANIFEST_URL`                                         | 后端记录 manifest URL。                                    |         否 |       是 |              是 | 需与前端公开 URL 一致。               |
| `VITE_TONCONNECT_MANIFEST_URL`                                    | 前端 TON Connect manifest。                                |         是 |       否 |              是 | 只放公开 manifest URL。               |
| `TON_PROOF_TTL_SECONDS`                                           | proof 有效期。                                             |         否 |       是 |              是 | 太长会增加重放风险。                  |
| `TON_PROOF_CHALLENGE_BYTES`                                       | challenge 随机长度。                                       |         否 |       是 |              是 | 不应过短。                            |
| `TON_COLLECTION_ADDRESS`                                          | NFT Collection 地址。                                      | 可公开展示 |       是 |       Mint 必填 | 配错会 Mint 到错误 Collection。       |
| `TON_NFT_COLLECTION_OWNER_ADDRESS`                                | Collection owner。                                         |     可公开 |       是 |            建议 | 用于校验合约 owner。                  |
| `TON_MINTER_WALLET_ADDRESS`                                       | 服务端 Mint 钱包地址。                                     |     可公开 |       是 |       Mint 必填 | 可公开地址，但不能公开私钥。          |
| `TON_MINTER_PRIVATE_KEY`                                          | 服务端 Mint 私钥。                                         |         否 |       是 |       Mint 必填 | 最高风险，必须放 Vercel Secret。      |
| `TON_MINT_ENABLED`                                                | 服务端 Mint 总开关。                                       |         否 |       是 |              是 | 关闭时不能新 Mint。                   |
| `TON_MINT_BATCH_SIZE`                                             | Mint worker 批量大小。                                     |         否 |       是 |              是 | 太大可能超时。                        |
| `TON_MINT_MAX_RETRIES`                                            | Mint 最大重试。                                            |         否 |       是 |              是 | 控制 manual_review。                  |
| `TON_MINT_RETRY_DELAY_SECONDS`                                    | Mint 重试间隔。                                            |         否 |       是 |              是 | 太短会放大链上失败。                  |
| `TON_MINT_CONFIRMATION_TIMEOUT_SECONDS`                           | 链上确认超时。                                             |         否 |       是 |              是 | 超时后进入 retry / review。           |
| `NFT_METADATA_BASE_URL`                                           | NFT metadata 根地址。                                      |     可公开 |       是 |              是 | 必须稳定可访问。                      |
| `NFT_COLLECTION_METADATA_URI`                                     | Collection metadata URI。                                  |     可公开 |       是 |              是 | 链上 metadata 引用。                  |
| `NFT_ITEM_METADATA_BASE_URI`                                      | Item metadata base URI。                                   |     可公开 |       是 |              是 | Mint item metadata。                  |
| `NFT_ROYALTY_FACTOR` / `NFT_ROYALTY_BASE` / `NFT_ROYALTY_ADDRESS` | royalty 配置。                                             |     可公开 |       是 |            可选 | 配错影响二级市场权益。                |
| `WALLET_SYNC_ENABLED`                                             | 钱包同步开关。                                             |         否 |       是 |              是 | 关闭时只读历史同步。                  |
| `VITE_ENABLE_TON_CONNECT`                                         | 前端钱包入口开关。                                         |         是 |       否 |              是 | 只控制 UI，不代表后端允许。           |
| `FEATURE_TON_MINT_ENABLED`                                        | 前端功能开关展示。                                         |     可公开 |       是 |              是 | 后端仍需独立校验。                    |
| `ADMIN_SESSION_SECRET`                                            | 后台 session。                                             |         否 |       是 |        后台必填 | 泄漏影响后台。                        |
| `ADMIN_BOOTSTRAP_TELEGRAM_USER_IDS`                               | 初始化超级管理员。                                         |         否 |       是 |        后台必填 | 错配会导致权限问题。                  |
| `SENTRY_DSN`                                                      | 后端错误监控。                                             |         否 |       是 |            可选 | 注意脱敏。                            |
| `VITE_SENTRY_DSN`                                                 | 前端错误监控。                                             |         是 |       否 |            可选 | 只能是前端 DSN。                      |

---

## 13. 部署与上线步骤

- [ ] 创建开发分支 `phase5-payments-onchain`。
- [ ] 冻结第四阶段基线并记录 commit。
- [ ] 确认本地 `.env`、Vercel Preview、Vercel Production 环境变量完整。
- [ ] 确认生产环境 `DEV_GACHA_PAYMENT_MODE=false`。
- [ ] 设计并 review 支付 / 钱包 / onchain migration。
- [ ] 在本地 Supabase 执行 migration 预检查。
- [ ] 运行现有 DB test。
- [ ] 生成 Supabase types，更新 `packages/db-types/src/database.types.ts`。
- [ ] 实现并测试 `/api/boxes/create-open-order` 正式 Stars invoice。
- [ ] 实现并测试 `/api/telegram/webhook`。
- [ ] 在 Telegram Bot 配置 webhook URL。
- [ ] 确认 webhook secret 与 Vercel 环境变量一致。
- [ ] 配置 TON Connect manifest。
- [ ] 确认 manifest URL 与 `VITE_TONCONNECT_MANIFEST_URL` 一致。
- [ ] 配置 NFT Collection 地址。
- [ ] 配置 NFT metadata base URI。
- [ ] 配置 TON minter 地址与私钥，仅后端可见。
- [ ] 配置 Mint worker cron secret。
- [ ] 更新 `vercel.json` 增加 Mint / onchain sync cron。
- [ ] 部署 Vercel Preview。
- [ ] 使用 Telegram 测试账号打开 Mini App。
- [ ] 使用测试账号创建 Stars 支付订单。
- [ ] 检查 invoice 是否可打开。
- [ ] 检查 pre_checkout webhook 日志。
- [ ] 检查 successful_payment webhook 日志。
- [ ] 检查 `payments.star_orders`、`star_payments`、`telegram_webhook_events`。
- [ ] 检查 `gacha.draw_orders`、`draw_results`、`inventory.item_instances`、`economy.currency_ledger`。
- [ ] 连接 TON 钱包。
- [ ] 验证 ton_proof。
- [ ] 创建 Mint 请求。
- [ ] 检查 `onchain.mint_queue`。
- [ ] 运行 Mint worker。
- [ ] 检查 `onchain.transactions`。
- [ ] 检查 Mint 成功后 NFT 地址绑定。
- [ ] 执行前端 E2E。
- [ ] 执行 API test。
- [ ] 执行 DB test。
- [ ] 检查 Supabase security advisors。
- [ ] 检查 performance advisors，确认阶段五关键索引已处理。
- [ ] 后台验证支付订单列表。
- [ ] 后台验证 Mint 队列列表。
- [ ] 演练暂停支付。
- [ ] 演练暂停 Mint。
- [ ] 合并主分支。
- [ ] 正式发布。
- [ ] 发布后 24 小时监控支付失败率、发货失败率、webhook 延迟、Mint 卡住数量。
- [ ] 出现异常时优先关闭 `FEATURE_STARS_PAYMENT_ENABLED`、`FEATURE_PAYMENT_WEBHOOK_FULFILLMENT_ENABLED`、`FEATURE_TON_MINT_ENABLED` 或 `TON_MINT_ENABLED`，不要删除订单数据。

---

## 14. 回滚和故障处理方案

| 故障场景                          | 用户影响                       | 排查位置                                                                          | 临时处理                                                   | 修复方案                                                         | 是否需要补偿                           |
| --------------------------------- | ------------------------------ | --------------------------------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------- |
| Telegram webhook 收不到           | 用户支付后无法自动发货。       | Telegram webhook 配置、Vercel logs、`telegram_webhook_events`。                   | 暂停新支付；保留订单查询。                                 | 修复 webhook URL / secret / Bot 配置后重放或人工处理 paid 订单。 | 已支付未发货需要补发。                 |
| pre_checkout_query 校验失败       | 用户无法完成支付。             | webhook logs、`star_orders.error_message`、盲盒状态、金额。                       | 提示用户重试；暂停异常盲盒。                               | 修复价格、库存、订单过期逻辑。                                   | 未支付通常不需要补偿。                 |
| successful_payment 收到但发货失败 | 用户已支付但未收到藏品。       | `star_payments`、`star_orders.status`、`draw_orders`、RPC error。                 | 暂停新支付；后台重试发货。                                 | 修复 RPC 或数据问题，调用幂等发货。                              | 是，必须补发或退款。                   |
| 重复 webhook                      | 理论无用户影响。               | `telegram_webhook_events.update_id`、`star_payments.telegram_payment_charge_id`。 | 返回成功但不重复处理。                                     | 确保 unique 约束和幂等逻辑。                                     | 否，除非已重复发货。                   |
| 用户支付成功但前端没显示结果      | 用户误以为失败。               | `/api/boxes/result`、前端 pending order、webhook 状态。                           | 提示“支付已成功，发货处理中”；提供刷新按钮。               | 修复轮询 / 状态恢复逻辑。                                        | 若已发货不需要；若未发货需要补发。     |
| 数据库事务失败                    | 订单卡在 paid / fulfilling。   | RPC logs、Supabase logs、`star_orders.error_message`。                            | 暂停发货开关，后台重试。                                   | 修复事务、索引、约束或数据异常。                                 | 视是否支付成功；通常需要。             |
| ledger 不一致                     | 余额显示错误。                 | `currency_ledger`、`user_balances`、reconciliation runs。                         | 暂停相关资产操作。                                         | 通过冲正 ledger 修复，不直接改历史 ledger。                      | 可能需要。                             |
| Mint 卡在 processing              | 用户无法完成 NFT Mint。        | `mint_queue`、worker logs、`onchain.transactions`。                               | 暂停 Mint worker；把超时任务转 retrying 或 manual_review。 | 修复 worker 锁、链上提交或状态同步。                             | 通常不涉及资产补偿，但需解除或继续锁。 |
| 链上交易失败                      | Mint 失败。                    | TON API、`onchain.transactions`、tx hash。                                        | 进入 retrying 或 manual_review。                           | 修复交易参数、gas、Collection 配置。                             | 视是否扣费 / 锁定时间决定。            |
| TON API 不可用                    | Mint / NFT sync 延迟。         | TON API provider logs、worker logs。                                              | 暂停 Mint worker 和 wallet sync。                          | 切换 provider 或延后重试。                                       | 一般不需要，除非用户长期卡住。         |
| 用户钱包 proof 验证失败           | 用户无法绑定钱包。             | `wallet_proofs.error_message`、domain、timestamp、signature。                     | 提示重新连接。                                             | 修复 manifest、domain、proof 验证逻辑。                          | 不需要。                               |
| 错误配置导致盲盒价格异常          | 用户可能支付错误金额。         | `blind_boxes`、`box_price_rules`、`star_orders`。                                 | 立即暂停支付和相关盲盒。                                   | 修复价格配置；对异常订单退款或补偿。                             | 需要逐单判断。                         |
| 需要紧急关闭支付                  | 用户不能新开盒支付。           | `ops.feature_flags`。                                                             | 关闭 `FEATURE_STARS_PAYMENT_ENABLED`。                     | 修复后重新开启；已支付订单继续补发。                             | 已支付未发货需要。                     |
| 需要紧急关闭 Mint                 | 用户不能新建 Mint。            | `ops.feature_flags`、`TON_MINT_ENABLED`。                                         | 关闭 `FEATURE_TON_MINT_ENABLED` 和 `TON_MINT_ENABLED`。    | 修复 worker / 合约 / 配置后重新开启。                            | 已入队任务需继续处理或人工取消。       |
| webhook secret 泄漏               | 存在伪造 webhook 风险。        | Vercel env、Telegram webhook 配置。                                               | 立即更换 secret，暂停 webhook fulfillment。                | 旋转 secret，检查 webhook_events 异常。                          | 若伪造导致发货，需要追回或封禁。       |
| TON minter private key 泄漏       | Collection / Mint 权限高风险。 | Vercel secrets、链上交易。                                                        | 立即暂停 Mint，迁移 minter 权限。                          | 旋转 minter，部署新权限策略。                                    | 视链上损失决定。                       |

---

## 15. 最终验收清单

### 15.1 支付验收

- [ ] Stars 订单可以创建。
- [ ] Telegram 支付可以正常打开。
- [ ] pre_checkout_query 可以正确校验。
- [ ] successful_payment 可以正确落库。
- [ ] 支付成功后只发货一次。
- [ ] 重复 webhook 不重复发货。
- [ ] 支付和 draw_order 一一对应。
- [ ] 支付失败不会发货。
- [ ] 用户刷新页面后可以查询支付结果。
- [ ] 订单过期不会继续支付。
- [ ] 金额不一致会被拒绝。
- [ ] paid 但未 fulfilled 的订单可后台重试。
- [ ] refund / dispute 状态可记录。

### 15.2 资产验收

- [ ] 藏品库存正确增加。
- [ ] K-coin 返还正确。
- [ ] ledger 正确记录。
- [ ] 余额和 ledger 可对账。
- [ ] draw_results 与 item_instances 一致。
- [ ] 保底状态正确更新。
- [ ] 邀请首次开盒奖励不重复发放。
- [ ] 好友开盒分红不包含首开盒异常场景。

### 15.3 钱包验收

- [ ] 用户可以连接 TON 钱包。
- [ ] 后端可以验证 proof。
- [ ] 钱包公开地址正确保存。
- [ ] 用户可以断开钱包。
- [ ] 前端不保存私钥。
- [ ] proof 过期会失败。
- [ ] proof 重放会失败。
- [ ] 错误 domain / signature 会失败。
- [ ] 刷新页面后钱包状态可以恢复。

### 15.4 Mint 验收

- [ ] 用户可以对可 Mint 藏品创建 Mint 请求。
- [ ] Mint 请求进入队列。
- [ ] Mint 状态可以查询。
- [ ] Mint 成功后 NFT 地址正确绑定。
- [ ] Mint 失败可以重试或进入人工处理。
- [ ] 挂售中 / 分解 / 非本人藏品不能 Mint。
- [ ] 未验证钱包不能 Mint。
- [ ] 同一 item instance 不能重复 active Mint。
- [ ] Mint worker 不会重复提交链上交易。
- [ ] 链上交易失败有错误记录。
- [ ] 超过重试次数进入 manual_review。

### 15.5 链上同步验收

- [ ] 用户可以手动同步链上 NFT。
- [ ] 同步任务进入 `wallet_sync_jobs`。
- [ ] 同步成功写入 `wallet_nft_snapshots`。
- [ ] 游戏 Collection NFT 能关联 `nft_items`。
- [ ] TON API 不可用时同步任务失败可重试。
- [ ] 钱包 `last_sync_at` 正确更新。

### 15.6 安全验收

- [ ] service role key 不出现在前端。
- [ ] Bot Token 不出现在前端。
- [ ] TON minter private key 不出现在前端。
- [ ] webhook 有 secret 校验。
- [ ] webhook 有幂等保护。
- [ ] payment charge id 有唯一约束。
- [ ] invoice payload 有唯一约束。
- [ ] RLS 没有明显漏洞。
- [ ] 所有核心资产操作有审计记录。
- [ ] 生产环境 `DEV_GACHA_PAYMENT_MODE=false`。
- [ ] 支付和 Mint 可通过 feature flag 紧急暂停。

### 15.7 运营验收

- [ ] 后台可以查看支付订单。
- [ ] 后台可以查看异常支付。
- [ ] 后台可以查看 webhook 事件。
- [ ] 后台可以查看退款和争议。
- [ ] 后台可以查看 Mint 队列。
- [ ] 后台可以重试 Mint。
- [ ] 后台可以查看链上交易。
- [ ] 后台可以查询用户钱包。
- [ ] 后台可以暂停支付。
- [ ] 后台可以暂停 Mint。
- [ ] 所有后台写操作写入 `ops.admin_audit_logs`。
- [ ] 支付、ledger、Mint 对账任务可以运行。

[1]: https://core.telegram.org/bots/payments-stars "https://core.telegram.org/bots/payments-stars"
