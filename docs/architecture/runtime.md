# 运行时

## Web

Battle 队伍选择页同时提交好友创建和公开随机匹配意图。Web 只在 `team_select` 或可接受邀请需要本人队伍时启用 `battle.team_options`；首次读取期间把该查询并入页面 loading，保留“正在读取本人可用藏品”并禁用确定动作，不能把尚未返回的空数组渲染成真实无藏品。公开匹配不新增浏览器队列运行时：Web 点击后立即锁定表单，`battle.matchmake` 的权威响应直接落到 `public_match/waiting` 或不可撤销的 `lobby_countdown`；公开 waiting 与邀请 waiting 共用 room snapshot、presence 和 2 秒 REST 回正，但按 `room_mode` 分别展示 120 秒匹配或 30 分钟分享界面。匹配成功后页面 presence 只用于回正显示，不得阻止、取消或重置 3 秒倒计时。

`apps/web` 使用 React、Vite 与 TypeScript。`app` 拥有启动、账号门禁、Provider、恢复协调、路由选择、顶部资产栏、全局弹窗和五个主导航壳层；`platform/navigation` 以浏览器 History API、单一 `popstate` 监听和 `useSyncExternalStore` 提供活动 Web 的轻量导航状态。五个主页面首次访问后在当前 session generation 内保持挂载，路由只切换 `hidden`/`inert`、本页查询参数和独立滚动位置，图鉴作为临时详情路由覆盖其上。`pages` 是跨领域 UI 的唯一组合层；`domains` 只拥有本领域 UI 与实际使用的类型；`workflows` 管理会话、操作与支付恢复；`platform` 封装 Telegram、浏览器导航、HTTP 与 React Query；`shared` 保存跨领域纯 UI。钱包、Mint 与 TON 平台文件继续保留为休眠源码，但不进入当前 Web 路由、弹窗或恢复协调器。

活动 Web 只导入 `@pokepets/api-contracts/app-client` 及其 `app-client/errors` 子边界；休眠适配器只能导入 `dormant-app`。活动 Web 不依赖 React Router，所有内部导航、查询参数、历史前进/后退、非法路径回正和 Telegram BackButton 复用 `platform/navigation`；所有共享 UI 直接导入组件叶子文件，禁止共享 UI barrel。领域 UI 不导入其他领域，库存页组合库存、进化和分解，任务页组合任务和邀请，市场页组合市场和 VIP，游戏页组合 Battle UI 与 battle-realtime workflow。首次进入 TMA 时入口、默认开盒页和首屏契约形成受构建门禁约束的同步闭包；Telegram 初始化后只预取首屏契约，轻量操作 Facade 随入口加载，重型操作运行时通常只在玩家意图或真实 operation 恢复需要后加载。默认开盒页只有在页面活动、身份与开盒数据读取完成、开盒规则完整且当前箱子主图解码完成后才发布首屏就绪信号；该信号发出后固定通过动态边界准备开盒 Runtime、开盒表现 chunk 与唯一月夜底图，既不把这些模块加入首屏同步闭包，也不请求业务数据或结果宠物图。五张稀有度徽记和三档公示概率随前端同步闭包静态提供，不读取目录快照或服务端概率。首屏失败或直接进入 `/game` 不启动自动页面预热。后台调度器仅在页面可见、浏览器在线、Telegram 未失活，并且 Network Information 明确提供 `saveData === false` 与 `effectiveType === "4g"` 时，按藏品、任务、交易、图鉴顺序在每个空闲时段逐个加载一个页面 JS/CSS；未知网络、慢速网络、省流量、离线、隐藏或 Telegram `deactivated` 均停止尚未开始的调度，已开始的单个动态 import 可以完成但不会继续下一个。Battle 页面模块永不进入该自动列表，只由玩家触摸、指针或键盘导航意图加载；玩家已经进入 Battle 后，功能内 Ably 与重特效运行时才按 ADR-047 独立准备，ADR-048 只从 realtime 的动态 preload 提示删除已经执行的应用入口 JS，真实 ESM 依赖和其余资源提示不变。页面与开盒表现准备都不挂载页面、不调用 API；所有 `React.lazy`、玩家意图和后台调度复用统一模块 Promise，失败的 Promise 会删除，自动序列停止且不重试，后续明确玩家意图仍可重试。Battle 使用 React + TypeScript、CSS 与 Web Animations API，不引入 Phaser；当前 Web 不注册 Mint 页面、不挂载钱包弹窗、不读取钱包摘要，也不初始化 TON Connect Provider。详细边界由 [ADR-040](adr/ADR-040-first-screen-runtime-boundary.md)、[ADR-043](adr/ADR-043-adaptive-page-module-warmup.md)、[ADR-046](adr/ADR-046-first-screen-direct-dependency-and-native-navigation.md)、[ADR-047](adr/ADR-047-battle-staged-runtime-loading.md) 与 [ADR-048](adr/ADR-048-battle-dynamic-preload-entry-deduplication.md) 固定。

操作注册中心不静态导入开盒、进化、分解、市场、转盘和图鉴表现。根 Provider 只发布身份稳定的 Store Context；命令通过 Runtime controller ref 委托，route 阻塞、导航锁、恢复队列和转盘 epoch 使用独立 `useSyncExternalStore` 频道，完整 operation 状态与结果不进入 Context。消费 `identity.initial.recovery.blocking_operations` 与统一发现结果的恢复 effect 通过 `useEffectEvent` 调用最新 `hydrate`，重型 Runtime 的展示状态发布不能使相同 operation snapshot 再次水合。入口恢复快照只属于当前 session generation 内存，不注册为 React Query 查询；日常用户与资产由 `identity.summary` 独立回正。对应按钮在 `pointerdown`、键盘 `focus` 和提交时复用同一加载 Promise，表现 JS/CSS 与 API 并行；表现失败只提供画面重载，不再次提交命令。开盒成功结果在仪式层后方挂载真实单抽详情图或全部十连缩略图，只有首轮周期完成且全部图片 `decode()` 成功才以同一批节点整体显现；最终图片失败只显示静态月夜重试入口，重试不调用业务 API。充值与 VIP 弹窗按打开意图动态加载。`foundation.css` 只承载 reset、变量、Startup、应用壳和跨领域基础组件；默认开盒页、普通页面、全局弹窗和六类结果舞台分别由所有者导入 CSS，`global.css` 不得恢复。Vite 保持默认 CSS code splitting，并关闭与 `es2023` WebView 基线重复的 module preload polyfill。

Telegram WebApp 在 `createRoot().render()` 前按 `ready → expand → disableVerticalSwipes → requestFullscreen` 同步初始化，不显示项目确认弹窗；内容区域下滑不能最小化或关闭主 Mini App，页面自身纵向滚动保持可用，Telegram 标题栏仍保留最小化和关闭能力。不支持垂直滑动控制的旧客户端继续使用客户端原生行为；原生全屏不可用时静默保留已展开的最大稳定视口。首帧即写入主题、稳定视口、四边设备安全区、四边内容安全区、Header/Background 和 HTML `theme-color`；运行中继续监听主题、安全区、视口和全屏事件。平台层以 `max(safeAreaInset.top, contentSafeAreaInset.top)` 作为已报告顶部值；仅 `platform` 为 `ios` 或 `android` 时，再补充 `max(0, safeAreaInset.top + 44 - 已报告顶部值)`，主壳层以两者之和统一定位唯一的全局顶部资产栏和页面内容起点。该计算保证设备安全区之后仍保留 44 CSS px 原生控件行；官方 `contentSafeAreaInset.top` 已经覆盖控件行时补充量为零，并且不依赖可能滞后或为假的 `isFullscreen`。普通浏览器与 Telegram Desktop/Web 的补充量为零。依赖可用高度的页面使用同一顶部值。主导航切换不主动失效查询，但会暂停隐藏页面观察者；服务端业务结果继续按 `refreshScopes` 标记全部匹配缓存失效，只刷新当前页面与全局活动查询。返回页面时，新鲜缓存不读取，超过 20 秒或已失效缓存按查询键回正一次；有缓存的失败保留内容并提供非阻塞重试。WebView 以前台 `activated`/后台 `deactivated` 事件计时，普通浏览器和旧客户端回退到 `visibilitychange`；重复事件只执行一次恢复。后台连续停留达到五分钟后恢复时，保留页面并静默刷新 `identity.summary`、VIP、钱包活动查询与当前路由查询；不足五分钟不请求普通页面。`identity.initial` 永不参加人工、前台、业务或页面重新激活刷新。除进化专用回执外，开盒、转盘、分解与通用 operation 结果只存在于当前前台运行期；隐藏或 `deactivated` 立即丢弃终态展示，返回时精确刷新受影响的权威状态且不恢复旧结果。全局操作恢复使用 `identity.initial.recovery.authority_cursor` 与 `GET /api/operations/recoverable` 的无结果路由标记追赶首屏之后提交的任意 operation 终态，同时发现转盘未决和进化规定状态；它绑定页面可见、Telegram 激活与在线状态，失活时中止定时器和请求，恢复时立即追赶，结果队列存在时暂停至清空。市场成交提醒是普通页面刷新规则之外的独立例外：交易页面可见时无论购买、出售或管理页签是否激活都每 10 秒同步本人当前挂售和成交事件游标，进入、重新可见或网络恢复时立即同步，隐藏或离开交易页面时停止；按内部用户 UUID 隔离的游标和未隐藏 SOLD 事件写入版本化 `localStorage`，同一待展示集合非空时直接显示“管理”红点，不保存第二份已读状态、会话或业务裁决数据。当前 Web 不下载或初始化钱包能力。访问令牌只保存在 JavaScript 运行内存中，页面重载后重新使用 Telegram `initData` 交换。

结果弹窗的“确定”“收下”或本地返回只移除 Web 内存状态，不请求 API、RPC、原操作或权威刷新。隐藏、Telegram `deactivated`、`pagehide`、刷新或重进立即丢弃旧结果和领域动画；非进化终态不进入 `blocking_operations` 的结果恢复，未决原操作只查询原 `operation_id`，取得终态后刷新权威状态并静默解锁。

进化确认通过 React Portal 挂载到 `document.body`，不进入藏品卡片或页面内容的层叠上下文。确认页固定覆盖主应用稳定视口，在 Telegram 内容顶部安全区下方开始布局，内部使用独立滚动区与位于设备底部安全区上方的固定操作区；确认页打开期间锁定根文档滚动，关闭后恢复原滚动状态。Catalog v1 产品数据构建同时生成 Web 专用的 140 条静态进化路线，打开确认页只读取该前端构建产物和页面已有库存、资产状态，不请求服务端进化预览；静态路线不参与提交后的保底或资产裁决。

正式宠物母版永久保存在 Supabase Storage 私有桶 `art-masters`，公开桶 `pet-runtime` 保存当前 420 张运行时 WebP 与尚在 90 天保留期的旧对象；Git 只保存当前/历史资源清单、SHA-256 和发布工具。正式藏品列表直接读取 API 返回的 256×256 公开桶完整 URL，主视觉读取 768×768 公开桶完整 URL；这是浏览器唯一允许的 Supabase 直连，浏览器不加载 Supabase SDK、不持有 key，也不连接 Postgres、RPC、Auth 或其他 Data API。藏品主图按 [ADR-063](adr/ADR-063-inventory-hero-image-continuity.md) 在模板切换时先显示正确缩略图预览，详情图解码成功后替换；选中详情就绪后只串行准备当前 4×2 页其他详情图，页面不活动或文档隐藏时停止开始新图片。开盒页五张 304×304 透明 WebP 收藏级近景龙形稀有度代表图不是正式藏品图片，固定存放在 Web `shared/assets/gacha-rarity/` 并随 Vercel 版本发布，不进入 `pet-runtime`、目录批次或 `CatalogImage`，生成源图和抠图中间文件不进入仓库。正式藏品读取最终失败时保持布局并显示 Vercel 内置统一宠物剪影，后台有限重试且不显示后端相关文案。进化静态路线与 operation 持久结果只保存模板身份和业务字段，运行时图片在 API 读取时统一从当前目录映射解析；已打开页面不轮询或订阅资源 revision。

Battle 页面在同一 session generation 内保持挂载，并只渲染产品第 21 章的八种页面状态。好友挑战创建按钮提交后立即进入本地 `preparing_share` 交接状态；该交接只显示非成功性玩家反馈，并持续到同一命令的权威 room 已提交给 React 页面状态，禁止在 `team_select` 与权威 `preparing_share/waiting` 之间渲染 `home`。明确失败且没有房间事实时才撤销交接并回到原队伍选择；未知结果继续停留在准备页并复用原 operation 与 room 恢复。页面可见且 room 为邀请 `waiting` 时只有创建者每 5 秒发送纯展示心跳；room 为 `lobby_waiting/lobby_countdown` 时双方每 5 秒发送 participant presence 心跳；进入 `active_turn` 后立即停止。`lobby_waiting` 沿用红蓝双方卡、90 秒重连和 5 分钟时限；数据库进入 `lobby_countdown` 后立即改为全稳定视口的红蓝对决 3 秒专用页面，固定覆盖顶部资产栏、主导航与全部产品内按钮，显示“倒计时已锁定”“离开不会取消战斗”，不渲染取消、退出、分享或重新选队动作。每次可见/激活 `/game` 时段使用一个独立 UUID lease 和数据库快照的下一 lifecycle version，lease 内命令序号严格递增。页面隐藏、Telegram `deactivated`、`pagehide` 或离开 `/game` 时立即结束 lease、中止全部在途 heartbeat、停止 UI 轮询并尽力发送同 lease offline；重新可见、激活或返回时先读取不带动作游标的 REST 快照，把表现游标初始化为 `latest_action_sequence`，再申请并使用数据库认可的新 lease。新 lease 接管后旧 heartbeat/offline 永久无副作用，安全不依赖 abort 或 offline 必达；任何新旧 lease 与页面生命周期都不改变已经锁定的 countdown deadline。Ably client 只能 subscribe，通知只携带 `event_id`、`room_id`、`state_version` 与 `event_kind`；收到通知、deadline 到达或连接失败时，battle-realtime workflow 通过 REST 携带 `after_action_sequence` 读取 viewer-specific snapshot，按 sequence 补齐动作并串行排入表现队列。邀请 waiting、接受和 lobby 每 2 秒、`active_turn` 每 1 秒短轮询；deadline 到达后的权威读取失败或仍返回同一 deadline 时，即使 Ably 仍连接也继续按该节奏读取，直到权威状态前进或终结。当前会话持有未终局 room 时，bootstrap 的空 participation 只触发 room 读取，不能清空当前房间。权威 room 立即决定按钮和倒计时，表现动画不得遮挡操作；heartbeat/offline 普通响应只应用 room snapshot，同次请求或回正实际确认退款/释放终态时才一次刷新 `battle + assets + inventory`，不得由 5 秒心跳无条件刷新 assets/inventory。
Battle 页面在同一 session generation 内保持挂载，并只渲染产品第 21 章的八种页面状态。好友挑战创建按钮提交后立即进入本地 `preparing_share` 交接状态；该交接只显示非成功性玩家反馈，并持续到同一命令的权威 room 已提交给 React 页面状态，禁止在 `team_select` 与权威 `preparing_share/waiting` 之间渲染 `home`。明确失败且没有房间事实时才撤销交接并回到原队伍选择；未知结果继续停留在准备页并复用原 operation 与 room 恢复。Battle 首次进入同步闭包只包含八状态、倒计时、REST 权威恢复、战场操作和核心样式；Ably SDK、技能轨迹播放器与轨迹样式分别动态加载。非首页权威状态和 Battle 内触摸、指针、键盘意图立即并行准备两类运行时；首页稳定后的 idle 准备只允许在前台 4G、非省流量条件下发生。Realtime token 与 Ably 模块并行取得，连接前继续既有 1—2 秒 REST 回正；技能命令不等待表现模块，下载中只显示“战斗准备中”且倒计时优先，失败则跳过重表现并继续应用权威事件。页面可见且 room 为邀请 `waiting` 时只有创建者每 5 秒发送纯展示心跳；room 为 `lobby_waiting/lobby_countdown` 时双方每 5 秒发送 participant presence 心跳；进入 `active_turn` 后立即停止。`lobby_waiting` 沿用红蓝双方卡、90 秒重连和 5 分钟时限；数据库进入 `lobby_countdown` 后立即改为全稳定视口的红蓝对决 3 秒专用页面，固定覆盖顶部资产栏、主导航与全部产品内按钮，显示“倒计时已锁定”“离开不会取消战斗”，不渲染取消、退出、分享或重新选队动作。每次可见/激活 `/game` 时段使用一个独立 UUID lease 和数据库快照的下一 lifecycle version，lease 内命令序号严格递增。页面隐藏、Telegram `deactivated`、`pagehide` 或离开 `/game` 时立即结束 lease、中止全部在途 heartbeat、停止 UI 轮询并尽力发送同 lease offline；重新可见、激活或返回时先读取不带动作游标的 REST 快照，把表现游标初始化为 `latest_action_sequence`，再申请并使用数据库认可的新 lease。新 lease 接管后旧 heartbeat/offline 永久无副作用，安全不依赖 abort 或 offline 必达；任何新旧 lease 与页面生命周期都不改变已经锁定的 countdown deadline。Ably client 只能 subscribe，通知只携带 `event_id`、`room_id`、`state_version` 与 `event_kind`；收到通知、deadline 到达或连接失败时，battle-realtime workflow 通过 REST 携带 `after_action_sequence` 读取 viewer-specific snapshot，按 sequence 补齐动作并串行排入表现队列。邀请 waiting、接受和 lobby 每 2 秒、`active_turn` 每 1 秒短轮询；deadline 到达后的权威读取失败或仍返回同一 deadline 时，即使 Ably 仍连接也继续按该节奏读取，直到权威状态前进或终结。当前会话持有未终局 room 时，bootstrap 的空 participation 只触发 room 读取，不能清空当前房间。权威 room 立即决定按钮和倒计时，表现动画不得遮挡操作；heartbeat/offline 普通响应只应用 room snapshot，同次请求或回正实际确认退款/释放终态时才一次刷新 `battle + assets + inventory`，不得由 5 秒心跳无条件刷新 assets/inventory。

Battle 浏览器网络边界按 [ADR-054](adr/ADR-054-ably-browser-csp-endpoint-allowlist.md) 固定允许 Ably 2.26 的旧兼容端点、当前 `main.realtime.ably.net` 主端点、`*.ably-realtime.com` 回退与联网检测端点，并同时覆盖 SDK 实际使用的 HTTPS/XHR 与 WSS。CSP 不用于决定业务降级；端点不可达或连接失败时仍由既有 1—2 秒 REST 回正接管，重新连接后停止固定短轮询，deadline 到达时的单次权威读取继续保留。浏览器按 [ADR-055](adr/ADR-055-battle-realtime-client-diagnostics.md) 只在 token 更新、连接或频道订阅真实失败时输出固定阶段、连接状态和整数错误码；不得记录原始异常、身份、room、channel、token、capability、请求或响应内容，也不得因此新增网络调用或改变业务状态。

Telegram `waiting` 房间的分享调用反馈只保存在内存，并以 session generation 与当前创建者 room ID 共同寻址。打开原生面板只产生非成功性的即时反馈；`USER_DECLINED` 或 callback 明确返回未发送属于可重试的本地取消，`shareMessageSent` 或成功 callback 属于发送成功，`MESSAGE_SEND_FAILED` 或 Telegram 官方等价的明确失败事件属于发送失败。官方 `UNKNOWN_ERROR` 仍是已经到达的明确失败事件，不等于“超过固定时限没有回调”。Telegram 没有为 `shareMessage` 规定 no-callback deadline，因此 Web 不新增或推断 waiting 分享 `unknown`。Web 只在本房间实际调用 `shareMessage` 后消费不含 room ID 的 Telegram sent/failed 事件；调用 callback 与事件落状态前都复核当前 generation、room、side 和 status。校验通过的 callback 或 sent/failed 事件同时表示本次原生面板流程已经结束；`/game` 已可见时，Web 立即把该信号交给既有前台恢复入口，完成信号先到时则保留到紧随其后的可见、页面显示、聚焦或激活事件。两种顺序都强制覆盖滞后的 `isActive=false`，先 REST 读取权威 room；平台已结束 lifecycle 时取得新 lease，只有 `blur` 且数据库仍确认原 lease active 时继续其单调命令序号。callback 与全局事件可能同时到达，同一分享尝试只启动一次恢复；已经结束的旧 lease 不得复用。切换房间、进入终态、离开再进入 `/game` 或重新认证时先使旧尝试和待恢复信号失效并隐藏旧反馈；延迟回调不能覆盖新房间或恢复新上下文。分享成功/失败反馈不进入 API、数据库、资产刷新或 Battle 业务裁决；唯一新增请求是 presence 本来就要求的权威回正与后续 heartbeat。

Prepared inline message 创建阶段的外部结果未知属于独立服务端路径：原 `create_operation_id`、同一 room 与同一 bearer invite 在 60 秒内恢复，超时后由数据库作废、退款并释放 reservation。该恢复不能与已经进入 `waiting` 后的原生分享 callback 混为一类，也不因 waiting 分享没有 no-callback `unknown` 而删除。

Battle 页面状态按“当前会话未离开的 viewer-specific current-room、`BTL_` 入口/本地流程、首页”依次渲染。终局 room snapshot 含数据库生成的 `terminal_result` 时显示当场结果；用户返回首页后，同一 session generation 的迟到 room、Ably、bootstrap 或命令响应不得重新打开该结果。接受成功的 room snapshot 一经应用，即使邀请刷新后回答 `accepted`，当前账号仍必须保持 lobby/current-room；只有本账号的 accept operation 返回 `BATTLE_ROOM_ALREADY_ACCEPTED` 时才渲染固定冲突。幂等回放与响应乱序以高 `state_version` 快照回正；重新加载和重新认证只恢复仍在进行的 participation，不恢复终局结果，邀请入口不得覆盖数据库参与事实。

## Functions

根目录 `api/app.ts`、`api/integrations.ts`、`api/jobs.ts` 是三个薄适配器，只创建 `@pokepets/api/entrypoints` 网关。每个 entrypoint 显式注入本网关的 route registry 与完整 handler map；三个 registry 互不导入。当前 App 注册表只路由非钱包、非 Mint 端点，当前 Jobs 注册表只路由三项非 Mint job；钱包、Mint 与 `reconcile-mints` 的契约和 handler 源码保留在休眠集合，但路由匹配和服务端 OpenAPI 均不包含它们。玩家请求按“路由匹配、网关认证、本地会话凭证证明、契约输入解析、领域查询或工作流、业务 RPC 数据库会话与入口交接裁决、契约输出解析、标准信封”执行；本地证明不读取数据库，只提取受 HMAC 保护的 `session_id`。只有 `referral.bind` 和 `operations.get` 声明 `allowPendingEntryHandoff`，对应例外只由数据库执行。

`apps/api/domains` 不跨领域组合业务，每个 handler 只完成输入映射并调用一个 RPC；`identity.authenticate` 是唯一例外，固定先调用验签前来源限流 RPC，再调用验签后登录事务 RPC。入口交接完成的正常结果在认证事务提交后由同一 Function 追加一次只读 `identity_initial`，因此认证裁决固定为两次 RPC、正常完整首屏固定为三次 RPC 且浏览器只有一个认证请求；推荐入口仍为 `pending` 时不执行第三次读取。支付、退款、Mint 对账、定时任务和操作恢复进入 `apps/api/workflows`。Battle app handlers 只调用 viewer-specific 读取或单个 Battle RPC；`battle-share` 与 `battle-outbox` 分别属于 integrations workflow，并通过受保护 RPC 领取任务。Functions 不计算价格、奖励、库存、资产归属、Battle 命中/伤害/终局或最终交易结果。

玩家 `battle.*` 与 `battle.outbox_integration` 的请求终态日志按 [ADR-028](adr/ADR-028-battle-request-observability.md) 输出鉴权、输入解析、handler、响应、数据库 RPC 和 Ably 累计耗时；outbox 正常响应同时输出 processed/published/deferred。采集只贯穿既有调用，不改变玩家请求与 outbox integration 的异步解耦。

契约包 `/app`、`/integrations`、`/jobs` 分别服务三个网关；`/server` 只用于 OpenAPI 与服务端静态校验；`/common` 提供不加载路由注册表的信封、错误和基础路由类型。浏览器 `/app-client` 按领域异步加载活动 route Schema，`/app-client/errors` 按需提供错误文案与恢复策略；`/dormant-app` 只为 Wallet/Mint 休眠源码保留静态类型，不进入活动 Web、App Function 或 OpenAPI。

## 部署

Web 与三个 Functions 位于同一 Vercel Pro Project，Functions 运行时为 Node.js 24。Battle 服务端发布使用 Ably Standard，数据库通过 `pg_cron` 每秒推进 deadline，并由 `pg_net` 唤醒两个受保护 integrations；Ably 不承担业务权威。非宠物美术和统一剪影随 Vercel 发布；宠物运行时对象以 v2 内容哈希键、禁止覆盖和 `public, max-age=31536000, immutable` 响应独立发布到 Supabase Storage，当前批次由数据库原子指针决定。`GET /api/catalog` 只以 `no-store` 返回当前 checksum、release key 与 revision；完整目录由 checksum + release key 唯一路径返回，并同时使用浏览器一年不可变缓存与 Vercel CDN 一年缓存。发布与回滚不 purge 目录或图片缓存。每日 Vercel Cron 调用受保护的公开对象清理工作流，数据库在与发布/回滚共用的耐久租约下领取最多 500 个到期对象，再由 Function 使用 Storage API 删除并回写逐批结果。`contracts/ton` 的 typecheck 先从 Tact 源码生成被 Git 忽略的绑定，正式发布再运行独立 `pnpm chain:build` 门禁。真实开发环境与未来生产环境使用同一 Git commit、migration 序列、OpenAPI 和 Battle checksum，只使用环境隔离的 Storage 桶、项目域名、Bot、Ably key、callback URL 与机密。

受控 Battle 验收夹具没有 Web、Function、Cron 或 Data API 运行时。它只通过数据库 owner 通道执行；迁移后的默认状态没有数据库身份绑定和 enable 记录。真实开发数据库重建后显式绑定 `real_development` 与当前 project ref，并使用不超过 24 小时的非秘密门禁；生产身份不能启用。
