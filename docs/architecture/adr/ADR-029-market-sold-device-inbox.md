# ADR-029：市场成交事件游标与当前设备 SOLD 收件箱

## 状态

已接受。

## 背景

市场管理读模型只返回仍有剩余数量的本人挂售。部分成交只能从聚合剩余数量间接看到，全部售罄后卡片消失，因此卖家无法识别每次新成交。产品已经裁决：每次成功交易为卖家生成一张独立 SOLD 卡片，置顶到当前设备用户点击隐藏；只要仍有未隐藏提醒，“管理”页签就显示红点，点击页签不清除红点；Mini App 关闭期间的成交也必须在再次打开后出现，但首次建立本地状态、清缓存或换设备后不补发更早历史。

## 决定

数据库在 `api.market_purchase` 的同一结算事务内，为每名实际卖家写入一条不可变成交事件。`market.seller_sale_sequences` 按卖家保存最新严格递增序号；`market.seller_sale_events` 以 `(seller_id, sequence)` 为主键，并以 `(seller_id, trade_id)` 唯一约束确保同一卖家同一交易只产生一条事件。同一交易从该卖家的多条 FIFO 挂单成交时，先按卖家聚合数量再写事件。涉及多个卖家时按卖家 UUID 稳定顺序锁定并递增序号，避免反向锁序。事件保存模板、真实成交数量、真实成交单价和成交时间，不保存或返回买家身份。

`api.market_create_listing` 在卖家首次上架时保证序号行存在。`api.market_my_listings` 是唯一管理读取 RPC，输入 `p_after_sale_sequence bigint default null`，先对当前卖家序号行取得共享事务锁，再返回当前有效挂售聚合、最多 100 条新成交事件、最新可继续读取的游标与 `has_more`；成交事务只有在挂售和事件均已写好后才更新同一序号行，因此管理读取不会组合“新 SOLD + 旧剩余数量”或“新剩余数量 + 旧游标”。未传游标时返回空事件和当前卖家最新序号，用作本设备首次基线，不补发历史；传入游标时只返回更大序号并按升序分页。HTTP 契约使用十进制字符串传递序号，避免 JavaScript `bigint` 精度丢失。服务端仍通过 session 重新确定卖家身份，前端不能提交卖家 ID。

Web 以版本化 `localStorage` 键和当前 session 的内部用户 UUID 隔离保存 `{ version, cursor, pendingEvents }`。读取后必须逐字段验证，损坏或越界内容整体丢弃并重新建立当前基线。合并响应时按事件序号去重，在同一次本地写入中同时保存新事件与推进游标；本地写入失败时不得只持久推进游标。SOLD 卡片按序号从大到小展示在当前挂售之前；`pendingEvents.length > 0` 同时直接驱动共享“管理”页签右上角的红点，不持久化第二份已读标志。点击整张 SOLD 卡片后，Web 立即在不接收触摸事件的顶层播放 12 枚立体 K-coin 的约 900ms 本地装饰动画，金币先分散到顶部资产栏至当前可见 SOLD 卡片的上半屏范围，不显示运动轨迹，再汇聚到顶部 K-coin 胶囊。点击 600ms 后才从内存移除并尝试持久化，不调用 API，不改变 K-coin 余额；只要仍有其他事件红点就保留，最后一条事件移除时红点同步消失。持久化失败时本次界面仍在 600ms 后隐藏并更新红点，但重新加载后允许再次出现。

交易页进入、重新可见、网络恢复时立即读取；页面保持可见时无论购买、出售或管理页签是否激活都每 10 秒读取一次，离开交易页或页面隐藏时停止，使用户在进入管理页前即可由红点发现成交。若一次响应 `has_more = true`，Web 立即使用返回游标继续读取，直到追平，再进入 10 秒周期。当前挂售和事件来自同一个 RPC 响应，防止先显示 SOLD 而仍展示旧剩余数量。

本地收件箱不是资产或成交事实，不能确认、删除、撤销或改变服务器记录。它不保存访问令牌、Telegram `initData`、买家身份、余额或库存。清除浏览器缓存、换设备或本地状态损坏后允许丢失旧提醒，并从服务器当前最新序号重新建立基线。

## 结果

卖家可以在当前设备稳定看到 Mini App 关闭期间产生的每笔新成交，并在交易页任一页签通过“管理”红点发现未确认提醒；部分成交同时回正剩余数量并拆出独立 SOLD 卡片。服务器只保存可分页的成交事件事实，不保存设备是否已读；SOLD 卡片和红点状态完全由同一当前设备待展示集合决定，符合用户已裁决的数据范围。
