{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "tmaGame Vercel API TypeScript Config",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["api/**/*.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "build",
    ".turbo",
    ".vercel",
    "coverage",
    "playwright-report",
    "test-results"
  ]
}
